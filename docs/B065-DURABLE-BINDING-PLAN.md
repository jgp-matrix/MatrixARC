# B065 — Durable ARC↔BC binding (kill the positional-mapping class) — build-ready plan

**Coach scope · 2026-07-27 · master @ prod v1.24.38 · `src/app.jsx`.** Design only. ROOT fix for the whole positional-drift class (Ryan 20510, the wrong-line-overwrite money risk, relink off-by-one). Money-path — HIGH stakes.
> Reconciliation note: this plan read *master*; its flag that the on-open path still stamps the hash unconditionally is moot — **B067 (on branch `claude/b067-sync-hash-honest`) already fixes that on-open site**; it just isn't merged yet.

## ★ BUILD STATUS (2026-07-27) — split into Phase 1 (shipped-ready) + Phase 2 (tested follow-up)
**Phase 1 — FOUNDATION (branch `claude/b065-durable-binding`, Test V.059, Coach APPROVE-WITH-NITS, Jon-approved to ship):** S1 additive storage/helpers (`panel.bcTaskNo`/`row.bcLineNo`, allocators, `resolveBcBindings` — all dormant) · **S3c the money-path DELETE part-identity guard** (both loops — never delete a line whose part# matches a live row; delete failures now surface, feeding B067) · S5 relink off-by-one fix + copy/relink binding-clear. Coach verified: additive fields survive save/load, helpers truly dormant (no money-path call sites), zero behavior change on a working sync, DELETE guard biases only toward keeping (transient duplicate, never a dropped part).

**Phase 2 — DEFERRED (needs live-BC validation on a ≥3-panel bound project — a wrong binding baked in blind = Ryan's exact bug):** S2 populate bindings from confirmed 2xx · S3a/S3b make resolver the read authority + assign Line_No from bindings · S4 convert ~27 call sites · S6 wire on-open lazy migration + `bcBindingUnresolved` destructive-write block.
**★ Coach MUST-FIX-before-Phase-2 (in dormant code today, harmless; tagged so they can't get lost):**
1. **[MED, before S6] `:3948` resolver Description-discriminator:** posting-task Desc is `${drawingNo||pfx} Rev …` (pfx=`PRJ###-{N*100}`); resolver keys panel side on `_leadToken(drawingNo||drawingDesc||name)` → aligns ONLY when the panel has a drawingNo. No-drawingNo panels degrade to positional AND the unique pfx tokens defeat the ambiguity flag → **Ryan's no-drawingNo/duplicate case would auto-bind positionally, defeating the locked ambiguity guard.** Key both sides on the same normalized field (or compute pfx panel-side) before enabling S6.
2. **[MED, before S2] `:4279`/`:4452` cross-task Line_No collision:** `_b065RowBound`/`_b065EcoRowBound` scan ALL `panel.bom` for `bcLineNo===ln`; base + ECO number 60000+ independently → once populated, an ECO row bound to 60000 would falsely protect a base orphan (and vice-versa). Scope each scan to the task's own rows.
3. **[LOW, before S2] `:40681` relink clears bindings AFTER the sync loop** — move ahead of the loop (harmless now; stale once S2 lands).
4. **[NIT] `:3953` `descTokenCounts` dead code** — drop or wire in.
Plus: when S2 lands, audit addPanel/duplicate-panel paths for stray `bcTaskNo` carry (same as copy/relink).

## Core defect
Every BC task# (`20000+panelIndex*100+10`) and line# (`60000+rowIndex*10000`) is a pure function of ARRAY POSITION, recomputed each op. No durable panel→task or row→line link. The read-back diff keys on the recomputed number (`existingByLineNo :3893` → PATCH `:4029` / DELETE `:4063` with `If-Match:"*"`, no part-identity guard). So a BOM-row insert/delete/reorder shifts every downstream line# → next sync PATCHes the wrong BC line + DELETEs the tail = **wrong part ordered / real part dropped, silent.** Same root = Ryan's 20510 404 storm.
**Template already in-codebase:** service cards persist `bcProjectTaskNo` (`allocateServiceCardBcSlot :1321`, `createServiceCard :1356`) and READ it (`bcSyncServiceCardTask :3622`) instead of recomputing. Panels carry a vestigial unused `panel.bcProjectTaskNo` (`:11107`).

## Design — three primitives
1. **Durable bindings (persisted, preserve-on-save):** `panel.bcTaskNo` (the `20N10` posting slot; written only after a confirmed 2xx create), `row.bcLineNo` (assigned at first successful POST; never recomputed, never reused).
2. **Shared read-only resolver `resolveBcBindings(projectNumber, panels, {mode})`:** 1 GET for BC's actual tasks + 1 for all lines; confirm/discover each panel's task (stored → Description match → positional fallback) and each row's line (part# `No` within the task). `mode:'audit'` = mismatch count without writing = **F070's read path (build once, share).**
3. **Resolve-don't-compute + fail-loud self-heal:** look up stored binding; if absent discover+persist; on 404/not-found during an op → re-discover, re-bind, and push to `result.failed` (feeds B067/B064) rather than trusting the increment or minting a phantom block.

## Ordered build steps
**S1 Storage+helpers (no behavior change):** keep `panel.bcTaskNo`/`row.bcLineNo` across save-reload (additive; confirm no BOM-normalizer/panel-sanitizer strips unknown keys; add to preserved-flags comment). Copy/relink must CLEAR bindings (fresh BC project). New `allocatePanelBcTaskNo`/`allocateRowBcLineNo` (lowest-unused-slot, mirror `:1321`) so a mid-list insert gets a FRESH number, not a collision. New resolver module + thin `resolvePanelTaskNo(project,panel)`.
**S2 Populate at create (read back from 2xx):** `bcCreatePanelTaskStructure.postTasks :3271` + `bcCreatePanelTaskBlock :3330` capture the created task# from the 2xx, return `panelIndex→{beginNo,postingNo,engNo,endNo}`; callers persist `panel.bcTaskNo=postingNo`. `bcSyncPanelPlanningLines :4049` on a new-line 2xx writes `row.bcLineNo=line.Line_No`. New-project `:11271` + addPanel `:36488` persist the returned task#.
**S3 Binding-driven money-path sync (the wrong-line fix):** in `bcSyncPanelPlanningLines :3814` + `bcSyncEcoPanelPlanningLines :4091`: resolve taskNo via `resolvePanelTaskNo` (not `:3834/4094`); assign each row's `Line_No` from `row.bcLineNo` (allocate fresh only if unbound; remove positional counter as authority `:3945/4126`); match existing by bound `Line_No`; **DELETE only if no ARC row is bound to it AND its part# matches no live row** (add part-identity guard before the `If-Match:"*"` DELETE `:4063/4218`) — this single change closes the money-path wrong-line-overwrite; on per-line 404, re-resolve by part# within task, rebind, else push to `result.failed`.
**S4 Convert ALL remaining call sites (cover-all-X):** task-compute sites (8): `:3253/3332/3786/3834/4094/6312/6370/27054`; line-compute (2): `:3945/4126`; panelIndex-passing callers (17): `:11291/11354/16231/16232/21314/25974/27104/27106/27124/27174/28802/35540/36488/36602/40272/40833/42075/42314/41755` + offline-queue `:7096`. **Recommended shape:** pass the panel OBJECT (now carrying `bcTaskNo`) to sync/patch fns, resolve inside, keep `panelIndex` only as a discovery hint — so a caller physically cannot pass "just a number."
**S5 Relink off-by-one (C1):** `relinkToBC :40420` passes 0-based `i` (everything else `i+1`) → writes panel i+1's lines onto panel i's task + phantom `20010`. Fix the arg + clear all old bindings first (new BC project).
**S6 Backward-compat / lazy migration (no bulk rewrite):** existing projects have no bindings. On next successful sync / on-open (`:40225`), `resolveBcBindings(mode:'resolve')` once per project → discover + persist. Match precedence: task Description → positional fallback; line: part# within task. **Ambiguity guard (Ryan's exact case):** duplicate panel names + a shifted block make matching non-unique → **bind only the unambiguous ones, flag `project.bcBindingUnresolved:true`, surface on the B064/F070 chip, require a Jon-confirmed reconcile before any destructive write. Never auto-bind-then-PATCH/DELETE.** Migration is additive-only (never deletes/renumbers).
**S7 Self-heal surfacing (coordinate, don't re-scope):** feed resolver not-found into B067's `result.failed`; build S1's resolver AS F070's read path (persist binding = double-duty, audit Q5=yes); RFQ-only pricing gate `:27085` still short-circuits (bindings won't populate for unpriced panels — migration waits for pricing); ECO tasks (`base+30+(eco-1)` `:4095`) get the same per-(panel,eco) binding; F069 env orthogonal. Also fold `deletePanel :36552` never-deletes-BC-task (orphan-by-omission) here or into B064.

## Repro / acceptance
BC-bound test project ≥3 panels × ≥3 rows (bindings populated): (1) reorder panels → each panel's task+lines untouched; (2) **insert a BOM row → existing rows keep `bcLineNo`, only the new row gets a fresh Line_No, NO existing BC line PATCHed to a different part, NO tail DELETE** (the money-path assertion); (3) delete a row → only that bound line deleted (part-identity confirmed); (4) delete+re-add a panel → new panel gets a fresh task#, survivors unmoved; (5) manually delete a BC posting task → open → re-discover by Description, re-bind or fail-loud (no silent 404 storm/phantom); (6) relink → old bindings cleared, tasks 1-based (no phantom 20010); (7) legacy project (no bindings) → open → discovered+persisted once, subsequent opens no-op.

## ✅ DECISIONS LOCKED (Jon 2026-07-27)
1. **Migration / discovery + ambiguity:** discovery key = task **Description** (panel name/drawing#) primary, positional fallback. On an ambiguous case (duplicate panel names / task-count mismatch, e.g. PRJ402141): **bind only the unambiguous panels, and BLOCK destructive BC writes on that project + flag `bcBindingUnresolved` until Jon confirms a reconcile.** Never auto-guess a binding that could drive a PATCH/DELETE. (Step 6c.)
2. **Row reorder:** **keep bindings STABLE** — an ARC-side BOM row reorder does NOT renumber the BC lines (correctness over cosmetic order). Eliminates the wrong-line-overwrite/dropped-part class. (Step 3b.)
3. **Part# uniqueness:** the same part# **CAN repeat** within a panel → the line-discovery matcher must use **part# + Description + residual order** as the tiebreak (part# alone is NOT a unique key). Apply in Step 3c/6b matching. The stored `row.bcLineNo` anchor is unaffected (already row-specific); this only affects the initial discovery/migration match.
4. **Row anchor:** **`Line_No`** (composite `Job_No`+`Job_Task_No`+`Line_No`). ✅ BC-probed 2026-07-27 — `Project_Planning_Lines_Excel` exposes **no SystemId/GUID** (only `@odata.etag`, not a durable key), so Line_No is the anchor. Stable from ARC's side (no renumber per #2); a BC-side Line_No renumber is the only residual risk → covered by the fail-loud self-heal (re-resolve on 404). Do NOT rely on `@odata.etag` as a durable key (it changes on every edit).

# B092 + B093 — PRJ402505 fresh-extraction findings

> Coach lane (read-only), via Freddy · 2026-08-04 · prod v1.24.88 · MONEY-PATH. No live data — each finding carries the decisive runtime artifact to confirm on a live re-extract.

## B093 — extraction left in-BC parts unmatched (BLUE circles)

### The tell: BLUE, not RED
`_bcCircle` (src/app.jsx:33597-33612) priority: matched (bcNo/bcVerify=in-bc/priceSource=bc)→null; `bcVerify.status==="not-in-bc"`→RED; `fuzzy && !suggestions`→YELLOW; else `!readOnly`→**BLUE (fallback)**. BLUE fires when **`bcVerify` is ABSENT**.

Both pricing paths stamp `bcVerify` on every priceable row in a Phase-6 pass (fg `runPricingOnPanel` :31908-31921; bg :17472-17485), assigning `not-in-bc` to unmatched. **A genuine fuzzy miss → RED. These are BLUE → the BC-match/pricing phase never ran to Phase-6.** (Corroborated: the rows that DID price went via ARC-Cross/alternates at extraction time (:12884), independent of pricing; every non-crossed row = no price + no bcVerify = BLUE. Signature of pricing-never-ran, not 15 individual fuzzy misses.)

### Fuzzy itself is fine — Fix-1 already live
The plan's cross-field Fix-1 is shipped (`57548a52`/`2a9c8037`, :5847-5883): step 4b `bcSearchItems(pn,{field:"both"})` = the Item Browser path (queries Vendor_Item_No/Common_Item_No, B063 uppercasing :5763-5766). Traced `3044102` / `SH1B-05C` / `ALD2QH211DNUW` → all auto-match via step-4b IF the phase runs. So they missed because the phase didn't execute — not a fuzzy defect.

### Candidate mechanisms (confirm with live artifact)
1. **Pricing bailed early (most likely):** `runPricingOnPanel` returns at :31345 if `!_apiKey`; bg at :17302. Extraction is server-side now, so it can succeed while an empty client `_apiKey` makes auto-pricing bail before any BC work → no Phase-6 stamp → all non-crossed rows BLUE.
1b. **Pricing threw before Phase-6:** post-extract call :28684 is non-standalone; a throw → catch :31975 → rethrow :31985 → swallowed by extraction `.catch` :28688 as "✓ Extraction done." Phase-6 never runs → BLUE.
2. **Yellow→blue color-gate inversion (genuine separate bug):** :33609-33610 — a `fuzzy` row WITH suggestions loaded renders BLUE (gate `fuzzy && !suggestions` is false) instead of YELLOW. If the phase ran and held suggestions, they'd show blue wrongly.

### Decisive live artifact (re-extract PRJ402505 in the controlled tab, console open)
1. `!!_apiKey` — false ⇒ Mechanism 1.
2. Presence of `"BC: Matching N items…"` (:31426) / `"[BG PRICING] BC: X matched…"` (:17405). Absent ⇒ phase didn't run (Mech 1/1b).
3. `panel.bom.find(r=>r.partNumber==='SH1B-05C').bcVerify` — null ⇒ Mech1/1b; `{status:"fuzzy"}`+suggestions ⇒ Mech2; `{status:"not-in-bc"}` ⇒ would be RED (contradicts screenshot).
4. Debug Logs `"[F019] pricing failed"` / `"BC pricing phase failed"` ⇒ 1b.

### Dedup: NOT the known fuzzy-miss plan
`docs/B-bcFuzzyLookup-misses-fix-plan.md` targets per-row misses (→ RED) and Fix-1 is already shipped. B093 is BLUE/unstamped/wholesale = different root (phase-not-run or color-gate). Treat as its own bug. (Plan's unshipped Fix-2 (step-5 prefix :5900) still worth doing for separator-in-prefix parts, but doesn't explain B093.)

### Proposed fix (confirm mechanism first)
- **Primary:** guarantee the post-extraction pricing/BC-match phase runs to Phase-6. If `_apiKey` gates it, decouple BC matching (server/BC-token) from the Anthropic `_apiKey` early-return, OR surface a hard error instead of silent bail (mirror `_bcWasUnavailable` toast :31963). 
- **Secondary (if Mech2 confirmed):** fix the color gate :33609-33610 so fuzzy+suggestions → YELLOW.
- Do NOT touch the bcFuzzyLookup cross-field/exact-equality gate — correct + load-bearing.

## B092 — project routes to RFQ SEND/RECEIVE not (BOM) IN PROCESS

### Root cause: computeProjectEffectiveStatus :18721
`if(hasBom&&(anyRedRow(project)||hasActiveRfqs))return"rfqs";` → "rfqs" maps to process_rfq (statusToCol :18494). With NO RFQ sent (hasActiveRfqs=false, rfqSentDate unset :18696), **`anyRedRow` ALONE** flips the project to the RFQ column. Unpriced rows are red (`_isBomRowFlaggedRed`: unitPrice===0 :18145, missing priceDate :18153) → the B093 blue/unpriced rows are red → routes to "rfqs". F026/B044 overcorrection: conflates "not yet priced" with "needs a vendor RFQ".

### Relationship: partly a symptom of B093
Fix B093 → in-BC parts price → most reds clear → project stops mis-routing. But B092 is independently real (any project has some genuinely-unpriced pre-RFQ rows).

### Proposed rule (product-call tradeoff for Jon)
```
if(hasBom&&hasActiveRfqs)     return "rfqs";        // an RFQ is genuinely out
if(hasBom&&anyRedRow(project)) return "in_progress"; // red but no RFQ sent = still being worked
if(readyToSend) return "evc_send"; if(readyToReview) return "evc_review"; return pipelineStatus;
```
Still honors B044 (red never reaches evc_send/review — red check sits above them). **Tradeoff:** if a priced-complete-but-red project (reds genuinely needing vendor quotes) SHOULD surface in RFQ before an RFQ is sent, gate that on send-readiness (`readyToReview && anyRedRow`) rather than raw redness. Jon's product call.

### Decisive artifact
On PRJ402505: `computeProjectEffectiveStatus(project)==="rfqs"` + `anyRedRow(project)===true` + no row has `rfqSentDate`.

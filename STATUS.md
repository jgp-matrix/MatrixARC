# 📊 Team Status Board (live) — owned by Dez

> **Owner (sole writer): Dez.** Freddy (hub) pings Dez a compact status block on each work-state change;
> Dez displays it live in her session (Jon glances **here**) and appends timestamped snapshots to the
> Progress Log below as the permanent record. One-writer-per-file — Dez only (per G003, 2026-07-02).
> Format: `B/F/G### — Title` / `• one-liner` / `• STATUS: who's doing what now`.

## Current — 🔴 2026-07-23 · prod v1.24.15 · EMERGENCY (PRJ402119 pricing)

> ## ✅ SHIPPED PROD v1.24.15 (release `7efbfad1`) — B052 + F045 (PRJ402119 recurrence-prevention, round 1)
> **B052** = BC poll divergence guard (the direct preventer — 5-min poll no longer silently applies a large downward BC swing; keeps price, flags `bcPollDivergence` → now renders RED via `_isBomRowFlaggedRed` + trips send-warning). **F045** = Budgetary checkbox gated to Manager/Admin (client-side; server-hoist fast-follow). Both Coach-reviewed (B052 APPROVE-WITH-NITS, nits fixed). Hosting-only.
> **⏳ Jon prod-verify:** F045 (non-manager sees greyed Budgetary + tooltip). B052 effect is non-visual until the poll catches a divergence (then red row). **Still building:** F050 → F044 → F048 → F049 → F045-server.
> **✅✅ FORENSICALLY CONFIRMED (live prod read via controlled tab, 2026-07-23):** Panel 3 Row 4 = SCE-60XEL4912SS6LP (SS316 enclosure, the ~$6000 item), `unitPrice:12`, **`priceSource:"bc"`**, verified in-bc 06-19, vendor Galco/V00233. **BC LIVE LOOKUP: BC itself holds $12 right now.** Root cause = **bad BC master data** ($12 for a $6000 enclosure) + ARC has **no magnitude-plausibility check** → pulled cleanly, not red, shipped. NOT a poll-revert; NOT a manual typo; NOT recoverable to a person (originates in BC). Timeline: built 06-02, priced 06-19, quoted 06-22. Full forensics → `docs/PRJ402119-PRICING-INCIDENT.md`.
> **⚠ REPRIORITIZED:** **F050 (plausibility/magnitude check) is now #1** — the ONLY fix that catches a bad-magnitude BC-sourced price (candidate signal: BC price vs AI-estimate divergence). **B052 (shipped) would NOT have caught THIS** (price was $12 from first pull, not a revert) — still valid for reverts. F044 wouldn't either ($12 not red). **ACTION for Jon/purchasing: fix the $12 BC purchase price for SCE-60XEL4912SS6LP (Galco/V00233) — ARC keeps pulling it until corrected.**
> **F050 build STARTED (Jon greenlit F050 + an AI-estimate divergence SWEEP).** 🟩 Marc lane confirming the pivotal prerequisite: is a per-row **AI price estimate persisted** (survives a BC overwrite) so a BC-vs-AI plausibility comparison is even possible on existing rows? If not, lane proposes the best implementable signal (store aiEstimate going forward / on-demand AI re-estimate / heuristic / consume `bcPollDivergence`). Then wiring via `_isPriceImplausible` SSOT → `findIncompleteQuoteItems`/`_isBomRowFlaggedRed` (red + send-block) + a read-only sweep (possibly reusing `runPricingAudit`'s ARC-vs-BC drift for existing data). No build until the reference signal is confirmed.
> **✅ F050 scoped → `docs/F050-PLAUSIBILITY-CHECK-SCOPE.md`. PIVOTAL FINDING: NO stored AI estimate exists, AND AI estimation SKIPS BC-priced rows** (`:28768`) — so "BC-vs-AI" is impossible on existing data without new AI calls. B052's flag AND runPricingAudit BOTH miss this incident (no swing; ARC==BC). ⇒ F050 needs an ABSOLUTE AI-based signal. **Freddy rec = HYBRID: store durable `aiEstimate` going-forward (instant/free send-check) + on-demand AI re-estimate for the retroactive SWEEP** (catches PRJ402119 + in-flight quotes now). Plan: P1 `_isPriceImplausible` SSOT hard-block · P2 store aiEstimate on all rows · P3 sweep cloned from `runPricingAudit`. **✅ Jon locked: D1 HYBRID, D2 ratio 0.5, D3 HARD BLOCK.** Build order (sweep-first): P3 sweep (read-only, immediate value) → P2 bank `aiEstimate` → P1 send-time hard-block. ✅ Phase 3 BUILT + on **Test V.047** — `runPlausibilitySweep`/`collectPlausibilityCandidates` + `PlausibilitySweepModal` + admin button (Settings, beside Pricing Audit). Read-only, purely additive (1 const + 2 fns + 1 component + 1 hook + 1 button), advisory-review clean. Two-phase: Scan Projects (no AI) → shows N rows/~M Sonnet calls → Run AI Sweep → flags rows priced <50% of AI estimate (HIGH <20%), worst-first, CSV export. Lives in the **API Setup** modal (grouped with Pricing Audit / Pricing Sync Reports — consistent pricing-tools home), admin-only. **z-index fix (Test V.048):** sweep modal opened BEHIND the API Setup modal (sweep `zIndex:1000` vs API Setup `9999`) → bumped to `10001`; **also fixed the identical latent bug in PricingAuditModal** (same clone source). **⏳ Jon: hard-refresh to V.048, run it** (API Setup → Run Plausibility Sweep). Coach review before prod (read-only, low-risk). Then P2 (bank aiEstimate) + P1 (send hard-block).
> **✅ Jon fixed the BC prices** on the bad items (root-cause data corrected in BC — ARC won't pull $12 going forward). Sent PRJ402119 quote committed/unchanged; its working BOM still $12 until re-priced (Jon's call).


> ## 🚨 2026-07-23 — PRJ402119 PRICING INCIDENT (money-path emergency) — investigation + F044–F047
> **What happened:** a quote for PRJ402119 went to a customer with wrong pricing + red rows — a ~$6000 item priced at **$12**. Major financial loss. Jon's key question: **human error vs. the recent BC-write-failure bug** (correct price shown on screen but never persisted → false sense of security). That distinction drives the fix.
> **Investigation (🏈 Coach lane RUNNING, read-only):** (A) does ARC store WHO entered/approved a row's price? (feasibility of the trace + Soft Fix) (B) the BC-write bug window — dates/symptom/fix commit + which timestamps place PRJ402119 in/out of it (C) current red-row send-block state (D) budgetary flag + role model (E) scope a full user audit trail. **Trace of who entered Line 3 Rows 4 & 54 needs LIVE data (Jon) — feasibility TBD on whether attribution is even stored.**
> **Fixes filed (NO build until investigation + Jon priority):** **F044** Hard-Fix 1 = block Quote send if ANY red row (any reason), all send paths. **F045** Hard-Fix 2 = Budgetary checkbox gated to Manager/Admin (greyed + tooltip for others; + Firestore-rules guard). **F046** Hard-Fix 3 = full user audit trail (who-did-what across all mutation paths). **F047** Soft-Fix 1 = Priced-Item hover popup shows the User who approved the price sent to BC (depends on F046 attribution).
> **BOM-lock thread (🏈 Coach lane #2 RUNNING):** Jon wants a sent BOM fully LOCKED (qty/price/lead-time/vendor) — NO auto-pricing updates (incl. auto-check on project open). He can't confirm whether PRJ402119's current BOM == the quoted BOM (may have changed post-send via auto-pricing or the BC-write bug) → possible ACTUAL ROOT CAUSE. Lane investigating: is a sent BOM locked today (likely NOT), every auto-mutation path (esp. auto-pricing-on-open), and whether a QUOTED-BOM SNAPSHOT exists to compare quoted-vs-current for PRJ402119. Sanctioned post-send change = ONLY a PO-receipt modal (quoted vs current cost, salesman confirms before accepting PO). Filed **F048** (lock BOM on quote-send + suppress auto-pricing) + **F049** (PO-receipt quoted-vs-current cost-confirm modal; needs a quoted snapshot that may not exist yet).
> **✅ BOTH Coach lanes reported → full findings `docs/PRJ402119-PRICING-INCIDENT.md`. CONVERGENT ROOT CAUSE (likely, needs live confirm): NOT human error.** The 5-min BC price poll (`pollBcPricing:25259`) silently overwrites a `priceSource:"bc"` row's `unitPrice` with BC's value + fresh date, `_noBumpWrite` (no rev bump). During the B013 BC-401 silent-write outage (bad window = before ~2026-07-11; fully closed 2026-07-22), a user's $6000 that never reached BC would be reverted to BC's stale $12 invisibly — not red, not send-blocked. `runPricingAudit:5663` is the built-in detector.
> **Key hard truths:** (1) **who typed $12 = NOT recoverable** — no price-write is attributed; if poll-revert, no human typed it. (2) **Jon's Hard-Fix 1 (block-on-red) would NOT have caught this** $12 (fresh, non-zero, firm → passes every gate); the real hole is a magnitude/divergence check that doesn't exist. (3) **NO quoted-BOM snapshot** → can't programmatically compare quoted-vs-current (sent PDF is the only record). (4) Budgetary server-enforcement needs a schema hoist. (5) Debug Logs weak (BC failures are warn→breadcrumb-only).
> **Reframed priority:** F048 (lock sent BOM + kill auto-pricing) = THE preventer · poll audit-logging (F046 slice) · NEW plausibility/divergence check · F049 (snapshot+PO reconcile) · F044 (block-on-red, hygiene) · F045 (budgetary gate) · F047 (hover approver).
> **Jon's answers (2026-07-23):** L3 = 3rd PANEL, rows 4/54 = BOM rows. **The $12 total MATCHES what was quoted → the bad price was baked in PRE-SEND** (not a post-send drift). Project created **June 2 2026** = deep in the B013 window (pre-July-11 fix) → strongly supports the poll-revert-during-B013 hypothesis. Quote already sent + committed (no re-issue; loss accepted) → focus = **prevent recurrence ASAP**. Debug logs only reach July 14 = the viewer's **500-row cap** (`:48259`), NOT data loss — June logs likely still in Firestore (but won't hold the failure: BC errors are warn→breadcrumb-only). Filed **G018** (Debug Logs viewer 500-cap / can't reach older logs — add date-window/pagination).
> **⚠ REFRAME (important):** since corruption was PRE-send, **F048 (post-send lock) would NOT have caught THIS**; and F044 (block-on-red) wouldn't either ($12 was fresh/non-zero/firm). The real preventers for THIS class = **(1) poll hardening + logging** (stop/guard the 5-min poll silently reverting a user-confirmed price; log before→after) + **(2) a plausibility/divergence check**. B013 itself already fixed (Jul 11/22) but the poll behavior is a standing latent risk.
> **BUILT: F045 client-side budgetary→manager gate on Test V.044** (isManager() incl admin; greyed + "Only managers can set quotes to budgetary" tooltip; Jon-confirmed). Server enforcement (schema hoist) = fast-follow.
> **✅ Jon greenlit the reframed order (all 5).** Build program: **B052** (poll divergence guard — THE preventer) → **F050** (send-time plausibility/divergence check) → **F044** (block-on-red) → **F048** (lock sent BOM) → **F049** (snapshot+PO) → **F045 server-hoist** (fast-follow). F045 client-gate = done.
> **BUILT: B052 on Test V.045** — `pollBcPricing` no longer auto-applies a large DOWNWARD BC swing (BC < 50% of current & ≥$5 drop): keeps current price, stamps `bcPollDivergence` flag, logs (console.error→persisted debugLogs). Routine changes still apply + logged. Additive field, preserves B016/B041 semantics. **🏈 Coach review lane RUNNING on the B052 diff (money-path gate) + scoping F050.** No prod until Coach APPROVE + Jon go.
> **Numbers stamped:** B052 (poll guard), F050 (plausibility check), G018 (debug-log 500 cap). Live-confirm snippet = deprioritized (evidence already strong; Jon: "whatever you think").
> **✅ Coach reviewed B052: APPROVE-WITH-NITS — ship to prod.** Confirmed: guard correct ($6000→$12 caught; no false-flag on first-pricing or BC-$0), preserves B016/B041 semantics, `bcPollDivergence` additive/data-safe, logging persisted, threshold sound (errs to over-quote). Both nits ADDRESSED: (LOW) numeric coercion `+r.unitPrice`; (MED) **visibility — `_isBomRowFlaggedRed` now returns red on `bcPollDivergence`** → the caught divergence is instantly a RED row + send-warning (front-runs F050). Coach also confirmed downstream re-apply paths (on-open check `:38459`, force-refresh `:28291`) are USER-gated not silent → B052 fully closes the SILENT path; F048/F050 handle the gated ones.
> **B052 (Coach-approved, nits fixed) on Test V.046 — READY FOR PROD pending Jon test-eyeball.** F045 also on it.
> **STATUS:** B052 + F045 on Test V.046, prod-ready. **⏳ Jon: eyeball test + go for prod on B052+F045.** Then continue: F050 (consume the `bcPollDivergence` flag via `findIncompleteQuoteItems:16484` + `_isPriceImplausible` SSOT) → F044 → F048 → F049 → F045-server. Money-path throughout.


> ## ✅✅✅ SHIPPED TO PROD v1.24.14 (release `a2c44601`) — To-Do pane batch
> **F042** (NEEDS ATTENTION shows ALL projects incl. green, sorted Ship Date → Est. Prod. Done → most-untouched; footer = total count) · **G016** (top status tiles shrunk 72→52px to reclaim space) · **G017** (rail + main Project column each their own viewport-locked independent scroll pane). All Jon-verified on Test V.039→V.042, hosting-only, advisory-review clean. One shared `TodoRail` edit covers rail + MY DASHBOARD page.
> **⏳ Jon prod-verify:** the three above on prod. **Next:** F043 (hover rail row → highlight matching project on board) to scope. F041 (supplier matrix) still PARKED at direction (ii).


> ## 🔧 2026-07-23 — To-Do pane follow-ups (NEW) — G017 (independent scroll) + F043 (hover→highlight)
> **G017 (Jon):** make the To-Do side pane scroll separately from the main page. Surface = **standing right rail** (Jon confirmed). Fix BUILT + on **Test V.041**: standing-rail aside pinned to `height:calc(100vh - 122px)` + `minHeight:0` + `overflowY:auto` (explicit viewport-lock instead of `alignSelf:stretch` — forces nested flex scroll to engage across browsers). Scoped to the 380px rail; main column untouched. Rail fix CONFIRMED working by Jon. **Round 2 (Test V.042):** Jon's symptom = mousing over the main Project section scrolls the whole document (dragging the rail with it) → the main content column (`:49862`) had `overflowY:auto` but no definite height (relied on stretch, same latent issue the rail had). Applied the SAME fix to the main column: `height:calc(100vh - 122px)` + `minHeight:0`. Now both panes are independent viewport-locked scroll containers. **⏳ Jon verify V.042.**
> **F043 (Jon):** hover rail row → highlight matching board tile. ✅ Scoped → `docs/F043-HOVER-HIGHLIGHT-SCOPE.md`. Key finding: only *reliably* useful on the **SALES tab** (rail is role-scoped/app-level; other tabs' boards show a different stage slice → most rows no-op; ITEMS has no tiles). Rec: DOM/imperative wiring (`data-project-tile` + querySelector + accent glow ring) — NOT a React-state lift (Dashboard/ProjectTile unmemoized → full-board re-render on every hover). Hover source = NEEDS ATTENTION rows only. **✅ Jon ruled:** D1 = highlight-only (no scroll); D3 = all tabs, silent no-op; D2 = DOM/imperative. **BUILT + on Test V.043:** `data-project-tile={p.id}` on ProjectTile root + `_hlBoardTile` helper toggling an accent glow ring from the attention rows' hover handlers. No lifted state / no board re-render. Advisory-review clean. **⏳ Jon verify on test** (SALES tab: hover a rail row → matching tile glows).
> **STATUS:** both filed; awaiting Jon surface-confirm. F042 + G016 still on Test V.040 awaiting eyeball.

> ## 🔧 2026-07-23 — To-Do pane NEEDS ATTENTION list rework — filed F042
> **Ask (Jon):** The NEEDS ATTENTION list shows only red/critical projects then "N on track" at the bottom. (1) Confirm "N on track" = projects not shown (non-red); if so, show ALL projects by time incl. YELLOW + GREEN (list can be long). (2) Sort should factor **Requested Ship Date** + **Est. Prod. Done date** + **how long untouched in ARC**. Refine later; this is the sort criteria for now.
> **+ G016 (2026-07-23):** shrunk the top status tiles/pills to reclaim vertical space (Jon, live on test). Rail tile height 72→52, pageMode minHeight 42→34, label/count fonts trimmed (12→11 / 20→17), section-header + grid padding tightened. Cosmetic only (advisory-review clean). Affects both rail + MY DASHBOARD page (shared `_pill`/`_grid`). Shipped in **Test V.040**.
> **STATUS:** 🚀 Deployed to **Test V.040** (base v1.24.13; F042 + G016), prod untouched, master synced `2b6f0664`. Jon ruled: sort = **Ship Date → Est. Prod. Done (stored `productionEndDate`) → most-untouched**; scope = **role-scoped set, just unhide green**; footer = total project count. One `TodoRail` edit covers rail + F030 page; build review-clean (updatedAt always epoch-ms). **⏳ Jon test-verify on matrix-arc-test.web.app** (check right rail + MY DASHBOARD attention list: greens now listed, sort order, total-count footer). Behavior note: `requestedShipDate` often blank → list effectively staleness-ordered until ship dates populate. **PROD deploy held until Jon OKs the test eyeball.**

> ## 🔍 2026-07-23 — Primary/Secondary supplier matrix (analysis, PARKED) — filed F041
> **Ask (Jon):** Salesmen price BOM items from non-primary suppliers → Purchasing has to re-source. Want a Primary/Secondary supplier matrix: BOM items default to PRIMARY supplier sourcing unless Purchasing picks a secondary. Sales↔Purchasing flow still TBD (no ARC Purchasing module yet — purchasing done in the old system).
> **STATUS:** ✅ Analysis DONE → `docs/F041-SUPPLIER-MATRIX-ANALYSIS.md`. Coach trace: NOT greenfield — a shipped manufacturer→vendor learner (`manufacturerVendorMap`) is the closest analog; BC ItemCard `Vendor_No` already read by ARC; 3 unguarded write-doors let a salesman price off-primary; SSOT predicate cluster is the natural home for `_isPrimarySupplier`. BC = single default vendor + flat catalog (no native ranked tiers). Freddy rec: **Hybrid source-of-truth + soft-gate near-term**, 4-slice order (surface→soft-gate→RFQ pre-select→hard-gate deferred to Purchasing module). **✅ Jon locked: HYBRID + SOFT-GATE** (2026-07-23). Freddy defaults assumed for granularity (per-item primary, manufacturer-learner as seed) + reconcile (extend `manufacturerVendorMap`, no parallel store). **Sales↔Purchasing handoff → (ii) leaning** (Jon: quote-time correctness + off-primary signal/export w/ context to external Purchasing; still refining exact flow). **F041 PARKED — no build until Jon confirms the flow.** Session pivoting to next fix.



> ## 🧾 SESSION END 2026-07-22 — big dashboard/notifications/markup batch shipped (v1.23.23 → v1.24.13, ~175 commits)
> **Shipped to prod after v1.24.5:** v1.24.6 F033 (rail on every tab) · v1.24.7 F031 #1 (Clear ✕) · v1.24.8 F031 #3 + B047 (name overflow) · v1.24.9 divider+B048+F031#4+B049+F031#2 (notification click-nav, 2 pre-existing bugs fixed) + B050 (post-review bell) · v1.24.10 F025 3b (Needs-Attention list + RFQ awaiting rows) · v1.24.11 tile font bump · **v1.24.12 B018** (money-path: phantom-red BC rows blocking Send / forcing Budgetary — `_effectivePriceDate` SSOT accessor; + split-by-reason send-block overlay) · **v1.24.13 F030** (MY DASHBOARD page) **+ F029 slice-1** (live Outlook Email panel, read-only/zero-persistence, Coach privacy-approved) **+ B051** (triangle/line markup render).
> **In flight at close:** Coach scope for **F035+F040 (interactive markup)** done → plan saved `docs/F035-F040-INTERACTIVE-MARKUP-SCOPE.md`. **⭐ NEXT UP #1 = F035-move + F040** (build move-first).
> **⏳ Jon (non-blocking):** verify B051 on prod (draw triangle/line + circle→oval call) · F036/F038 clarifications · G015 test-ribbon fix (offered). Engineer feedback triaged → B051 done, F034-F040/G014 queued (`docs/ENGINEER-FEEDBACK-TRIAGE.md`).
> **State:** master==origin, working tree clean, prod v1.24.13 ACTIVE (no freeze).

> ## ✅✅ SHIPPED TO PROD v1.24.5 (release d07eb7e9) — role-aware dashboard + name fix
> **F032** role-differentiated To-Do pane (Salesman pills only for actual salespeople → Andrew's phantom RFQs gone; Reviewer + Designer sections assigned-to-me; new `permissions.designer` flag; admins see only roles they hold) + **B046** name fallback (all members get a name). Hosting-only. Coach APPROVE WITH NITS. ⏳ Jon: set Andrew's Designer flag in Team→Permissions so his Engineering section populates.

> ## ✅✅✅ SHIPPED TO PROD v1.24.4 (release c2a78bf1) — big dashboard/reliability batch
> Live for ALL users now: **F025 3a** To-Do right-pane (collapsible, "Jon's To-Do List", 8 equal 3-up pills w/ timeout colors, pill→My-Projects, By-Status full reset) · **G1** BC-reliability (search+pricing helpers now honest-health-gated) · **B045** notification BELL fix (index-free listener — bell finally works for everyone) · resizable+persistent bell dropdown · **B023** quote-summary price-clip · **SENT RFQ badge** on tiles. Hosting-only (no rules change). All Coach-vetted through Test V.021.
> ⏳ Jon prod-verify: bell shows for all users (have a teammate confirm) · G1 mode-B (`_arcForceBc401=8`) · pane/tiles look right.

> ## ✅✅ SHIPPED TO PROD v1.24.2/.3 — F027 MANAGER role + priority pin (rules+hosting)
> • `permissions.manager` flag (admin-assigned in Team UI) · pin checkbox (manager/admin) on the project header, works on locked projects (rules carve-out). Coach APPROVE (NIT-1 folded). Rules verified via review + established live-testing (no emulator — existing gate byte-for-byte → no write-path regression).
> • **Jon verify (2026-07-22):** (1) manager checkbox ✅ works (set for Noah + Jon). (2) ★ pin's "pin to top" should live in the USER's To-Do list, NOT the main board → **v1.24.3 removed the board-column reorder + the 📌 tile badge** (pin control/field/rules retained; `_priorityPinCompare` kept for F025). ⏳ Jon re-verify board no longer reorders/badges on pin. **Design note for F025: the priority pin is a To-Do-list-only ordering concept.**

> ## ✅✅ SHIPPED TO PROD v1.24.1 (release b6dc593c) — board split + tile rework + inert F028
> • **F026** 8-column split (READY TO REVIEW · IN PRE-REVIEW · READY TO SEND) + **B044 fix** (send-blocked reds no longer show as "Ready"); **G013** tile pills removed; **tile restructure** (narrower/taller, customer own line, 2-line name, minWidth 130); **F028** RFQ-all-items toggle (inert, default OFF); v1 attention top-strip REMOVED (right-pane replaces it). Hosting-only (rules already live). Coach-reviewed throughout.
> • ⏳ Jon prod-verify: board columns + tile layout + projects in correct columns.


> ## ✨ F028 (NEW, MED) — Admin toggle: RFQ all items ignoring Priced Dates
> • Jon: dual-ERP (ARC+M1) lag → want an admin Settings toggle to RFQ ALL of a supplier's quotable items every time, not just stale-priced. Add-only `_pricingConfig` flag, default OFF (opt-in). Money-path-adjacent → Coach review before deploy.
> • Coach scoped (`docs/F028-RFQ-ALL-ITEMS-SCOPE.md`): ONE choke point `_eligibilityReason` in `buildRfqSupplierGroups` (:6663); add-only `rfqAllItemsIgnoreStale` config flag, opt-in. Build S-M (M = threading a `forceAll` reason through group counters so it doesn't flip to lead-time-only).
> • Jon ruled: flag-ON bypasses BOTH staleness AND cooldown; exclusions preserved. **✅ Marc BUILT (master `02579a1d`) + Coach APPROVE (no blockers/nits).** Default OFF → inert until an admin toggles it; rides to prod with F026. OBS-1 (Coach): customer-supplied rows land on a forced RFQ = pre-existing behavior, matches Jon's ruling — eyeball when toggled ON. **DEPLOY-READY (inert).**


> ## ✨ F025 — right-pane To-Do dashboard: BUILDING (final phase, sub-phased 3a→3e)
> • Decisions locked: 8 pills (one per board column incl. Ready To Send) · always-personal scope · pill color = worst project in bucket · thresholds {yellow,red} pairs (Jon's cutoffs, Settings-editable, defaults for the 3 unspecified) · ECO real-BOM-hook · idle map unbounded.
> • **3a ✅ on TEST V.013** (master `9cf4c7c4`). Coach CHANGES-REQUIRED → FIXED (pane filter now mirrors the board's inclusion chain; dropped `!wonAt` so Active-ECO pill counts match the board — the count-parity blocker). Collapsible 380px rail + 8-pill grid; board grouping refactored to shared `_todoBucketOf` (byte-equivalent per Coach). ⏳ Jon eyeball on test. Next: 3b list+RFQ+quotes-weekly · 3c threshold Settings · 3d ECO timer · 3e idle-flash.
> • **B023 ✅ MERGED** (cherry-picked `811a6574`→master, auto-merged clean) + on Test V.013; worktree cleaned up.
> • **3a UI tweaks ✅ MERGED** (cherry-pick `57dd891a`→master `2a096981`, clean) + on Test V.014: header "<FirstName>'s To-Do List" (no icon, +40% font), compact pills w/ larger labels. Worktree cleaned up.
> • **★ Test V.017 = 3a + UI tweaks + B023 + G1 + feedback rounds** (pills 3-up/compact; pill→My-Projects; By-Status re-click clears focus; **project tiles now show "{n} SENT" RFQ badge beside "{n} RFQS" received** — `_rfqAwaitingSummary.sentVendorCount`). Prod untouched v1.24.3. ⏳ Jon eyeball + G1 mode-B verify → prod deploy (v1.24.4).
> • F029: pinned NECESSARY→tabled for research; **build starts after prod deploy.**
> • **Test V.021** adds: equal-size To-Do pill tiles w/ bottom-left-aligned values (Jon).

> ## 🌐 F033 — global persistent To-Do rail ✅✅ SHIPPED PROD v1.24.6 (c1054057)
> • App-level `<TodoRail>` — persists on every top-level tab; suppressed in ProjectView + future-F030 hook + while AI Assistant open; shared collapse; pill-click → Sales board via `pendingFocus`. Coach APPROVE WITH NITS (race verified correct, no Sales regression, flag can't stick). Shipped straight to prod per Jon (code-level review; UI-only). ⏳ Jon prod-verify: rail on each tab · non-Sales pill-click → Sales focused · collapse persists · no double right-pane w/ AI Assistant open. Rollback = redeploy prior if any layout issue.
> • **F031 Clear ✕ (#1)** 🔨 BUILDING (tree now clean post-F033 → direct build, no worktree).

> ## 👥 Andrew (Reviewer/Designer) feedback → B046 + F032 (role-aware dashboard)
> • **B046** (name missing in To-Do header) ✅ FIXED on Test V.022 (userFirstName/memberMap fall back to displayName→email-prefix — all-users fix).
> • **F032** ✅ BUILT + Coach APPROVE WITH NITS · **on Test V.023** — role-aware pane (stacked union of held roles); salesman pills gated on ACTUAL salesperson (fixes Andrew's phantom RFQs); Reviewer + Designer sections scoped assigned-to-me; new `permissions.designer` flag; admin gating on RAW flag (Jon: admins see only roles they hold, non-salesperson admin keeps Sales Pipeline). ⏳ Jon verify (set Andrew's Designer flag in Team settings to see the Engineering section). Ships next prod batch. Applies to F025 pane + future F030.

> ## 🐛 B045 — bell never fired: ROOT-CAUSED + FIXED (on Test V.021)
> • ROOT CAUSE (Coach, code-confirmed): listener query `.where(read==false).orderBy(createdAt)` needed an uncreated composite index → silently errored (empty `()=>{}` handler) → list stayed `[]` forever. Creates/rules/render all healthy. **Fixed (Jon chose index-free):** `orderBy(createdAt desc).limit(50)` + client-filter `read!==true`; error handler now logs. **⏳ Jon verify on Test V.021: does the bell NOW show notifications?** (test shares prod data → any real unread should appear = confirmation). **★ Unblocks F029 Phase B** (reuse this bell).
> • Jon V.015 item 1 (RFQ needing-approval "N RFQS" badge on tiles) = **verified INTACT** (ProjectTile :46370, survives G013/tile rework) — no regression, no change.
> • **F030 filed (FUTURE, parked):** dedicated personal User Dashboard PAGE (own nav) = user's project tiles w/ $ totals + attention list + combined Outlook/ARC tasks + email/meeting awareness. Convergence of F025+F029. Jon-aware, build later.
> • **Outlook/Graph research DONE** (`docs/OUTLOOK-GRAPH-TODO-RESEARCH.md`): Q1 To-Do↔Outlook sync + Q2/Q3 time-mgmt notify-tool both feasible on ARC's existing Graph plumbing; notify vision = foreground poll + existing bell (no server infra). **⏳ Jon decision: pursue (file F029 + greenlight Q1 pilot) or park.**
> • Plan: `docs/F025-RIGHT-PANE-PLAN.md`.

> ## 🔎 B013 — ✅ DIAGNOSED (`docs/B013-BC-RELIABILITY-DIAGNOSIS.md`)
> • B013-1/2/3+B021 already shipped → it's a COVERAGE HOLE. **Mode B root cause CONFIRMED:** ~7+ BC helpers (incl. Item Browser/BOM `bcSearchItems`) use raw fetch, bypass `bcGatedFetch` → their 401s never flip the pill = "search died while blue." **Mode A:** MSAL sessionStorage (per-tab) + no persistent MS session → members' silent refresh fails → honest red. **"per-member API key" = RED HERRING** (Anthropic keys, not BC). Gaps G1 (route raw-fetch thru gate) · G2 (MSAL localStorage + proactive renew) · G3 (leading probe). **★ G1 ✅ BUILT + Coach money-path APPROVE (byte-identical 2xx across 8 helpers + ItemsTab; found a 2nd bypass surface beyond the diagnosis; no silent-read site missed).** On **Test V.014** (master `6958dc33`). ⏳ **Jon live-verify (mode-B money-shot):** `window._arcForceBc401=8` → BOM/Item search → pill flips RED (was green-while-empty). Then prod. **Nits → G1b** (a few more raw-fetch reads that already fail loudly: fetchVendors PARTS read, Codale/DigiKey admin bulk, discovery-401). G2/G3 follow-ups.

> ## 🔧 B023 — ✅ FIXED (worktree `811a6574`, pending merge)
> • One-line QUOTE SUMMARY layout fix (status-pill wrapper shrinkable → long status truncates, price never clipped). Merge onto master after 3a commits, re-validate (worktree used --no-verify), deploy with 3a. "IN ETR" abbreviation = optional Jon follow-up.

> ## ✨ F025 v2 RESHAPE (Jon 2026-07-21) — original reshape spec (see plan doc)
> • Top-strip (v1, on TEST) SUPERSEDED → right-side pane (like in-project Panel/Quote Summary). Reqs captured: `docs/F025-V2-TODO-DASHBOARD-REQUIREMENTS.md`.
> • Decomposed: **G013** (remove redundant tile status pills), **B044** (projects wrongly in READY TO REVIEW/SEND — evc predicate), **F026** (split evc→READY TO REVIEW+READY TO SEND, reorder IN PRE-REVIEW column, per-status timestamps, review→RFQ-return), **F027** (MANAGER role + priority flag), **F025** (right-pane: pill grid GREEN/YELLOW≥80%/RED≥100% + timer-sorted list + ECO list + Quotes-Sent follow-up + idle-flash 40h/24h).
> • v1 foundation (statusChangedAt / _rfqAwaitingSummary / _statusClockStart / _attentionThresholdMs) REUSED.
> • Jon decisions locked: per-user idle · admin-assigned manager · foundation-first. Both Coach lanes DONE. **★ PHASED MASTER PLAN: `docs/F025-V2-MASTER-PLAN.md`.** B044 = real bug (evc `hasUnpriced` narrower than red-rule/send-gate) → SSOT consolidation fixes B044+B018+enables split. Manager = `permissions.manager` flag (reuse reviewer). Board already 7-col flexbox (reorder trivial). Timer infra mostly exists + lock-safe.
> • Phases: 0 (G013 ✅) → 1 (F026 ✅ SHIPPED v1.24.1) → 2 (**F027 ✅ BUILT + Coach APPROVE WITH NITS** — NIT-1 folded [pin allowlist narrowed]; master `427cef7e`; ⏳ **needs rules-test approach + Jon deploy gate**, rules are GLOBAL) → 3 (**F025 📋 PLAN READY** `docs/F025-RIGHT-PANE-PLAN.md` — decomposed 3a-3e, 7 open decisions + ECO-hook correction; awaits Jon sign-off).
> • **★ F027 deploy note:** firestore.rules is GLOBAL (test=prod); Coach recommends emulator rules-tests before deploy (9-case matrix). Decision pending: emulator harness vs careful-live vs hold.
> • ✅ ALL decisions locked (Jon 2026-07-21): width = horizontal-scroll board + collapsible rail; READY-TO-REVIEW gate = tech-review + manualVerify only (confidence/BC advisory). Master plan fully specced.
> • Marc BUILT (master `a1d49f02`, +84/-17, validate PASS). **Coach APPROVE WITH NITS** — routing correct/total, SSOT-clean, kanban lockstep, matches Issues-gate ruling. Nits (Jon-facing): (a) vendor-is-customer $0 rows now reach a Ready column (was pinned in RFQs) — correct SSOT fix, eyeball on test; (b) _timingStates now incl. quotes_sent/active_eco (Phase-3 will define per-bucket timers anyway); (c) one-time aging re-stamp on legacy evc projects (benign). B018 deferred.
> • **✅ ON TEST — Test V.011** (`matrix-arc-test.web.app`, prod untouched v1.23.23). Jon flagged rightmost column cut off → trimmed widths (row gap 16→10, col minWidth 180→150, tile padding 10→8, owner maxWidth 60→46) so 8 cols fit; overflow-x backstop stays. **⏳ Jon re-checking fit on test.** On OK → prod deploy (minor bump; carries inert F028). No rules change.

> ## ✨ F025 v1 (SUPERSEDED) — attention top strip (on TEST V.009 only, never prod)
> • Ask: glanceable "what needs me / what's timing out" — pending-RFQ visibility (Ryan/Noah pain) + aging alarm (DRAFT/IN PROCESS/READY/PRE-REVIEW/RFQ > ~1wk).
> • Coach scoped (`docs/F025-ATTENTION-DASHBOARD-SCOPE.md`). Jon ruled: top-strip, My-Projects+toggle, ~7d admin-configurable. Pure client derive, LOW risk, build M.
> • Jon ruled ALL decisions (2026-07-21): top-strip · My-Projects+toggle · ~7d admin-configurable · **EXACT time-in-status (new `statusChangedAt` stamp)** · READY-TO-REVIEW = evc bucket · 3 chips (⏳ awaiting RFQ · 📥 responses to review · ⏰ timing out).
> • Marc build-ready plan DONE (`docs/F025-ATTENTION-DASHBOARD-PLAN.md`) — 6 pieces, ~140-160 LOC, chip#2 source (`rfqCounts`) already exists (no new listener). Save-path piece A (`statusChangedAt` in saveProject+saveProjectPanel) is the sensitive bit → Coach gate + live backward-compat before deploy.
> • Jon APPROVED plan. Marc BUILT (+161/-20, master `56a6cae9`, validate PASS). Coach save-path review → CHANGES-REQUIRED (2 blockers) → BOTH FIXED: **B1** rules carve-out regression (added `_lastEffectiveStatus`+`statusChangedAt` to 4 `firestore.rules` allowlists so the every-save stamp doesn't break Request-Unlock/ECO/lease writes on locked projects), **B2** chip deep-link `_attnClickRef` guard. Freddy verified both diffs.
> • Added unit-aware threshold (Minutes/Hours/Days, `_attentionThresholdMs()`) so aging is testable at minute-scale (Jon: can't test 7-day blocks). Scope-checker caught a leftover `_thrDays` ref → fixed (unit-aware `_thrLabel`).
> • **✅ ON TEST CHANNEL — Test V.009** (`matrix-arc-test.web.app`, base v1.23.23). Additive `firestore.rules` DEPLOYED (global, safe — prod build doesn't write the keys). **PROD UNTOUCHED (v1.23.23).**
> • **STATUS: ⏳ Jon reviewing interface on TEST + live-testing** (set threshold to minutes; test chips, deep-link, Request-Unlock-on-locked). ⚠ test shares PROD config — Jon must reset threshold to "7 days" before prod rollout. On Jon OK → prod deploy v1.24.0 (rules already live).

> ## ✅ B043 SHIPPED TO PROD v1.23.23 (release 3a297d66) — Ryan's RFQ send hardened
> • Coach diagnose lane DONE (`docs/B043-RYAN-RFQ-COACH-TRACE.md`) — **BOTH symptoms = ONE root (H1):** RFQ mail sends client-side via MS Graph from the sender's mailbox; vendor recipient emails are BC-token-dependent (`bcGetVendorEmail` @6282 / `bcFetchVendorContacts` @19732 both return empty w/o `_bcToken`) → blank recipients SKIPPED (@19892) → supplier gets nothing; sender confirmation gated on ≥1 successful send (@20057) → zero sends = zero confirmation. Ryan-specific = team-member BC not connected / per-member key lacks vendor read (same class as B024).
> • LIVE TRIAGE (Jon): PROD + BC connected (blue) + Ryan sending fine now → not a broken account; root = blank-vendor-email data/handling path (all-blank send = zero sent + no confirmation).
> • Marc build-ready plan DONE (`docs/B043-RYAN-RFQ-MARC-PLAN.md`) — all in `RfqEmailModal`, ~50-70 LOC, LOW-MED, not money-path: per-vendor "no email" marker + pre-send banner + guaranteed zero-sent feedback + Debug Log.
> • Jon ruled: **warn-and-continue**. Marc BUILT (`RfqEmailModal`, master `d233d0f7`, `node validate_jsx.js` PASS, +35/-5 LOC). Coach review **APPROVE WITH NITS** — NIT-1 (blank-vs-failed label split + empty-included edge) folded in. Freddy independently re-validated PASS.
> • STATUS: **✅ DEPLOYED v1.23.23** (Jon deploy gate → prod). **⏳ Jon live-verify on prod:** all-blank send → "⚠ 0 RFQs sent" + "RFQ NOT SENT" confirmation email arrives; partial → good send + blanks flagged + confirmation fires; all-good → unchanged; `rfqSend` warn entries in debugLogs. Master==origin @3a297d66, tree clean.

> ## 🧳 (prior) SESSION END 2026-07-14 — parked for return (still valid)
> Prod stable at **v1.23.22**. **Parked (all filed):** Quick-wins batch (Triangle bug eng #4 first, F024 ECO nit, B023, G007, B030, B029, B022); Engineer Review-markup feedback → `docs/ENGINEER-FEEDBACK-ON-REVIEWS.md`; Live-verify-later (B039, F022 PO test, B041 unlock re-test, `deploy-test.sh`); Deferred (B016-2/3, F014-B, F007/F016, tech-review cluster B024-B027/F017/F018).

> ## 🧳 JON AWAY 4 DAYS (2026-07-14 → ~07-18) — PROD FROZEN, no deploys/changes until he's back
> Prod stable at **v1.23.22**, working tree clean, master==origin (`1e861a3c`), no lanes running. Jon stopped the quick-fixes batch to avoid touching ARC before leaving. **Do NOT deploy or change prod while away.**
> **Parked for his return (all filed, nothing urgent, prod not exposed):**
> - Quick-wins batch (unstarted): Triangle-not-rendering bug (eng #4), purchasing-board ECO nit (F024 follow-up), B023 quote-summary pill overflow, G007 (TEST upload bar) + B030 (silent-catch log), B029, B022.
> - Engineer Review-markup feedback cluster → `docs/ENGINEER-FEEDBACK-ON-REVIEWS.md` (Triangle bug + move/resize/edit markups + highlighter/text tools + spacing).
> - Live-verify-later (non-blocking): B039 (BC transient carries `No.: ''`), F022 disposable-BC PO test, B041 unlock re-test, `deploy-test.sh` run.
> - Deferred: B016-2/3 (concurrent row-merge — one-editor lock holds), F014-B, F007/F016, tech-review cluster (B024-B027/F017/F018).

> ## 🔤 STARTUP + CLOSEOUT COMMANDS SET (2026-07-14, Jon) — subagent-lane pair
> **Startup:** `/team-sub-start` renamed → **`/ARC-team-Startup`** (same behavior; old name retired).
> **Close-out:** new matching **`/ARC-team-Closeout`** written (`.claude/commands/ARC-team-Closeout.md`) — subagent-native: one Freddy session, **no peer clear-check / no paste-relay**, Freddy owns all handoff files directly, and **freeze-aware** (commits+pushes docs but **skips deploy** when prod is frozen / Jon away). The two-word "Close Out" / "Closed" triggers map to it.
> Docs updated to match: both command files, CLAUDE.md (startup-variants row + shutdown section + default-model callout), FREDDY.md (operating-model block), SESSION-STATE.md (boot + close-out lines), memory `feedback_subagent_lane_model_preferred`, and this board. **Next session boots via `/ARC-team-Startup`, closes via `/ARC-team-Closeout`.** Docs-only change — no app code, no deploy (prod stays frozen at v1.23.22).

## (prior) ✅ SESSION 2026-07-14 · prod v1.23.22 · F024 ACTIVE ECO column SHIPPED; B042 fully resolved

> ## ✅✅ SHIPPED v1.23.22 (release `06f93e00`): F024 ACTIVE ECO column — ✅ verified live
> New ACTIVE ECO column (red, any-active-ECO via computeActiveEco) between In-Pre-Review + Quotes-Sent; (BOM) IN PROCESS now pre-PO-only + renders yellow (color-key fix). Confirmed live: ACTIVE ECO + (BOM) IN PROCESS + QUOTES SENT all present on the board. Coach APPROVE WITH NITS. Nit (deferred, Jon-aware): purchasing_kanban still narrow → non-draft active-ECO Won projects show in both Sales ACTIVE-ECO + Purchasing boards.

> ## ✅✅ SHIPPED v1.23.21 (release `e62171bf`) — ✅ JON VERIFIED (quick checks all pass): F023 + B035 + B036 + B037 + B039 + B042-fix
> **F023** board click-header filter (all kanban views) · **B035** $0 service-card blocks Send · **B036** preserve quoteSent* in saveProject guards · **B037** F022 header offline-queue · **B039** tighten B038 retry (true empty-No.) · **B042-fix** dedup guard stops new duplicate project docs. All Coach-approved. **Live-verify-later:** B039 (confirm the genuine BC transient still carries `No.: ''` — Debug Logs `_b038LastRaw`); F022 disposable-BC PO test.
> **🔨 F024 building** `f024-active-eco-column` (on post-F023 master): ACTIVE ECO column (any-active-ECO routing, RED, + (BOM) In Process color-key fold) → Coach → deploy.
> **✅✅ B042 CLEANUP DONE (Jon go, 2026-07-14):** 36 dup empty stubs archived → `companies/{cid}/projects_archive` (archiveReason "B042 duplicate empty BC-import stub", tagged `_b042KeepId`, restorable) → deleted from projects. **Verified: projects 128→92, remainingDupGroups=0, canonical keepers intact.** (First attempt safely blocked by rules — wrong collection name; corrected to the sanctioned `projects_archive` path per `archiveProject`; nothing was deleted on the blocked attempt.) The 46 remaining `arc-<hash>` docs are legit sole-copies (BC-import-only projects, no manual twin) — left as-is. **B042 RESOLVED** (guard fix v1.23.21 stops new + 36 existing cleaned).
> **✅ F024 Coach APPROVE WITH NITS — DEPLOY-READY** `07a54198`: broad computeActiveEco routing at both sites, no non-ECO regression, no TDZ, (BOM) In Process now yellow + pre-PO-only, Badge/maps complete, composes w/ F023. **Nit to flag Jon:** purchasing_kanban still uses the narrow ECO predicate → a non-draft active-ECO WON project shows in BOTH the Sales ACTIVE ECO column and the Purchasing board (intended per "other boards untouched"; broaden `:44498` if undesired).

> ## 🔧 Loose-ends cleanup (Jon 2026-07-14)
> - **🔎 Loose-ends batch** `loose-ends-b035-b036-b037-b039` (`1160f508`, Marc BUILT, Coach reviewing): B035 ($0 service-card blocks Send) + B036 (preserve quoteSent* in saveProject guards) + B037 (F022 header offline-queue) + B039 (tighten B038 retry to true empty-No.). B041 nit RATIFIED (no-bump).
> - **🔎 F024** (NEW, Coach scoping): ACTIVE ECO board column between In-Pre-Review + Quotes-Sent; route active-ECO projects there → (BOM) IN PROCESS = pre-PO only. Build on F023 branch (shared board render).
> - **✅ F023** `f023-board-column-filter` (`b0744509`): click column header → focus to that status (all 6 kanban views), full-width grid + "← All columns" reset + toggle + empty-state; search already existed (broad, kept). View-only. **Coach APPROVE WITH NITS — DEPLOY-READY** (no board regression, drag-drop intact).
> - **📄 ENGINEER FEEDBACK ON REVIEWS** captured → `docs/ENGINEER-FEEDBACK-ON-REVIEWS.md` (9 markup/BOM-edit items; not yet scoped; item-8 screenshot missing).
> - **🔎 B042 fix-first** `b042-import-dedup-guard` (`b2aa0d96`, Marc BUILT, Coach reviewing): dedup guard now fresh-reads the persisted collection + matches BOTH `bcProjectId`+`bcProjectNumber` + load-gates the initial sync → stops new arc-hash stub dups. (Root: 36/92 projects duplicated by the old in-memory/single-key guard.) **Cleanup of the 36 existing stubs = separate Jon-gated archive pass after this ships.**

> ## 🅿️ B016-2/3 (concurrent-edit row-merge) — DEFERRED (Jon ruled 2026-07-14)
> Status confirmed: NEVER shipped (`_mergeBomOnSave` absent from prod; `b016-23-merge` unmerged, **108 commits stale**, predates today's B034/B041 saveProject rewrites → would need a full rebuild + re-matrix). **NO open data-loss:** the B012 hard one-editor lock (22 refs, live since v1.23.5) contains concurrent-edit loss (one editor per project). B016-2/3 was the enhancement to safely RELAX that lock (row-merge for simultaneous co-editing) — a deferred robustness feature, not a bleed. **Jon: DEFER — keep the one-editor lock.** Branch `b016-23-merge` PRESERVED on origin (not deleted) for a future fresh rebuild if simultaneous single-project co-editing becomes a need. Revisit trigger: users needing to co-edit one project at once.

> ## ✅ SHIPPED v1.23.20 (release `66183dcc`): board column header "In Process" → "(BOM) In Process" (Jon — clarify meaning). Header label only (`app.jsx:44433`); status value + Badge + tile pills + Purchasing-board column untouched. Verified live on prod.

> ## ✅✅ SHIPPED v1.23.19 (release `1508c0a4`): B041 + G009
> **B041** — `_noBumpWrite` guard: background/programmatic saves no longer bump a sent quote's rev (only genuine user edits do) → the In-Process drift that hit 7 sent quotes CAN'T recur. **G009** — Test V.### env-build versioning: ribbon "🧪 TEST · Test V.### (base vX)" on the test channel, prod-safe `deploy-test.sh` (bumps counter, cache-bust, hosting:test only), version.json loop-guard, `docs/TESTING-PROCEDURES.md`. **✅ Prod verified live (controlled tab):** v1.23.19, no ribbon, no reload loop, clean load. **Earlier today (v1.23.18):** B038+F022+G012. **B040:** 7 casualties healed (targeted). **⏳ Jon re-test:** B041 (unlock sent quote → only real edit bumps); G009 (run `./deploy-test.sh` → test channel Test V.008, one reload, prod untouched). **Follow-ups:** B042 (dup docs), B037/B039, B041 budgetary-auto-clear nit-ratify, needs-review sent-quotes.

> ## 🔎 Two prod issues (Jon, 2026-07-13/14) — Coach diagnosed
> **#1 IN PROCESS should be QUOTES SENT → B040** (regression casualties): projects sent in the B034 window (12:36–13:42 MDT) persisted `quoteRev=quoteSentRev+1` → mis-columned. Jon chose **self-heal migration** → **✅ Marc BUILT** `b040-inprocess-selfheal` (`8a8fabf8`, +106/−1): guarded re-anchor in loadProjects (raw-doc guards: window[start−5min/END tight] + rev===sentRev+1 + no post-send qvHistory + no active ECO), targeted `.doc.update({quoteSentRev,quoteRevAtPrint})` bypassing hash/bump, idempotent (guard2 false after heal), 400ms-spaced, session healed-ids set. Board flips on load. **⏳ Coach data-migration review** (gate: can it EVER re-anchor a legit revision? + persist safety + idempotency) → Jon deploy. **#2 owner VIEW-ONLY on PRJ402131/402126 → NOT a bug (G011):** the sent-quote soft-block (`_sentSoftBlockActive`) makes any sent quote read-only for everyone incl. owner → click **"Verify with Project Owner & Enable Edits"** to edit (predates B034). Awaiting Jon's banner-text confirm; owner-exemption logged as **G011** design consideration.
>
> ## 🔎 Testing-procedures cleanup (Jon 2026-07-14) — G009 reactivated + refined
> Jon: unsure Prod-vs-Test; test versioning stale/untracked; wants distinct **"Test V.###"** (single incrementing counter) + a way to confirm the right ENV+version. **🔎 Coach scoping:** Test V.### scheme (independent counter, cache-bust `?v=` per test deploy = fixes staleness) + unmissable `IS_TEST_ENV` env+version badge + `deploy-test.sh` (prod-safe) + a DRAFT `docs/TESTING-PROCEDURES.md`. ★ Anchored on: **matrix-arc-test SHARES prod Firestore+BC** (test=different code, same data → disposable projects only; true isolation = G005). → proposal + decisions to Jon → build. **✅ Coach scope DONE; Jon approved (2026-07-14): Test V.007 (padded, monotonic), extend existing ribbon, deploy-test.sh no-rules-by-default, wire freshness auto-reload w/ loop-guard.** ★ Root cause corrected: NOT cache (all no-store) — it's the absence of a per-deploy build-identity signal on hosting:test (same `?v=`, dormant version.json check). **✅ Marc BUILT `g009-test-versioning`** (`ae51965b`): TEST_BUILD="007" const + ribbon "🧪 TEST · Test V.007 (base v1.23.18)" (IS_TEST_ENV-gated, prod no-op) + prod-safe `deploy-test.sh` (bumps counter, cache-bust `?v=<ver>-T<NNN>`, hosting:test only, no APP_VERSION/tag/prod) + version.json loop-guard (combined build-id both sides) + TEST-BUILDS.md + TESTING-PROCEDURES.md + CLAUDE.md pointer + check-scope allowlist. **✅ Coach APPROVE WITH NITS:** loop-guard verified NO permanent-disagree on prod OR test (incl. prod-deploy-after-test); `deploy-test.sh` prod-safe by construction (hosting:test only, no APP_VERSION/tag/push). Prod runtime byte-identical. **1 nit to fix before merge:** committed `version.json` seeded with `testBuild:"007"` = latent reload-loop on localhost/manual-non-deploy.sh push → revert to prod-shaped `{"version":"v1.23.18"}`. **✅ Marc FIXED nit** → tip `0e432942`: version.json reverted to prod-shaped `{"version":"v1.23.18"}` (landmine gone); BC-suppression doc claim verified+corrected (bcGatedFetch belt `:454` fake-200s non-sandbox mutating writes on test; sandbox writes pass; reads hit real BC). Directly implements Coach's requested nit → **✅ G009 DEPLOY-READY** (bundling with B041 once its review lands). Live-test = Jon runs `deploy-test.sh` (test-only, one clean reload to V.008, prod untouched).

## 🧪 B034 re-test (Jon, prod v1.23.17): REGRESSION FIXED ✓ + 2 refinements
> Steps 1–3, 5–7 PASSED. **Step 2 (send → stays Quotes Sent, NOT In Process) = the regression fix CONFIRMED ✓.** Bump-once + re-send cycle works. **2 items surfaced:** (a) **B041** — the bump fired on the "Verify & Enable Edits" unlock click (step 3), BEFORE the edit (Rule-2 deviation) → 🔎 Coach tracing (suspect F020 term-seed on edit-enter); (b) **G012** — status wording → ✅ SHIPPED v1.23.18. B034 core = good; these are refinements. **B041 UPDATE:** Coach traced — the unlock click fires NO save; a background/programmatic save coinciding with unlock bumps the hash (unmasked by the B034 fix removing post-send suppression). **Jon chose: guard ALL background saves.** **✅ Marc BUILT + Coach APPROVE WITH NITS** (`f7871ed7`): `_noBumpWrite` flag on every programmatic save (enumeration verified complete, Rule 3 intact, print/send + extraction untouched). Nit → Jon ratify: budgetary-auto-clear=no-bump. **✅ DEPLOY-READY.**

> ## ✅✅ B040 REMEDIATED (2026-07-14) — 7 sent-quote casualties healed out of IN PROCESS
> Freddy (controlled-tab) enumerated 17 sent quotes → 7 casualties (rev>sentRev, no post-send user-edit, not won/lost), Jon-confirmed, re-anchored `quoteSentRev=quoteRevAtPrint=quoteRev` via targeted 2-field update: **402131 Messabi, 402126 Longonjo, 402106, 402109, 402092, 402119, 402137** → QUOTES SENT (402137 stays Draft). Root = **B041 background-save bumps over time** (402126 drifted 2→4 since 06-30), NOT the B040 window → validates B041. Sent-quotes-WITH-post-send-edits left alone (legit revisions, Jon reviews). Filed **B042** (duplicate arc-<hash> project docs). Auto-migration stays shelved. **✅ F005 Print-Only VERIFIED on prod (Jon 2026-07-14): PDF, no bump, no unlock.**

## ✅✅ SHIPPED v1.23.18 (release `8c537674`): B038 + F022 + G012
> **B038** Create-In-BC auto-retry (transient empty-No. now self-heals, no dup) · **F022** PO Received drag-n-drop upload + BC attach (Replace-safe versioned filename) + "View PO" button · **G012** sent-quote status wording ("✓ Quote sent Qv.NN to <recipient> · date"). **⚠ Pre-deploy catch:** the SHELVED B040 self-heal code was found STAGED in the shared working tree — discarded before merge (safe on branch `b040-inprocess-selfheal` `8a8fabf8`); confirmed absent from the release. **⏳ Jon live-BC test (F022) on a DISPOSABLE project:** upload→BC attach correct name; Replace-same-PO#=exactly one attachment; View PO opens. **Follow-ups:** B039 (B038 regex-tighten), B037 (F022 offline-queue). **B041** (bump-on-unlock) still Coach-tracing → next deploy.

> ## ✅✅ SHIPPED v1.23.17 (release `e6a1ac26`) — quote heading relabeled
> `_quoteHeadingLabel` → **"Project Name: {name} - {customer} / PROJECT #: {cust#}"** (uses `project.bcCustomerName`; graceful fallbacks; no PO# on quotes). Coach APPROVE WITH NITS (narrow Sell-to vs Bill-to cosmetic edge, non-blocking).
>
> ## 🔨 F022 — PO Received drag-n-drop upload (building)
> **Coach scoped:** reuse the `rfqEmailFile` upload pattern (handler `:34419`, dropzone `:35633`), store `pageImages/{uid}/{projectId}/customerPO/…` + `project.customerPoDoc` (additive, NO rules change), PDF-only 25MB, multi-project-safe via onDone→single saveProject. **Jon decisions (2026-07-13):** (Q1 answer "ARC only" but Q2 note said push to BC → **going with BC attach per explicit note**) → **F022 v1 = ARC storage + BC attachment** (`bcAttachPdfToJob`/`bcEnqueue('attachPdf')`, ARC/BC-protocol PO filename) + **"View PO" button** (once a PO exists, the PO-Received button relabels to "View PO"; opens the modal with an open/download link). Money-path (BC-write). **✅ Marc BUILT F022** → `f022-po-upload` (`ea4caa75`, +158/−11): dropzone (reuses rfqEmailFile pattern) → Storage `pageImages/{uid}/{pid}/customerPO/` + `project.customerPoDoc`; persists via onDone→single saveProject (multi-project-safe); BC attach via `bcAttachPdfQueued` (offline-queue fallback, best-effort) with filename `PO-[CUSTOMER PO] <po> - <proj>.pdf` (house-style) + `bcFileName`-tracked Replace (no dup); "📄 View PO" button once won/PO exists + modal View panel. Additive, no rules change. **🛑 Coach CHANGES REQUIRED (money-path blocker):** same-PO# Replace produced a filename == the existing attachment → `bcDeleteAttachmentByName` deletes ALL name-matches incl. the just-uploaded one → ZERO attachments (data loss). Root: dropped the drawing-path filename VERSIONING. Everything else PASS (multi-project safe, best-effort attach, retention clean, View PO). **✅ Marc FIXED** → `f022-po-upload` `d6189a49`: versioned filename `PO-[CUSTOMER PO] <po> - <proj> - <uploadedAt>-<rand>.pdf` → new≠prev by construction → `bcDeleteAttachmentByName(prev)` targets only the genuinely-old file; first-upload prev=null. **✅ Coach delta APPROVE (`d6189a49`):** invariant new≠prev holds by construction (versioned stamp+rand); first-upload prev=null; no regression; deploy-ready. **✅✅ F022 DEPLOY-READY — bundling with B038 for one deploy** (both Coach-approved). Live-test gate: Replace-same-PO# → exactly ONE correct BC attachment.

> ## 🔨 B038 auto-retry (2026-07-13) — Ryan's "Create In BC" transient
> Jon chose **auto-retry + friendly fallback**. **✅ Marc BUILT** `b038-item-create-retry` (`9321a38a`, +58/−23): `_findByVendorItemNo` factored; retry loop (1+2, 800/1600ms) on `Internal_DataNotFoundFilter`/empty-No. signature; adopt-existing-by-`Vendor_Item_No` (idempotency); fail-fast on 401/already-exists/other; friendly fallback; surrogate scheme untouched. **⏳ Coach money-path review** — ★ key check: does adopt catch a partially-created item given `Vendor_Item_No` is set by a LATER PATCH (not the create POST)? i.e. can a retry ever duplicate, or does `No.: ''` guarantee no item persisted. **✅ Coach APPROVE WITH NITS:** duplicate risk DEFINITIVELY nil (`No.: ''` = No.-Series failed mid-insert = primary key never assigned = rollback = nothing persisted → retry can't collide; `Vendor_Item_No` PATCH-written post-create so adopt is a correct secondary net). Fail-fast preserved; success shape identical. Nit → **B039** (retry regex too broad; no data risk). **✅ DEPLOY-READY — bundling with F022 for one deploy.**
>
> **STILL PENDING Jon live re-test on prod (now v1.23.17):** B034 rev cycle (send→Locked/not-In-Process → Verify→modal→edit→bump+In-Process) + F005 Print-Only. F021 ✅ confirmed.

> ## ✅✅ SHIPPED 2026-07-13 — v1.23.16 (release `9bd2db4d`): B034 send-anchor fix — regression CLOSED
> The v1.23.15 regression (SEND prematurely bumped `quoteRev` → every sent quote falsely showed In-Process/unsent/unlocked + the first real post-send edit didn't bump) is FIXED: send now stamps `quoteRev===quoteSentRev===quoteRevAtPrint` from the FINAL post-populate rev via the `_sendAnchorWrite` guard + Firestore read-back; shared `_sentSoftBlockActive` SSOT predicate. Coach money-path APPROVE (all 4 invariants confirmed). **⏳ JON RE-TEST on prod (v1.23.16):** (1) send a quote → lands **Locked Rev NN, NOT In Process**, no unsent-pill; (2) Verify & Enable Edits → **warning modal shows** → Continue; (3) one edit → **Qv bumps once + In Process**; (4) never-sent unchanged; (5) Print-Only (F005) → no bump/no unlock. **Follow-up:** separate ticket to delete dead `_doInlineQuoteSend` (unreachable).

> ## ✅✅ SHIPPED 2026-07-13 — v1.23.15 (release `991b2915`): F021-4 quote heading (absorbs G010)
> Quote line-items heading now shows **`<Customer Project #> / <Project Name>`** (e.g. "923455698 / Messabi") in place of "Line Items" — both PDF + on-screen, falls back to Project Name alone. Coach APPROVE. **Completes the full F021 feature + G010.** ⏳ Jon eyeball on a real quote.

> ## ✅✅ SHIPPED 2026-07-13 — v1.23.14 (release `d6ddbd3e`): B034+F005 + F021-1/2/3 (one release)
> **B034** (ASAP) — editing a SENT quote now bumps Qv.## on the first change after send (cap re-anchored on `max(quoteRevAtPrint,quoteSentRev)`), the quote flips to **In Process** + unlocks, and the divergence-warning modal shows Jon's exact text; never-sent cadence unchanged. **F005** — "🖨 Print Only" button in the locked overlay prints a PDF with NO rev bump / NO unlock. **F021-1/2/3** — new **CUSTOMER PROJECT #** field (New Project + inline-editable header on every project screen), bound to BC **External Document No.**; PO-received composes `Project#: X / PO#: Y`. Coach money-path-approved (F1 print-only-persist hole + F2 pill-on-never-sent both fixed pre-merge). **🚨 B034 PROD REGRESSION — ROOT FOUND, fixing forward (Jon chose fix-forward 2026-07-13):** ROOT (Coach) = the SEND itself prematurely bumps `quoteRev` (its `ensureQuoteFieldsPopulated`/`ensureQuoteNumber` persists mutate `project.quote`, which is in `_computeQuoteHash` → bump R→R+1) while `quoteSentRev` is stamped from the STALE pre-populate rev → quote lands `quoteRev>quoteSentRev` = falsely "diverged" the instant it's sent. Effects: (1) first genuine post-send edit hits `_alreadyAhead=true`→bump SUPPRESSED (Jon's report); (2) **live prod side effect: every sent quote immediately shows In-Process + unsent-pill + unlocked** (Change B/C off the false divergence); (3) modal-not-showing = likely knock-on of the corrupted post-send state (wiring confirmed intact). **✅ Marc BUILT FIX** → `b034-send-anchor-fix` (`3ceb6197`, off master `a43de350`): precise mechanism = `persistProject`→`safeSave`→`saveProject` DISCARDS saveProject's return, so in-memory `quoteRev` stayed stale (R) after the populate/number persists bumped Firestore → `quoteSentRev` stamped from stale R. Fix: (1) transient in-memory `_sendAnchorWrite` guard in `saveProject` (`delete`d pre-persist, never in Firestore/hash/state) → send write refreshes hash but NEVER bumps/unlocks; (2) `handleSend` reads FINAL persisted `quoteRev` back from Firestore → stamps all 3 anchors (`quoteRev`/`quoteSentRev`/`quoteRevAtPrint`) equal + `quoteLocked:true` + PDF/filename/qvHistory use the same rev; (3) shared `_sentSoftBlockActive` predicate (banner + `sentReadOnly`, closes the SSOT `!isProjectLocked` gap). Modal wiring confirmed intact (no-modal was the corrupted-state knock-on). Also fixed a latent bug in the DEAD `_doInlineQuoteSend` path. Never-sent byte-identical. **✅ Coach APPROVE WITH NITS (money-path):** all 4 invariants confirmed — flag never persists/hashes; post-send `quoteRev===quoteSentRev===quoteRevAtPrint` + locked + NOT In-Process; read-back strongly-consistent + safe fallback; real first-edit still bumps+unlocks. SSOT predicate byte-equal. Nits cosmetic/pre-existing; dead-`_doInlineQuoteSend` deletion = separate ticket. **✅✅ DEPLOY-READY — HELD for Jon redeploy** (removes the live prod regression) + live re-test (send→locked/not-In-Process; ack→edit→bump+In-Process). F005 + F021 unaffected (stay live). **⏳ JON LIVE-TEST (remaining):** Print-Only=no bump/no unlock; F021 on a **DISPOSABLE BC project** — create→"Project#: X", PO→"Project#: X / PO#: Y", + confirm no BC-side report chokes on the composite. **Follow-ups filed:** B035 ($0 service-card send-block), B036 (`quoteSent*` preserve-guards), B037 (F021 header offline-queue). **F021-4** quote heading ("<cust#> / <name>", absorbs G010): **✅ Marc BUILT** `f021-4-quote-heading` (`dba3587f`, +13/−2) — `_quoteHeadingLabel` helper + both sites (PDF + on-screen), fallback to name. **✅ Coach APPROVE — deploy-ready** (`dba3587f`); HELD for Jon deploy (small standalone release).

> ## ✅✅ SHIPPED 2026-07-13 — v1.23.13 (release `d49f882f`): B033 + F020 Option A (one release)
> **B033** — commissioning/programming-only quotes (no control panel) now render the full quote right pane (QUOTE SUMMARY + PROJECT TOTAL + Print/Send/Tech-Review/PO buttons) instead of an empty pane; PDF `Subtotal (Panels): $0` row suppressed for services-only. **F020 Option A** — new/non-BC customers can set Payment Terms + Shipping Method inline in the send modal (blank-send block kept); company default (admin-editable in Pricing Config) seeds blanks; manual entries survive a BC re-fetch. Both Coach-approved (C150/C151). Deferred: F020 nit #1 (read-only-viewer mount-persist banner, narrow). **Queued next:** B034 build (sent-quote revision bump — was queued behind B033, now unblocked) + F005 (Print-Only, bundle w/ B034); B035 ($0 service-card send-block); PRJ402063 in-app check; B016-2/3 fold Phase A + re-matrix.

> ## ✅ RESOLVED (2026-07-13) — C137 M1 prod data-loss FIXED · was EMERGENCY
> Coach diagnosis (C148): the BOM-wipe is **NOT a B016 regression** — it's the **C137 M1 save-on-open** path, **live on prod v1.23.11**. Opening a **flat/panels-less** project can persist `migrateProject(stale init)` = `{panel-1, bom:[], status:'draft'}` wholesale → wipes the BOM + flips to draft (merge bypassed [no matching server panel], nBom belt skipped [first-save]). The fix (**C137 Phase A** — delete the save-on-open effect) was Jon-approved but landed on shelved `claude/phase-b-bom-merge`, **never on master**. **✅ Marc BUILT Phase A** (delete save-on-open effect + dead `didMigrate` ref) → branch **`c137-phasea-saveonopen` (`ff1ea466`)**, off master, validate PASS, migration confirmed intact (migrateProject re-runs on snapshot), no other `didMigrate` readers. **⏳ DEPLOY HELD** — Jon corrected the repro (add was on **PRJ402068**, not 402063; **PRJ402063** is missing its BOM but still in BC). **Coach forensic lane running** (Firestore state + debug-log/keystroke scan on both projects) to CONFIRM whether the C137 M1 path actually wiped real data + which project, and whether PRJ402063 is flat (M1-eligible) or paneled (different cause) — BEFORE the emergency deploy. Code bug is real regardless; deploy on evidence + Jon's go. Tracked: **B032**. **✅✅ SHIPPED 2026-07-13 — Phase A deployed to prod v1.23.12 (release `a6fdbd4b`); save-on-open wipe vector REMOVED; active prod exposure CLOSED. B032 fix live.** Remaining: (a) pin **PRJ402063** via Jon's in-app check (empty? flat/paneled?) + recover from BC if wiped (forensic subagent retired — CLI can't read Firestore); (b) **fold Phase A into `b016-23-merge` before B016-2/3 deploys** (that branch predates Phase A → still carries the wipe until folded), then re-run the matrix; (c) defense-in-depth follow-ups (nBom belt in saveProjectPanel, panel-level union, first-save high-water seed) — part of the B016-2/3 hardening.

> ## 🔨 B033 (2026-07-13) — Commissioning/Programming-only quote shows EMPTY right pane
> Jon reported: a services-only quote (no control panel) renders no quote buttons/data in the right pane. **Coach scoped (confirmed Jon's control-panel hypothesis):** the whole right-pane action suite is gated on a **panel existing** — `sp = panels.find()||panels[0]||null` (app.jsx ~35767). Services live in `serviceCards`, not `panels`, so `sp===null` → the **simplified branch** (~35773–35811) renders header+service rows+PROJECT TOTAL but **no action buttons** (Print/Send Quote, Tech Review, Send BOM for Approval, PO Received/LOST all live in the `sp`-only "third pane" block ~36084+). **Fix (Jon-approved):** lift the QUOTE SUMMARY + action block so it renders for **any** quote line (`panels.length>0 || serviceCards.length>0`); keep **Send-BOM-for-Approval hidden** for services-only (no BOM — `generateTravelerBomPdf` returns null when `!panels.length`); **skip ship-date** for services-only (Jon's call). Paneled quotes unchanged (purely additive). **STATUS: 🔨 Marc lane building** (worktree → branch `b033-services-quote-pane` → Coach review → Jon deploy). Tracked: **B033** (TODO.md).
>
> ## 🚨 B034 (2026-07-13) — ASAP: editing a SENT quote doesn't bump Qv.## — Jon's authoritative lock-centric spec
> Jon (ASAP): rev isn't bumping on quote changes. ROOT (Jon's diagnosis): a modal says "rev bumps on next **save**" but users never click save — saving is implicit (on change + send) → phantom-save gate never fires. **Authoritative spec (supersedes C149's print-anchored model):** (1) SENT quote = **locked** → rev **frozen**; (2) unlock → no change → relock = rev **unchanged**; (3) unlock → **ANY change → rev bumps IMMEDIATELY** + quote → **"active"** until re-sent; (4) first change after unlocking a quote with send history → **warning modal** "This Quote has been sent to the customer. Making changes will cause the Customer copy and internal copy to be different. Continue?" (continue/cancel). **★ Bundle with F005** (Print-Only in the locked overlay). **F005 scope DONE (Coach):** locked overlay = the post-send soft-block banner in `PanelListView` @:36614; print-stamp sites @:38243 (Path A) + :37068 (Path B); Print-Only = normal print + a `printOnly` flag that SKIPS the `quoteRevAtPrint` stamp (so it can't reset the cycle) via a new `arc-print-only` window event; **build B034+F005 ONE branch, B034 first**. **★★ Coach authoritative scope DONE:** ROOT = print-anchored bump cap + send never stamps `quoteRevAtPrint`. Fix (one branch, B034+F005): re-anchor caps on `max(quoteRevAtPrint,quoteSentRev)`; diverged sent-quote → **In Process** (Jon's choice) + unlock; reword warning modal to Jon's text; delete dead unlock modal; F005 Print-Only = no-bump PDF in the locked banner. Rules 1/2 already met; Rule 3 = the fix. No schema bump. Money-path → Coach re-review + live-test before deploy. **✅ Marc BUILT** → `b034-f005-quote-revision` (`4df3fd74`, +86/−37, validate PASS): all 6 B034 changes + F005 Print-Only; never-sent cadence byte-identical (guard confirmed); diverged→In Process (explicit return); Print-Only skips all 3 stamps + no unlock + reuses quote #. **✅ Coach APPROVE WITH NITS (money-path bar):** core logic CONFIRMED — never-sent byte-identical, bump-exactly-once, send-save can't self-bump, Change C unlock divergence+sent-gated, dead modal clean, data-retention PASS. **1 REQUIRED fix (F1):** Print-Only's PRE-print saves (populate @`:38507` + budgetary flip @`:38535`) aren't print-only-aware → narrow case could bump+unlock a sent quote. **1 fix (F2):** unsent-pill OR-in lights on every never-sent quote → gate on `quoteSentAt`. Filed **B036** (pre-existing: `quoteSent*` not in saveProject preserve-guards, now load-bearing). **✅ Marc applied F1+F2** → tip `3f15678a`: F1 gates all 3 pre-print persists on `!printOnly` (populate/budgetary-flip/quote#-assign; in-memory populate retained so PDF still renders; checklist eqs/sync persists confirmed non-bumping — not in hash); F2 gates the pill send-clause on `quoteSentAt` at both sites (never-sent reverts to pre-branch behavior). **✅ Coach delta APPROVE** (`3f15678a`): F1 closes the print-only bump/unlock hole end-to-end (normal print byte-unchanged); F2 correct both sites. **✅✅ DEPLOY-READY — HELD for Jon deploy + live-test cycle** (send→edit bumps once→In Process→re-send; Print-Only = no bump/no unlock). Tracked: **B034** + **F005**.
>
> ## 🔨 F021 (2026-07-13) — Customer Project # field (BC External Document No.) — building
> New "CUSTOMER PROJECT #" field bound to BC External Document No. **Coach scope:** External-Doc-No is WRITE-ONLY from ARC (zero readers) → composite-string risk to ARC = ZERO; internal PO# is the separate `bcPoNumber` field. **Jon decisions:** header **inline-editable**; pre-PO format **prefixed "Project#: X"**; quote heading **"<Cust #> / <Project Name>"** (e.g. "923455698 / Messabi", absorbs G010). **✅ Marc BUILT F021-1/2/3** → `f021-customer-project-number` (`0580eb84`, +61/−5, validate PASS): `_composeExternalDocNo` helper + new-project field + inline-editable header + PO-append composite; additive/no schema bump; quote-heading sites untouched. Marc caught a **3rd `bcCreateProject` caller** (`relinkToBC`) beyond the scope's 2 → threaded it. **✅ Coach APPROVE WITH NITS (C-note):** `_composeExternalDocNo` correct+null-safe (no empty-fragment leak); External_Document_No confirmed STILL zero readers repo-wide (composite risk=0); `bcPoNumber` stays bare; all 3 `bcCreateProject` callers correct + backward-compat; additive/retention-safe. Nits (non-blocking): clearing cust# actively blanks BC (intended, single-writer); create-path guards blank writes but header doesn't (acceptable). **Routable decision → Jon:** header inline-edit best-effort BC write (mirrors customer-name sibling) vs add `bcEnqueue` for guaranteed convergence (Coach leans enqueue). **⏳ HELD for Jon deploy** — needs LIVE-BC test (3 write paths + BC-side bare-PO# consumers) on a DISPOSABLE BC project. **F021-4 (quote heading) deferred** until `b034-f005` merges (independent — no overlap confirmed). Tracked: **F021** (+ G010 absorbed).
>
> ## 📋 B034 prior scope (C149 — SUPERSEDED by the ASAP spec above)
> Jon: post-send edits don't bump the revision → the live quote can silently diverge from the customer's copy at the same Qv.##. **Coach scoped (C149):** ROOT = bump gate + "unsent revision" pill anchored to last PRINT (`quoteRevAtPrint`); SEND (`quoteSentRev`) never participates. Bump logic already centralized in 2 save choke points (`saveProject`/`saveProjectPanel`, hash-driven, capped once per print cycle) — send just never advances the cap. **Fix (Coach Option A):** factor `_lastDeliveredRev=max(quoteRevAtPrint,quoteSentRev)` → route both bump gates + 2 pills + the warn through it; no new field, minor bump. **Jon chose: "first change after send bumps"** (once per revision, not per-keystroke — rejected the literal "every change" which recreates the Rev-293 runaway; keeps PRINT as a reset too). **STATUS: build QUEUED behind B033 deploy** (B034 edits the unsent-pill @:36088, inside B033's refactored quote-summary region → build on post-B033 master to avoid collision). Tracked: **B034**.
>
> ## 🔨 F020 (2026-07-13) — Manual Payment Terms + Shipping Method for new (non-BC) customers
> Jon: printing/sending a quote for a NEW customer shows "Missing required fields: Payment Terms, Shipping Method… enter manually before sending" but there's no obvious way to set them. **Coach scoped:** a manual path ALREADY exists (free-text inputs in the QuoteTab quote editor) — the real gap is the SEND hard-block (`QuoteSendModal.handleSend` @:33493, OK-only, no fields) bounces the user with no inline fix; new customers start blank; and a BC re-fetch can clobber a manual entry. Concrete user-facing half of **F014**. **Jon chose: Option A now + fold B into F014.** **✅ Marc BUILT Option A** → `f020-payment-terms-entry` (`f834fdf8`, +93/−5, Babel validate PASS): (1) inline Payment Terms + Shipping Method fields in `QuoteSendModal` (write via onUpdate like QuoteTab; **#117 blank-send block KEPT** — fill-inline only, no bypass); (2) override guard in `ensureQuoteFieldsPopulated` (`&&!q.<field>`) so a BC re-fetch can't clobber a manual entry; (3) company-wide default in the EXISTING `_pricingConfig` admin UI ("Default Quote Terms", constants Net-30 / Customer-Handles-Shipping) seeded ONLY into blank fields, visible/editable, gated `!bcProjectNumber`. **✅ Coach APPROVE WITH NITS (C151):** no blank-bypass, guard-no-regression, seed BC-gated + one-time mount, hooks legal, persist safe vs B012 lease. Nits: modal mount-seed writes on open (swallowed banner for read-only-viewer edge); branch rebase before landing. **⏳ HELD for Jon deploy.** Option B folded into F014. Tracked: **F020**.
>
> ## ✅ B033 APPROVED — HELD for Jon deploy (services-only quote pane)
> **Marc BUILT** → `b033-services-quote-pane` (`3fddc7fe`, off master `dce149a2`, validate PASS): shared `quoteSummaryPane` (header + line list + PROJECT TOTAL + buttons) for both branches; `panelDetailPane` panel-only; Send-BOM-for-Approval gated `panels.length>0`; RFQ row + Update-BOM-in-BC hidden for services-only. **✅ Coach APPROVE WITH NITS (C150):** panel case output-identical, no sp-null-deref, Print/Send traced SAFE for zero panels (quote# assign + PDF totals services-aware). Nits cosmetic (PDF "Subtotal (Panels): $0", pane top accent). Pre-existing gap filed **B035** (send not blocked by $0 service card). Render-only, data-safe. **PDF $0-Panels nit + accent-line fixed** → branch tip `78578731` (Coach delta APPROVE). **⏳ HELD FOR JON DEPLOY** (live smoke test advisable, not blocking).
>
> ## 🌅 MORNING SUMMARY (for Jon — 2026-07-12 catch-up)
> **Prod = v1.23.11.** Shipped + verified yesterday: **B021** (BC hang/deadlock timeout), **B013-1** (401 auto-recovery), **B013-2/3** (honest BC health pill + 401 sync-modal), **F019** (standalone pricing survives nav-away + tile bar — you verified), **nav-modal cleanup**. **Notifications fixed** (Priority-2 persistent/ack). **B028** scraper concurrent-login: verified not-an-active-problem (prod logs clean + Royal tolerates concurrent sessions) → tabled.
>
> **B016 status (chain's last item):**
> - ✅ **B016-1 (churn reduction) DEPLOYED to prod v1.23.11** (2026-07-12, release `0c55f6e9`). N1 breadcrumb stripped pre-prod; Jon approved `priceDate`→`priceUpdatedAt`. ⏳ **F1b still open** (the sell-price coalesce's minor self-healing transient — accept / harden via latestPanelRef / drop the coalesce; shipped as a documented known-transient per Coach's deploy-ready-with-F1b).
> - ⏳ **`b016-23-merge` — B016-2 row-merge (data-safety core) + B016-3, Coach APPROVE-for-deploy, HELD for Jon's 2-session matrix.** (2 review rounds caught+fixed a HIGH money-path silent-revert; Layer A keys the price group on the new `priceUpdatedAt` clock @ 22 price-writers; 39 tests pass.) **Before deploy: run the 2-session ADD+DELETE matrix — MUST include clear-price / budgetary / AI-estimate / commitBcItem rows — then deploy (minor).** Being kept rebased on the deployed B016-1 so the matrix runs the exact code that'll ship. **→ 2026-07-12: deployed to TEST CHANNEL for Jon's matrix. 🛑 MATRIX FAILED — CATASTROPHIC: Jon added a new BOM row and the ENTIRE BOM was wiped (all rows deleted).** Part A PASSED (clear-price null-priceDate edit persisted = F1 fix verified) + A4 PASSED (commitBcItem). But Part B (add/delete) triggered a full-BOM wipe — a data-loss defect in the B016-2 row-merge / `_deletedRowIds` / nBom-belt-ordering (39 unit tests missed the live-app condition). **🛑 DO NOT DEPLOY b016-23-merge. PROD SAFE (untouched, v1.23.11) — the matrix gate caught it before prod.** ✅ **CONFIRMED throwaway test project (PRJ402063, single tab) — NO real data lost.** Repro clue: added row → showed added → nav-away/close → status flipped to **draft** + BOM wiped (persisted) → so the wipe is on the **nav-away/close SAVE** (writing a stale/empty state), not the add. Diagnosis lane running: root cause + fix + **explicit prod-exposure check** (is the wipe B016-2/3-specific, or a nav-away path also on master/prod v1.23.11? — decides if live projects are at risk). Expectation: B016-2/3-specific (prod safe), but confirming. F1b + B031 still open.
>
> **Open flags/decisions:** priceDate→priceUpdatedAt (approve) · F1b + N1 (B016-1 nits) · :24501 cosmetic priceDate-backfill unstamped (reasoned non-risk — your call on consistency) · **B029** (per-row ⚠ pill 401, LOW) · **B030** (:27119 silent catch, LOW). Docs: `docs/B016-PLAN.md` · `docs/BC-RELIABILITY-PLAN.md` · `docs/F014-BC-PAYMENT-TERMS-RESEARCH.md`. Review trail: COACH.md **C141–C147**.
>
> *(Detailed session log continues below.)*

> ### ✅ DECISIONS LOCKED (Jon 2026-07-11) — was ⏳ NEEDS JON
> 1. B021 = **45s + `let`** ✓ · 2. B016-2 = **last-writer-by-timestamp** · 3. 401 POST auto-replay = **yes** (single retry) · 4. owner/member auth = **separate probe** · 5. F014 = **cached ARC-editable default seeded from BC** · 6. gap#5b save-gate = **HOLD** · 7. build order = **B021 → B013-1**.
> **Next:** B021 BUILD IN PROGRESS (Jon chose build-now-via-subagent, gated). Full context: `docs/BC-RELIABILITY-PLAN.md`.

**🔨 2026-07-11 (cont.) — B021.** Marc lane ✅ BUILT the `bcGatedFetch` timeout fix on branch **`b021-bcgatedfetch-timeout`** (`b8610a1b`, pushed): AbortController 45s + single `finally` release + caller-signal compose + `BcTimeoutError`, 429 + G005 belt preserved; **14/14** mirror-harness tests pass (`tests/bc-gated-fetch.test.js`). Matches locked spec (deviation = `let`, per Jon). Coach lane ✅ **APPROVE for deploy** (C142 in COACH.md). **✅✅ B021 SHIPPED TO PROD v1.23.6** (Jon sign-off → merge → deploy.sh, release `d165ad6b`, `validate_jsx` passed). The 95%-pricing-freeze + BC-semaphore-deadlock fix is LIVE. **Next in the BC-reliability chain: B013-1** (401 retry + token refresh, layers on B021 — re-sync the mirror test). **Auto-notify hook = ALWAYS-ON** (`notify-hook.ps1`; wiring active next reload). ✅ **NOTIFICATION FIX CONFIRMED** (Jon 2026-07-11): need-you pings + auto-hook now **Priority 2 (Emergency)** — persist + require acknowledgment (Jon verified the P2 test). `notify.ps1` gained `-Retry`/`-Expire`. Delivery was always healthy; root cause was P0/P1 one-shots.

**🔨 2026-07-11 (cont.) — B013-1 BUILD (gated subagent, on the deployed-B021 base).** Marc lane building retry-on-401 + silent token auto-refresh into `bcGatedFetch` (401 branch mirroring 429; `acquireBcToken(false)` + Authorization-header rewrite; `_bc401RetryDepth` max 1; relies on B021's `finally` release; 401-POST single-replay per Jon's locked ruling). B013-2/B013-3 (health pill, sync-modal) OUT OF SCOPE. Re-syncs + extends the mirror test. **✅ BUILT on branch `b013-1-bc401-retry` (`d6f75153`, pushed): +27 lines (401 branch), 35/35 tests pass, validate PASS, no deviations, B021+G005 intact, B013-2/3 correctly not built.** Coach lane ✅ **APPROVE WITH NITS** (C143). **✅✅ B013-1 SHIPPED TO PROD v1.23.7** (Jon "proceed" → C143 persisted → merge → deploy.sh, release `ad3c0f2c`). BC calls now auto-recover from a mid-session 401 (silent token refresh + Authorization-header rewrite + single retry). **BC-reliability chain remaining:** B013-2/B013-3 (honest health pill + fix the misleading 401 sync-modal) → B016 (on-open churn reduction + row-merge + resilient mutations). Away-notify P2 test fired (awaiting Jon ack). **prod now v1.23.7 (B021+B013-1 both live).**

**🔎 2026-07-11 (cont.) — F019 scoping (Jon-directed, read-only).** Jon prod-tested B021 by running standalone "Get New Pricing" + leaving → still killed on nav-away + no tile progress bar. **Clarified: NOT a B021 miss** — B021 only fixes hung-BC-request timeout/deadlock. The nav-away kill is **F019**: standalone Get-New-Pricing runs FOREGROUND (not a registered `_bgTasks` background task) → unmount kills it + no tile progress (same root). ✅ **SCOPING DONE → `docs/F019-BACKGROUND-PRICING-PLAN.md`.** Root cause confirmed: standalone pricing isn't a registered `_bgTasks` task → on nav-away the B012 editing lease releases → the writeback is rejected (`permission-denied`) AND swallowed by an empty catch (silent) + no tile bar. **Rec = Option A (~1–2 hrs):** wrap the existing `runPricingOnPanel` engine in a `_bgTasks` registration (bgStart/bgSetPct/bgDone + lifecycle-independent `saveProjectPanel` via captured ids + exhaustive `finally`) — flips on the proven extraction-resilience machinery (lease keep-alive + tile bar). + interim nav-away guard option. **✅ 4 DECISIONS LOCKED (Jon 2026-07-11):** Option **A** · **plain `_bgKey` + guard** · **ship nav-away guard** · **direct `saveProjectPanel`**. **🔨 F019 BUILD IN PROGRESS** (gated subagent, isolated worktree): Option A (bgStart/bgSetPct/terminal-finally bgDone|bgError + direct captured-id save + button-disable) + nav-away/`beforeunload` confirm — bundled on ONE branch (they share `runPricingOnPanel`; Jon away so no separate early deploy). **✅ BUILT on branch `f019-bg-pricing` (`b7f2b94f`, pushed): +74 lines app.jsx, validate PASS.** Key design catch by the lane: standalone discriminator = **`!onEpProgress`** (NOT the plan's `!bomOverride`) — because extraction/re-extract/validate call `runPricingOnPanel` WITH onEpProgress while their extraction bg-task runs on the same key; a blanket guard would've aborted post-extraction pricing. Exhaustive `finally` terminal state (bgDone/bgError on every exit — guards the leaked-task→lease-pin risk); direct `saveProjectPanel` w/ captured ids + `!_hasOverrides`; nav-away guard accurate ("continues in background"). No unit test (component-coupled — justified) → manual test-channel checklist. Coach lane ✅ **APPROVE WITH NITS** (C144). **✅✅ F019 SHIPPED TO PROD v1.23.8** (Jon "go to prod" → merge → deploy.sh, release `1e96bf84`, `validate_jsx` passed, prod serving confirmed). Standalone Get-New-Pricing now runs as a bg task → survives nav-away, tile progress bar, writeback lands + nav-away guard. **✅ JON VERIFIED F019 ON PROD** (v1.23.8): pricing persists after leaving + main tile shows progress. **✅✅ v1.23.9 SHIPPED** (release `a33cc9e3`): redundant in-app nav-away modal REMOVED (Jon: no nag for known behavior), **browser hard-close guard KEPT** (genuine unrecoverable-loss case), F019 bg-task fix intact. **prod now v1.23.9 (B021+B013-1+F019 + modal-cleanup live).**

**🔎 SCRAPER concurrent-login (Jon Q 2026-07-11) → B028 TABLED.** Investigated: real architectural exposure (fresh login/call, shared credential, maxInstances 2–5, no client serialization) BUT **verified NOT an active problem** — prod logs show 2 `customScraperBatch` runs overlapping ~21:16–21:26 both status-200 w/ prices + zero errors; Jon confirmed Royal tolerates concurrent same-credential sessions (2-tab test). Tabled as latent (B028); mitigation (maxInstances:1 + error-classification) only if harm ever observed.

**🔨 2026-07-11 (cont.) — B013-2/3 BUILD (gated subagent, on v1.23.9 base).** Next in the BC-reliability chain (Jon "move to next"). B013-2: pill off token VALIDITY via `_bcHealthSubs` pub-sub + 3 states (green/amber-reconnecting/red-click) using B013-1's `_bcLast401At` hook — closes the up-to-5-min green-but-dead window. B013-3: fix the misleading 401 sync-modal (stamp HTTP status on failed rows → deterministic 401 classify + "Reconnect & Retry" affordance + auth-dominated header). **✅ BUILT on branch `b013-23-bc-health` (`982e5863`, pushed): +116/−21 app.jsx, validate PASS.** Composes on B013-1 (bcGatedFetch retry/replay byte-for-byte unchanged; only additive `_setBcHealth` emits); B021/F019 intact; status-stamp additive (data-retention safe). Manual checklist + debug hooks (`_arcBcHealth`, `_arcForceBc401`). Flagged follow-up: a separate per-BOM-row `⚠` error pill (~L29734) still routes 401s to Item Browser (out of B013-3 scope). Coach lane ✅ **APPROVE WITH NITS** (C145). **✅✅ B013-2/3 SHIPPED TO PROD v1.23.10** (Jon "yes" → merge → deploy.sh, release `6cc1c3d7`, serving confirmed). Honest BC health pill (green/amber/red off token validity, closes the up-to-5-min blind window) + 401-aware sync modal now LIVE. **Verify via debug hooks** `window._arcBcHealth(...)` / `window._arcForceBc401=N`. Follow-up B029 filed (per-row pill 401, LOW).

**🏁 B013 FULLY COMPLETE (1+2+3).** BC-reliability chain status: B021 ✅ · B013-1 ✅ · B013-2/3 ✅ — all live. **Last item = B016** (reduce on-open BC churn + row-merge M2 + resilient mutations M3) — the HIGHEST-RISK item (data-safety save-merge, money-path, #86 class). Jon chose **scope-first** → 🔎 **B016 scoping lane running** (read-only, against current v1.23.10): re-verify all refs (plan predates today's merges), design B016-1 churn reduction + B016-2 row-merge (last-writer-by-timestamp per Jon's ruling, delete-vs-add via `_deletedRowIds`) + B016-3 resilient mutations, and analyze the CRITICAL interactions — F019 now saves via the same `saveProjectPanel` + B012 lease/cross-user guards live there. ✅ **SCOPING DONE → `docs/B016-PLAN.md`.** Corrected all stale line refs vs v1.23.10. Approach: B016-1 churn reduction (~0.5d, patch — incl. framing fix: the 30s price-check is READ-ONLY, fires once/mount, only re-attempts while locked → dep-driven not polling) · B016-2 row-merge last-writer-by-timestamp + delete-via-`_deletedRowIds` + metadata gap-fill (~1.5-2d, minor, PR+sign-off) · B016-3 resilient mutations (~0.5d). **★ Load-bearing companion fix found:** `pollBcPricing` updates `unitPrice` WITHOUT stamping `priceDate` → under last-writer-by-timestamp the merge would silently lose poll price updates → must stamp. **F019 verified NO regression** (its fresh `priceDate` wins the merge → writeback still lands); **B012 lease composes** (additive, guards untouched). **✅ Jon reviewed + approved approach + 3 decisions** (1: stamp priceDate in pollBcPricing YES · 2: add `productionEndDateUpdatedAt` · 3: whitelist as floor + grep sweep). **🔨 B016 BUILD (gated, split for staged deploy):** Lane A = **B016-1 churn reduction** (drop 30s interval→dep-driven, visibility/env-gate pollBcPricing, coalesce sell-price patch, seed bcPrevSyncCount from persisted count) + the load-bearing `priceDate` stamp in pollBcPricing — own branch, deployable as a low-risk patch. ✅ **BUILT on branch `b016-1-churn` (`124791e8`, +39/−8, validate PASS)** — 30s interval→dep-driven unlock, pollBcPricing visibility/env gate + `priceDate` stamp, sell-price coalesce, `bcPrevSyncCount` seeded from new persisted `bcLastSyncedBcCount` (+ breadcrumb). No merge/mutation changes; data-retention safe. **Overnight (Jon left; deploys HELD):** Coach reviewed B016-1 → **CHANGES REQUIRED (F1)**: the sell-price coalesce left `bcAutoSyncTimer.current` stale-truthy on cancel-without-rearm → could silently suppress BC line-10000 (money-path sell price) updates. **✅ F1 FIXED** (one-line, Coach-verified) → **b016-1-churn now @ `3f1ac068`, DEPLOY-READY** (patch, low-risk). Items 1/2/3/5/6 clean. **⏳ Jon morning decisions:** F1b (coalesce can carry an interim sell price if it settles without a further trigger — a minor money-path transient vs master; accept / harden via latestPanelRef / drop the coalesce) · N1 (strip the `[B016-1]` count-check breadcrumb before prod). **✅ Lane B (B016-2/3) BUILT** → `_mergeBomOnSave` (Layer A last-writer-by-timestamp + Layer B undefined-only gap-fill + delete-via-`_deletedRowIds`) in saveProject + saveProjectPanel; 5 delete sites stamped (grep-swept); inclusive metadata whitelist; B016-3 resilience centralized at `saveImmediatePanel→safeSavePanel`; **31/31 tests pass** (real sliced helper). Invariants confirmed (merge-before-dedup, B012 guards + nBom belt intact, F019 lands). **F1 folded in cleanly** → staged on **`b016-23-merge` (`6067fd09`, pushed)**. Coach adversarial review → **CHANGES REQUIRED (C146): HIGH money-path silent-revert BLOCKER** — Layer A keyed the price group on `priceDate`, which is legitimately null for clear-price/budgetary/AI → those fresh user edits silently lose to the server's stale price + revert. (Core algo/delete-coverage/ordering/B012/F019/funnel all otherwise SOUND.) **🔧 Fix lane running** (Coach's rec: dedicated `priceUpdatedAt` clock on every price-writer + key Layer A on it; revert the pollBcPricing `priceDate:Date.now()` band-aid → this **supersedes Jon's Decision-1** — priceDate→priceUpdatedAt, correct field/same intent) + whitelist adds `rfqSentDate`/`restoreSkipped` (F2) + 2 new tests. **✅ FIX APPLIED** (`5262c091`, b016-23-merge advanced): `priceUpdatedAt:Date.now()` clock stamped at **21 price-writer sites** (Coach's 8 + 13 more from independent grep sweep), Layer A re-keyed on it, pollBcPricing `priceDate` pollution reverted, whitelist +`bcNo`/`rfqSentDate`/`restoreSkipped`, **39 tests pass** (incl. clear/budgetary regressions), backward-compat safe. **Re-review (C147): CHANGES REQUIRED — one more missed writer** `commitBcItem` (:27076, the swap-row→BC-item action) unstamped → re-opens F1 for that path under concurrency. Coach confirmed it's the ONLY remaining gap (swept all 48 priceSource sites); everything else verified correct (21 stamps, Layer A re-key, pollBc revert, backward-compat, whitelist, 39 tests). **🔧 One-line fix lane running** (add `priceUpdatedAt:now` at commitBcItem). Coach **pre-approved conditional on this one line** → after it lands, B016-2/3 is APPROVE-for-deploy, ready for the 2-session matrix. Filed **B030** (:27119, LOW). **prod v1.23.10.**

**🟢 2026-07-11 — subagent fleet running (Jon away; Freddy is sole notifier via Pushover).** Standing Marc/Coach/Dez sessions closed clean (no commits). Freddy runs read-only analysis subagents in-session and persists their output to the mapped files; escalates to Jon only on a critical question.
- **Coach lane** — delta review of the never-yet-reviewed gap #5b core (branch `gap5b-f015`, tip `40153e82`) vs the approved plan + analyst review → findings to `COACH.md`. STATUS: 🔄 running.
- **Marc lane** — F014 part-B research → ✅ DONE (`docs/F014-BC-PAYMENT-TERMS-RESEARCH.md`). Finding: **ARC already reads per-customer terms from BC** (standard v2.0 `customers.paymentTermsId` → `paymentTerms` code, live in `ensureQuoteFieldsPopulated`). F014(B) ≈ surface/broaden, not greenfield. ⭐ Open decision for F014 kickoff (non-blocking): live-read-on-demand (exists) vs ARC-editable per-customer default seeded from BC (new; precedent = `customerDefaults`/`_loadCustomerValidity`).
- **Coach lane** — gap #5b core review → ✅ DONE (C141 in `COACH.md`). **Verdict SOUND** — core is data-safe + mergeable on its own; all invariants PASS (rules/functions untouched, cross-user lock + gap#4 guard untouched, advisory-only, data-retention honored, Babel OK). **ONE MAJOR build fix:** the `_leaseInitResolvedRef` save-gate (R1 belt / test G12) is inert (declared+set, never read) → G12 can't pass → **must be wired before F015 §2d ships** (its R1 precondition). +1 MINOR (bfcache `pageshow` claim-tick), +1 NOTE (adopt degraded path, acceptable). **No blocking design question for Jon.**
- **⏭ Resume path (when work restarts):** Marc wires the save-gate (`saveImmediatePanel`:34439 / `onSaveImmediate`:40002 — hold persist while `!_leaseInitResolvedRef`, flush on resolve) → Coach re-reviews the delta → then build §2f/§2d → validate_jsx → test deploy → verify G1–G16 (needs Jon + Andrew + 2nd device) → Jon prod deploy.
- **Freddy** — orchestrating, aggregating results, board-keeping.

**🟢 2026-07-11 (cont.) — BC-RELIABILITY scoping fleet launched** (Jon-selected next big item; read-only scoping → build-ready plans, no cold deploys):
- **Lane 1 (B021)** — ✅ DONE → build-ready fix in `docs/BC-RELIABILITY-PLAN.md`. `bcGatedFetch` (`app.jsx:419-454`) confirmed: NO timeout, releases only on settle → a hung BC request freezes pricing at 95% + deadlocks all BC calls. Fix: AbortController timeout (45s const) + single `finally` release + caller-signal compose + catchable `BcTimeoutError` (pricing phases already log+proceed). ⚠ NOT build-cold (whole BC surface, money-path) → needs Jon sign-off (45s value + const→let) + Coach review + headless tests. Non-blocking now.
- **Lane 2 (B013 + B016)** — ✅ DONE → design in `docs/BC-RELIABILITY-PLAN.md`. **B013:** retry-on-401 + token auto-refresh factored into `bcGatedFetch` (rewrites the Authorization header); pill driven off token VALIDITY via `_bcLast401At`+`_bcHealthSubs` pub-sub (green/amber-reconnecting/red-click) closing the up-to-5-min "green-but-dead" blind window; fix the misleading 401 sync-modal (stamp HTTP status on failed rows, "Reconnect & Retry" affordance). **B016:** reduce on-open churn (kill the 30s price-check repeat, seed `bcPrevSyncCount` from persisted count so a reopen doesn't false-trip sync) + row-level merge in the whole-doc save (M2, last-writer-by-timestamp, delete-marker) + resilient mutations via `safeSave` (M3, kill fire-and-forget). Composes cleanly with B021.
- **✅ BC-RELIABILITY SCOPING COMPLETE.** Full plan: `docs/BC-RELIABILITY-PLAN.md`. Build order: B021 → B013-1 → B013-2/3 → B016-1 → B016-2/3. **3 design Qs for Jon at build-time** (B016-2 conflict policy: timestamp vs lease-aware — likely edge-case now the B012 lock is live · 401 POST auto-replay sign-off · owner-vs-member auth diagnostic scope). Money-path parts (B013-1, B016-2) are PR + Jon sign-off gated.
- **Freddy** — held at a clean seam. Next steps need Jon (rulings + high-stakes builds when back). No new bucket opened unattended per the away-mode contract.
- prod **v1.23.5** · master `be625821` · gap #5b/F015 build HELD on branch `gap5b-f015`; verify + prod deploy still need Jon + Andrew + a 2nd device.

## Prior snapshot — 🌙 SESSION END (2026-07-10) · ★★ B012 P1 SHIPPED v1.23.5 (containment RELAXED) · gap #5b + F015 → verify+deploy NEXT SESSION · prod v1.23.5 · master `ee6659e9`

> # ✅ CONTAINMENT RELAXED (2026-07-10, v1.23.5)
> ## The B012 hard one-editor lock is LIVE on prod — the one-editor-per-project restriction is LIFTED.
> **Two different users on the same project are now server-locked to a single editor (enforced on `editingBy` vs uid); the reviewer workflow hands off/back cleanly; refresh no longer self-locks.** The CRITICAL concurrent-edit BOM data-loss bug is fixed on production. (Same-user-multi-tab hard-block + ghost-lease cleanup ship as the gap #5b + F015 fast-follow.)

**🌙 SESSION END — 2026-07-10.** Two headlines:

**1. ★★ B012 P1 hard one-editor lock — SHIPPED to prod (v1.23.5, release `7b2ba1ba`, rules deployed).** The CRITICAL cross-user concurrent-edit BOM data-loss fix is LIVE; **ONE-EDITOR CONTAINMENT RELAXED** (see banner). **Lock matrix CLOSED — L1/L2/L3/L5/L6/L7/L8 PASS.** Closes the data-loss incident that opened 2026-07-08.

**2. 🔨 gap #5b + F015 (2nd-tab hardening) — designed + analyst-reviewed + Phase-1-core BUILT on branch `gap5b-f015`.** One BroadcastChannel liveness primitive → same-user ADOPT (kills the false "open in another tab") + duplicate-tab detection → makes F015's non-dismissible hard-block safe; cross-user lock untouched. **VERIFY + DEPLOY are NEXT SESSION's first task** — needs **Jon + Andrew + a 2nd device** (G1–G16). **Prod HELD**; branch retained (do NOT auto-merge/deploy).

- **🧭 NEXT SESSION resumes here:** gap #5b + F015 → finish build (Phase 2 F015 if not done) → Coach per-phase delta review → verify **G1–G16** (Jon + Andrew + 2nd device) → **Jon deploy checkpoint (likely v1.23.6)**.
- **⏭ Queue behind the pair (Jon-gated):** **F014 (★ CRITICAL payment terms)** · B024/B025/B026/B027 · F016 · F019 · F017/F018 · G008/G009 · then backlog. P2/P3/P4 lock phases later.
- **📥 New intake stamped this session:** B027 (rev-history drops reviewer notes) · F017/F018 (per-row review notes) · F019 (background standalone Get-New-Pricing). **B024/B025/B026 confirmed PRE-EXISTING** (not P1 regressions). **INBOX clear.**
- **WHO (session end):** Marc — Phase-1-core built on `gap5b-f015`, resumes build next session · Coach — plan finalized, per-phase reviews next · Freddy — coherence PASS, orchestrating close-out · Jon — wrapping the session; next touch = gap #5b/F015 verify+deploy · Dez — final board snapshot posted.
- **STATUS:** 🌙 **SESSION END 2026-07-10.** **★★ B012 P1 SHIPPED v1.23.5** (`7b2ba1ba`, rules deployed) — cross-user data-loss fix LIVE, **containment RELAXED**, matrix CLOSED (L1/L2/L3/L5/L6/L7/L8). **gap #5b + F015** designed + analyst-reviewed + **Phase-1-core built on branch `gap5b-f015`** → **verify + deploy NEXT SESSION** (Jon + Andrew + 2nd device, G1–G16); **prod HELD**, branch retained (no auto-merge). New intake: B027/F017/F018/F019; B024/B025/B026 pre-existing. prod **v1.23.5** · master **`ee6659e9`** · queue F014-CRITICAL · B024–B027 · F016 · F019 · F017/F018 · G008/G009 · **INBOX 0 pending** · *(housekeeping: COACH.md >1500 — flagged for close-out).*

**🆕 Deferred under B012 (in TODO.md):** B013 (chronic BC connection) · B014 (Codale parse-gap) · B015 (lock reactivity) · B016 (mutation write-race) · B017 (special-char PN pricing 400s) · B018 (send-block overlay clarity) · #182 re-push. **⏭ Also queued:** #192 · G005 §10-8 smoke · F004/F005/F007/F008 · B008/B011/B005 · G005 Phase 2.

**🗂️ TRACKER — items stamped by Freddy this session (all in TODO.md; no INBOX capture needed):**
- **F009** — Debug Mode: persistent cross-user activity tracing [Backlog · parked behind B012]
- **F010** — Suggested alternates: multi-alt ALT picker + supplier-portal alt sub-lines [Backlog]
- **F011** — CSV BOM export: add Supplier / Lead Time / Priced Date columns [✅ RESOLVED — shipped **v1.23.4**, `ebe0cbc2`, Jon-confirmed]
- **F012** — Sort the BOM by column (non-destructive view sort) [Backlog]
- **F013** — Reviewer full control + Admin request/force-takeover + reviewer-consent flow (the fuller review-workflow) [Backlog · follow-on after P1]
- **B019** — Extraction progress bar bounces (non-monotonic) [OPEN · LOW/cosmetic · deferred behind P1]
- **B020** — "BC Purchase Price Updates" modal re-prompts every project open [OPEN · MED · cross-ref B016/B012]
- **B021** — Pricing phase hangs at 95% [OPEN · MED · **pre-existing, NOT a P1 regression**; confounded L5's e2e run]
- **G008** — Test channel not in Storage CORS allowlist [OPEN · MED · test-env; being fixed alongside the keep-alive]
- **G009** — Test-environment build versioning (v1.23.x-T### tag + UI badge + T→SHA log; solves fixed-vs-cached test-build confusion) [OPEN · MED · test-infra · routed to Marc, queued after P1 ship]
- **B024** — Reviewer-assignment notification fails silently in prod (brittle BC-email resolution instead of assigned uid; Marc-diagnosed, Jon-endorsed fix = notify by uid) [OPEN · fast-follow · NOT P1-blocking · ⚠ test-env notification-suppression masked it — verify on PROD]
- **B025** — Marked-for-review rows lose checkbox + go RED after Send-for-Review (should stay YELLOW "in review", F003) [OPEN · **Marc VERDICT: PRE-EXISTING, P1-INDEPENDENT** (P1 diff zero hunks in those paths) → non-blocking fast-follow]
- **B026** — Can't start a 2nd tech-review cycle without cancelling the prior one (re-checking TR boxes should re-arm a new review + re-enable Send-for-Review) [OPEN · **Marc VERDICT: PRE-EXISTING, P1-INDEPENDENT** (checked vs P1 diff, zero hunks) → non-blocking F003 fast-follow; workaround: cancel prior / fresh project]
- **F014** — ★ CRITICAL — Customer-specific payment-terms note on ALL quotes + identify a BC storage field (e.g. WTR = 30% ARO / 40% AT Procurement / 30% Shipment; related #117) [Backlog · **CRITICAL · slotted TOP of the post-P1 queue**; classed FEAT]
- **B022** — Login "Continue with Microsoft" misleads existing users (account-setup only) [OPEN · MED]
- **B023** — Quote Summary line-row overflow clips total price → truncate "IN PRE-REVIEW"→"IN ETR" [OPEN · LOW/cosmetic]
- **B018** — send-block overlay clarity **+ NOW: BOM-row-RED-despite-reqs** (PRJ402100 3044102, 1-cent clears; decisive repro folded in — same `_isBomRowFlaggedRed` priceDate-staleness root) [OPEN · MED]
- **F015** — Hard-block 2nd-tab modal (same-user) [Backlog · FAST-FOLLOW · **MUST follow P1 gap #5b** close-ghost fix]
- **F016** — "REJECTED REVIEW" dashboard column (RED header, like the RFQ Send/Receive header) — surfaces projects whose tech review was rejected so they don't fall into limbo (visible action queue) [Backlog · ties to F003 reject-path + B026 re-arm-after-reject]
- **B027** — Revision history doesn't persist the engineer's review notes (transient light-red only) [OPEN · fast-follow]
- **F017** — Engineer per-row approval notes [Backlog · F003 review-workflow]
- **F018** — Owner per-row notes before sending a review [Backlog · F003 review-workflow]
- **F019** — Background standalone Get-New-Pricing (make pricing survive nav-away + show tile progress like extraction; multi-project parity) [Backlog · fast-follow · from the L5 reframe — foreground pre-existing, not a P1 regression]
- **M001** — User engagement telemetry (page-access / activity cadence / per-project time) [Backlog · NEW METRICS/M category · privacy guard: cadence-not-verbatim; overlaps F009 pipeline]
- **M001** — User engagement telemetry (page-access / activity cadence / per-project time; "engaged"/"periodic"/"no activity") [Backlog · scoped by Freddy — privacy guard: cadence-not-verbatim-keystrokes; overlaps F009 capture pipeline (shared-vs-standalone TBD)] — **★ first item of the NEW "METRICS" category (prefix `M`)** established in NUMBERING-CONVENTION.md 2026-07-10 (own sequence + ranking; Jon expects more M-items to follow).
- **📥 INBOX: 0 pending** — all captures this session triaged + promoted (F014 / B022 / B023 / M001; red-row folded into B018).

## Progress Log (periodic snapshots, newest first)
- **[2026-07-10 23:52 MDT]** **🌙 SESSION END (2026-07-10) — final snapshot.** **★★ B012 P1 hard one-editor lock SHIPPED to prod (v1.23.5, `7b2ba1ba`, rules deployed)** — the CRITICAL cross-user concurrent-edit BOM data-loss fix is LIVE; **ONE-EDITOR CONTAINMENT RELAXED**; lock matrix CLOSED (L1/L2/L3/L5/L6/L7/L8). Closes the incident opened 2026-07-08. **gap #5b + F015 (2nd-tab hardening)** — designed + analyst-reviewed + **Phase-1-core BUILT on branch `gap5b-f015`**; one BroadcastChannel liveness primitive (same-user ADOPT + duplicate-tab detection) makes F015's non-dismissible hard-block safe, cross-user lock untouched. **VERIFY + DEPLOY = NEXT SESSION's first task** (needs Jon + Andrew + a 2nd device; G1–G16); **prod HELD**, branch retained (no auto-merge/deploy). **New intake stamped this session:** B027 (rev-history drops reviewer notes) · F017/F018 (per-row review notes) · F019 (background standalone Get-New-Pricing); **B024/B025/B026 confirmed pre-existing** (not P1 regressions). **INBOX clear.** Also shipped earlier today: **F011** (CSV BOM export columns, v1.23.4). prod **v1.23.5**, master **`ee6659e9`**. Team standing down; next session resumes at gap #5b/F015 verify+deploy.
- **[2026-07-10 23:33 MDT]** **🔨 gap #5b + F015 — DESIGN COMPLETE → BUILDING (Marc, per-phase).** Plan finalized (Coach, `76b82e92`) + **§2g "Edit here" round-trip fix** (Freddy coherence catch — a cross-device loser was otherwise stranded read-only); Freddy coherence-confirm = PASS; Jon-approved (rulings folded), no new gate. **Marc building per-phase:** Phase 1 = **gap #5b** (BroadcastChannel liveness + tab-id uniqueness + same-uid adopt + relinquish-on-takeover §2e + "Edit here" §2g + pagehide release + aging-lease countdown) → Phase 2 = **F015** non-dismissible hard-block riding on it. Coach re-reviews each phase delta → verify **G1–G16** → **Jon DEPLOY checkpoint** (likely one release v1.23.6). Build/review/verify runs autonomously; Jon comes in at deploy. Cross-user server lock untouched throughout. B012 P1 SHIPPED (v1.23.5) holds; queue behind: F014 (CRITICAL) · B024/B025/B026/B027 · F016 · F019 · F017/F018 · G008/G009. Master `ee6659e9`.
- **[2026-07-10 23:17 MDT]** **✅ gap #5b + F015 — DESIGN APPROVED (Freddy Analyst Review SOUND), finalizing → build next.** Coach design (`docs/GAP5B-F015-PLAN.md`) → Freddy Analyst Review = SOUND/APPROVE (`docs/GAP5B-F015-ANALYST-REVIEW.md`, `2e0b7a8b`). **One BroadcastChannel liveness primitive solves both:** ghost → same-user ADOPT (kills the false "open in another tab" Jon hit on prod) + duplicate-tab detection → makes F015's non-dismissible hard-block safe. **Cross-user server lock untouched throughout.** **Jon's 3 rulings:** Q1 = accept ≤90s + ADD a live countdown ("available in ## seconds", auto-grants at 0); Q2 = non-dismissible confirmed; Q3 = adopt across devices (+ losing device relinquishes to avoid same-user clobber). **Freddy 4 build notes R1–R4** (dup-tab editable window · throttled-tab false-adopt · simultaneous-adopt race · tie-break determinism). **Pipeline:** Coach finalize plan (rulings + R1–R4) → Freddy coherence check → Marc builds (likely one release v1.23.6) → Coach delta review → verify G1–G11 → Jon deploy; per-phase gated. B012 P1 remains SHIPPED (v1.23.5). Queue behind: F014 (CRITICAL) · B024/B025/B026/B027 · F016 · F019 · F017/F018 · G008/G009. Master `ee6659e9`.
- **[2026-07-10 22:54 MDT]** **🔨 NEW PHASE — gap #5b + F015 pair (2nd-tab hardening) STARTED (Jon greenlit).** Top fast-follow after B012 P1 shipped. **Goal:** deliver Jon's non-dismissible "Close the tab" hard-block SAFELY by first killing the ghost lease that would otherwise trap a user ≤90s. **gap #5b** = reliable lease release on tab close (pagehide/sendBeacon vs the tx that dies on unload) + same-user ADOPT (your own reopen re-takes the lease → no false "open in another tab" — the warning Jon hit live on prod). **F015** = then remove "View read-only" → non-dismissible hard-block + a tab-id uniqueness fix so Chrome "Duplicate Tab" is also detected. **Cross-user server lock unchanged throughout.** **Phase:** Discovery/Design — routed to Coach for `docs/GAP5B-F015-PLAN.md` → Freddy Analyst Review → Jon approve → Marc build → verify → deploy; per-phase gated (holds for Jon between phases). **B012 P1 remains SHIPPED + matrix CLOSED (v1.23.5).** Queue behind this pair: F014 (CRITICAL payment terms) · B024/B025/B026/B027 · F016 · F019 · F017/F018 · G008/G009. Prod v1.23.5, master `ee6659e9`.
- **[2026-07-10 22:43 MDT]** **★ B012 P1 — MATRIX CLOSED, fully in the books.** **L5 (async writeback survives nav-away) ✅ PASS via the EXTRACTION path** — extraction is the backgrounded task (`bgStart`/`_bgTasks`); the keep-alive fix renews its lease so a long backgrounded run's writeback lands post-nav-away (Jon's months of extract+price-while-away confirm it). The "nav-away kills **Get Prices**" finding = standalone Get-New-Pricing is **FOREGROUND, PRE-EXISTING** (P1 diff touches zero pricing/abort code) → wrong L5 vehicle → **new enhancement F019.** **Full matrix: L1/L2/L3/L5/L6/L7/L8 ✅** (no L4). **B012 P1 = SHIPPED v1.23.5 + matrix CLOSED** — critical cross-user data-loss bug fixed on prod, containment relaxed. **NEW INTAKE stamped: F019** — "Background standalone Get-New-Pricing" (make pricing survive nav-away + tile progress like extraction; multi-project parity; fast-follow, Jon to prioritize). **Fast-follow queue:** gap #5b + F015 (pair) → F019 → B024/B025/B026/B027 → F016 → F017/F018 → G008/G009; P2/P3/P4 later. Prod v1.23.5, master `ee6659e9`.
- **[2026-07-10 22:31 MDT]** **★★ B012 P1 — ✅ SHIPPED TO PROD. The CRITICAL concurrent-edit data-loss bug is FIXED on production.** **Prod v1.23.5** (matrix-arc.web.app), release `7b2ba1ba`, **Firestore rules deployed**, DEPLOY Pushover fired; live master `ee6659e9`. **The fix is LIVE:** two different users on the same project are server-locked to one editor (`editingBy` vs uid); the reviewer workflow hands off/back cleanly; refresh no longer self-locks; dismissible "View read-only" escape on the 2nd-tab modal. **⇒ ONE-EDITOR CONTAINMENT RELAXED** — the team is no longer restricted to one-editor-per-project; Jon can bring sales back onto ARC. **Matrix:** L1/L2/L3/L6/L7/L8 ✅ PASS; **L5** (async writeback survives nav-away) gap found + FIXED (keep-alive, in the shipped build + Coach-reviewed) but the clean LIVE re-run wasn't stamped → **ONE post-deploy L5 prod re-run pending (Jon, ~2 min)** to fully close the matrix. (No L4; L8b covered by reopen testing.) **Fast-follow (Jon-gated, not started):** gap #5b + F015 (pair) → B024/B025/B026/B027 → F016 → F017/F018 → G008/G009; P2/P3/P4 lock phases later. **Huge milestone** — closes the data-loss incident that opened 2026-07-08. Prod v1.23.5, master `ee6659e9`.
- **[2026-07-10 22:27 MDT]** **🚀 ★ B012 P1 — SHIPPING TO PROD NOW (Jon's go).** All gates GREEN: **Jon clean-slate re-confirm ✅** (tab2 read-only immediately, no editable window, dismissible modal; tab1 kept edit) · **Coach prod-gate confirm ✅ (`ed168052`)** (full stack cleared) · **Jon prod sign-off ✅ ("Do it").** Marc running the prod deploy from the main checkout — merge → `deploy.sh` (client + version bump) → `firestore:rules`; full P1 stack `b654c5d6` → `89e289f7`. **Cross-user B012 data-loss guarantee code-confirmed intact throughout.** **ON Marc's deploy confirm** (prod version + master tip + live on matrix-arc.web.app) → **the one-editor containment RELAXES — the B012 concurrent-edit data-loss fix is LIVE.** Fast-follow (Jon-gated): gap #5b + F015 (pair) → B024/B025/B026/B027 → F016 → F017/F018 → G008/G009. Containment HOLDS until the deploy confirm; prod v1.23.4 → deploying. Master `c8a69538` → bump.
- **[2026-07-10 22:21 MDT]** **🟢 P1 pre-tick guard Coach-PASS; Jon ruled SHIP DISMISSIBLE. PROD still HELD (close to ship).** **Pre-tick guard (Root A fix, `37b15953`) — Coach PASS (`2b4987e0`):** a tab opening onto an already-held project is now read-only from first render (no editable window); the sole editor never flashes read-only. **Coach flagged a prod-gate trap:** the non-dismissible "Close the tab" modal + the deferred ghost-lease (#5b) = a ≤90s self-trap on close-then-reopen-within-90s (non-data-loss); non-dismissibility buys zero data-safety. **Jon RULED: SHIP DISMISSIBLE NOW** — restore the "View read-only" escape for P1; the non-dismissible hard-block ships WITH gap #5b (F015 fast-follow). **Pipeline:** Marc restore escape + validate + deploy test → Coach quick prod-gate confirm on the restore delta → Jon clean-slate re-confirm (fresh Ctrl+T 2nd tab = read-only immediately + dismissible modal) → prod sign-off → SHIP + relax containment. **Cross-user B012 data-loss guarantee code-confirmed intact throughout.** **Fast-follow family (same-user-multi-tab):** gap #5b + F015 together → then B024/B025/B026/B027 · F016 · F017/F018 · G008/G009. Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 21:43 MDT]** **🟢 P1 SHIP-SAFE — severity RESOLVED (data-loss guarantee intact); Jon chose to fold in a pre-tick guard. PROD still HELD.** Marc's verdict (code-confirmed): **the CROSS-USER data-loss guarantee is UNAFFECTED** — server rules lock on `editingBy` vs uid, no tab-id involved. The episode Jon saw = **same-user-two-tabs, client-only, NON-data-loss**: a brief **pre-first-tick window** where a freshly-opened tab is editable until its first async lease check resolves (same class as the deferred #2b window; NOT gap #5a, NOT the button). **Jon's call: FOLD IN a small pre-tick guard before shipping** (he saw the rough edge, wants it clean) rather than ship-now-fast-follow. **Pipeline:** Marc builds guard → validate → Coach delta review → deploy test → Jon re-confirms on a clean slate (fresh Ctrl+T 2nd tab = read-only immediately, no editable window) → prod sign-off → SHIP + relax containment. Marc also checking if keep-alive/release-on-exit timer cleanup is in-scope or a #5b fast-follow. **Prod HELD until the guarded build re-confirms.** Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 21:15 MDT]** **⚠ P1 root REFRAMED — NOT a regression; an INIT-TIMING RACE in the #5a tab-id. PROD still HELD.** The button fold-in (`97c8ec31`) is **CLEARED** (Marc's diff proof + Jon's console check). **Real root:** an init-timing race in the gap #5a tab-id, present since `98576f8e` (not the button). Jon's two tabs had DIFFERENT ids (bca1c3de vs fcddfef8) but **tab 2 read `null` FIRST** → a null tab-id didn't match the held lease → no modal → both editable; once the id initialized, the lock engaged. Intermittent, same-user-2nd-tab. **🔑 SEVERITY CALL PENDING (decides ship-blocker vs fast-follow):** Marc confirming whether the null-id window can touch the **CROSS-USER lock** (the real B012 data-loss, uid/server-enforced) or is **strictly same-user-multi-tab.** Prod HELD until severity resolved + fix + Jon re-confirms. **Lesson (Freddy):** he mis-attributed it to the button edit; Marc's diff evidence corrected it — **verify root by artifact before calling a regression** ([[feedback_runtime_artifact_over_code_read]] / [[feedback_surface_first_and_instrument_timing_bugs]]). Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 21:02 MDT]** **⚠ P1 REGRESSION on the pre-ship modal tweak — PROD HELD, back to building.** The fold-in (`97c8ec31`, remove "View read-only" from the 2nd-tab "Already Open" modal) **broke 2nd-tab detection:** Jon's confirm showed a fresh Ctrl+T tab (the exact L8 method that PASSED) now gets **NO "Already Open" modal at all → BOTH instances fully open.** NOT the Chrome Duplicate-Tab caveat — a genuine regression from that edit. **P1 GREEN → back to Building.** Marc diagnosing the `97c8ec31` delta → fix so the modal reappears with only "Close the tab" → re-deploy test → Jon re-confirm → prod. **Prod deploy HELD until the fixed build re-confirms.** Everything else unchanged (all prior gates still green on `@98576f8e`; fast-follows queued). Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 20:50 MDT]** **🟢 ★ P1 FULLY GREEN on test — final re-test complete.** Jon: ✅ gap #5a refresh false-positive · ✅ gap #4 reject-path hand-back (rows stayed yellow + checkboxes) · ✅ L8 2nd-tab view-only. **ALL final checks GREEN.** **One Jon-approved pre-ship tweak folding in:** Marc removing the **"View read-only" button** from the 2nd-tab "Already Open" modal (leave only **"Close the tab"**) → re-deploy test → **Jon ~30-sec confirm → prod sign-off → SHIP B012 + relax containment.** Nearly at the finish line. **New intake stamped (fast-follow, from the re-test):** **B027** (revision history doesn't persist the engineer's review notes — transient light-red only) · **F017** (engineer per-row approval notes) · **F018** (owner per-row notes before sending a review). **Fast-follow queue:** gap #5b · F015 · B024/B025/B026 · B027 · F016/F017/F018 · G008 · G009 · backlog (F009/F010/F012/F013/F014-CRITICAL/M001). Prod v1.23.4 untouched; containment HOLDS until ship. Master `c8a69538`.
- **[2026-07-10 20:44 MDT]** **🟢 P1 FINAL RE-TEST — 2 of 3 GREEN, L8 pending.** Combined build `@98576f8e` on test (all 6 layers verified live by Marc). Jon's results: **✅ gap #5a refresh false-positive FIXED** (hard-refresh no longer shows "open in another tab") · **✅ gap #4 reject-path hand-back PASS** (edit returns to owner; rows stayed yellow + checkboxes intact) · **regression — Jon running targeted L8 (2nd-tab view-only) NOW** (L1/L3/L5/L6 unaffected by the sessionStorage delta → still-green). **On L8 pass → Freddy routes prod-deploy sign-off to Jon → ships B012 → relax containment.** **Fast-follow (post-ship):** gap #5b (close-ghost) · F015 (2nd-tab hard-block, incl. Chrome Duplicate-Tab caveat) · B024/B025/B026 · F016 · G008 · G009 · then the backlog (F009/F010/F012/F013/F014/M001). Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 20:13 MDT]** **🟢 ★ P1 COMPLETE + COACH-CLEARED — Coach PASSED gap #5a (`fdd45dfc`).** Combined P1 = all 4 gaps + the reload-ghost fix, all Coach-reviewed. **Marc RE-DEPLOYING to test now.** **Jon's FINAL re-test next:** refresh scenario + reject-path (Test B) + regression **L1/L3/L5/L6/L8** → green → **PROD SIGN-OFF → ship → relax containment.** **Fast-follow queue:** gap #5b (close-ghost) · F015 (hard-block 2nd-tab) · B024/B025/B026 (pre-existing review bugs) · F016 (rejected-review column). Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 20:06 MDT]** **🗂️ F016 stamped + gap #5a → Coach re-review.** **F016** — "REJECTED REVIEW" dashboard column (RED header, like the RFQ Send/Receive header) to surface projects whose tech review was rejected so they don't fall into limbo (visible action queue) [Backlog; ties to F003 reject-path + B026 re-arm-after-reject]. **P1:** gap #5a (reload-fix, sessionStorage tab-id) Marc BUILT → **now in Coach re-review** → re-deploy → Jon's final refresh/reject/regression re-test → prod sign-off. Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 20:03 MDT]** **✅ ★ P1 CLEAN OF REVIEW REGRESSIONS — all 3 re-test bugs ruled PRE-EXISTING.** Marc verified **B026** (can't re-arm a 2nd review) = PRE-EXISTING, NOT a P1 regression → doesn't block ship. **PATTERN:** all three review-workflow bugs from Jon's re-test — **B024** (reviewer-notif), **B025** (rows go red on submit), **B026** (re-arm) — are Marc-verified **PRE-EXISTING / P1-INDEPENDENT** (each checked vs the P1 diff → zero hunks). ⇒ **the P1 lock work introduced NO review-workflow regressions; the ship is clean of these** — they're separate F003 fast-follows. **P1 remains:** gap #4 handoff CONFIRMED + gap #5a reload-fix building → refresh/reject/regression re-test → prod sign-off. **Fast-follow queue:** gap #5b · F015 · B024 · B025 · B026. Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 20:01 MDT]** **🔧 GAP #5 SPLIT (Jon-approved) — reload-ghost fix PULLED INTO P1.** Root confirmed (Marc): the **tab-id regenerates on every page-load** → every hard-refresh false-positives "open in another tab" (~90s lockout) — P1-introduced + frequent enough to fix before ship. **#5a (reload-ghost)** = persist tab-id in sessionStorage (~5 lines) → **Marc BUILDING now** → Coach re-review → re-deploy → Jon retests the refresh scenario. **#5b (close-ghost** — closed-tab lease for a brand-new tab; rarer, self-heals) = **stays fast-follow.** P1 remaining re-test = refresh scenario + reject-path (Test B) + regression → prod sign-off (gap #4 handoff already CONFIRMED). **Fast-follow queue:** gap #5b · F015 · B024 · B025 · B026. Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 19:58 MDT]** **🐞 B026 stamped during Jon's P1 re-test.** Can't start a 2nd tech-review cycle without cancelling the prior one — re-checking TR boxes should re-arm a new review + re-enable Send-for-Review. **Marc diagnosing regression-vs-pre-existing** (if from the P1 review-handler changes → P1-relevant; else separate). NOT blocking the re-test (workaround: cancel prior / fresh project). Adds to the re-test bug set (B024 fast-follow · B025 pre-existing/P1-indep · B026 TBD). **P1 re-test still in progress: gap #4 handoff CONFIRMED; remaining = reject-path (Test B) + Test C banner + regression → prod sign-off.** Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 19:54 MDT]** **🟢 ★ P1 GAP #4 HANDOFF-BACK CONFIRMED in Jon's re-test — the headline fix works.** Andrew approved → **Jon REGAINED edit**; Andrew saw **"Project in use"** (live indicator working on the locked side). **Remaining before PROD SIGN-OFF:** Test B (reject-path hand-back) + Test C (live banner in Jon's OWN view) + regression L1/L3/L5/L8 → then prod ship → relax containment. **B025 VERDICT (Marc): PRE-EXISTING, P1-INDEPENDENT** (P1 diff has zero hunks in those paths) → does NOT block the ship; separate fast-follow. **B024** (silent reviewer-notify fail) also fast-follow. **Fast-follow queue:** gap #5 (tab-close ghost) · F015 (hard-block 2nd-tab) · B024 · B025. Prod v1.23.4 untouched; containment HOLDS until ship. Master `c8a69538`.
- **[2026-07-10 19:49 MDT]** **🐞 2 new bugs stamped during Jon's P1 re-test (both SEPARATE from the P1 lock; neither blocks P1).** **B024** — reviewer-assignment notification fails silently in prod (brittle BC-email resolution instead of the assigned uid; Marc-diagnosed, Jon-endorsed fix = notify by uid); NOT P1-blocking, fast-follow; ⚠ test-env notification-suppression masked it → verify on PROD. **B025** — marked-for-review rows lose the checkbox + go RED after Send-for-Review, should stay YELLOW "in review" (F003); **Marc diagnosing regression-vs-pre-existing — if it's from the recent review/lease changes it becomes P1-relevant.** P1 re-test itself (gap #4 handoff-back + live banner + L1/L3/L5/L8) still in progress on test; these don't block it. Prod v1.23.4 untouched; containment HOLDS. Master `c8a69538`.
- **[2026-07-10 19:29 MDT]** **🧪 P1 LIVE ON TEST (all 4 gaps closed + Coach-passed) → Jon RE-TESTING NOW.** Combined P1 (with gap #4) deployed to test → Jon running the final gate: **L6 post-review handoff-back + the live "🔒 {holder} is editing" banner + regression L1/L3/L5/L8.** Green → **prod sign-off.** ⚠ Gap #5 (ghost-tab) may still show in L8 — **EXPECTED** (fast-follow, not a P1 blocker; data-loss guarantee unaffected; rides F015). **NEW intake stamped: G009** — test-environment build versioning (v1.23.x-T### tag + UI badge + T→SHA log; solves the fixed-vs-cached test-build confusion) [OPEN·MED, test-infra], routed to Marc, queued after the P1 ship. **WHO:** Jon → re-testing then prod gate · Marc → P1 on test, standby (G009 next) · Coach → gaps all reviewed/PASS · Freddy → routing green→prod sign-off. **Prod v1.23.4 untouched; containment HOLDS until P1 ships.** Master `c8a69538`.
- **[2026-07-10 19:25 MDT]** **🟢 ★ ALL FOUR P1 GAPS CLOSED + REVIEWED — Coach PASSED gap #4 (`d1c4b4e1`).** Keep-alive · Fix B · owner-priority reviewer-exemption · post-review handoff-back + live "🔒 {holder} is editing" indicator — all built + reviewed. **Marc RE-DEPLOYING combined P1 to test** → Jon final re-test (L6 post-review handoff + the live lock banner + regression L1/L3/L5/L8) → green → **PROD SIGN-OFF** → merge/deploy P1 to prod → RELAX containment. **Fast-follow after ship:** gap #5 (tab-close ghost lease; data-loss guarantee unaffected) → then F015 (hard-block 2nd-tab modal; MUST follow gap #5). **WHO:** Marc → re-deploying to test · Jon → final re-test then prod gate · Coach → all gaps reviewed/PASS, done unless re-test surfaces something · Freddy → routing Jon's green to prod sign-off. **Prod v1.23.4 untouched; containment HOLDS until ship.** Master `c8a69538`.
- **[2026-07-10 19:23 MDT]** **🟢 P1 NEAR PROD — gaps #1–#3 done, gap #4 in Coach re-review (Freddy full catch-up after the gap #4/#5 flurry).** Gaps #1 keep-alive + #2 Fix B + #3 owner-priority reviewer-exemption: BUILT, Coach-PASS, live on TEST. **Matrix: L1/L2/L3/L5(lock)/L7/L8 PASS; gap #3 CONFIRMED** (reviewer approves under owner presence). **★ Gap #4** (post-review handoff-back + no live lock indicator): diagnosed → Coach design (`3749771d`) → Jon APPROVED → **Marc BUILT (`93607beb`** — all 4 parts: directed hand-back + fail-closed + top-level persistent "🔒 {holder} is editing" banner + #2b deferred) → **NOW in Coach delta re-review** → on PASS: re-deploy test → Jon re-tests L6 post-review handoff + indicator → **PROD SIGN-OFF.** **Gap #5** (same-uid ghost-tab lease) = **FAST-FOLLOW** (data-loss guarantee UNAFFECTED; self-heals ≤90s; root = Fix B's transactional release doesn't fire on tab-close) → rides with **F015**. **FAST-FOLLOW queue after P1 ships:** gap #5 → then F015 (hard-block 2nd-tab modal; MUST follow gap #5). **WHO:** Marc → gap #4 built, standby to re-deploy on Coach PASS · Coach → re-reviewing gap #4 delta · Jon → will re-test L6 post-deploy · Freddy → routing. **Intake this session (all triaged):** B019/B020/B021/B022/B023 (+B018 fold) · G008 · F012/F013/F014(CRITICAL)/F015 · NEW category M + M001. F011 shipped v1.23.4. F009/F010 backlog. **Prod v1.23.4 untouched by P1; containment HOLDS until P1 ships.** Master `c8a69538`.
- **[2026-07-10 17:18 MDT]** **📥 INTAKE CLEARED — all 4 session captures triaged + promoted by Freddy; INBOX now 0.** **F014** (★ CRITICAL — customer-specific payment-terms note on ALL quotes + identify a BC storage field; e.g. WTR = 30% ARO / 40% AT Procurement / 30% Shipment) classed FEAT, **slotted TOP of the post-P1 queue**; related #117. **B022** (Login "Continue with Microsoft" misleads existing users, MED). **B023** (Quote Summary line-row overflow clips total price → truncate "IN PRE-REVIEW"→"IN ETR", LOW/cosmetic). The BOM-row-RED-despite-reqs report (PRJ402100 3044102, 1-cent clears) was **folded into B018** (decisive repro added; same `_isBomRowFlaggedRed` priceDate-staleness root; no new number). Dez cleared all bullets + triage-logged per Freddy's request; INBOX empty. (No change to the P1 thread — gap #3 still in build.) Prod = v1.23.4, master `c8a69538`.
- **[2026-07-10 16:42 MDT]** **🔧 P1 — L6 re-run found a THIRD small gap (owner-priority, not the lease); fix in build.** **Gap #2 Fix B WORKING** (reviewer resolves rows + Review Drawings + redlines). **★ Gap #3:** the review **"Approved" button was greyed by OWNER PRIORITY MODE** (owner present → non-owner reviewer blocked) — NOT the lease. Coach designed the fix (`1af77a0c`); **Jon ruled + confirmed FULL exemption** (assigned reviewer has full control during a pending review, exempt from owner-priority); **Marc BUILDING now** (compute-level exemption). **Pipeline:** Marc build → Coach re-review delta → re-deploy test → Jon re-runs L6 → prod gate. **NEW FEATURE stamped: F013** (Reviewer full control + Admin request/force-takeover + reviewer-consent flow — the fuller workflow; follow-on after P1). **Intake:** Dez's 2 new captures (Quote Summary overflow clips price; red-row-despite-reqs PRJ402100 3044102 overlaps B018) noted by Freddy → triage next pass (INBOX = 3 pending). **WHO:** Marc → gap #3 · Coach → on deck to re-review · Jon → will re-run L6 · Freddy → routing. **Prod v1.23.4 untouched; containment HOLDS.** Master `c8a69538`.
- **[2026-07-10 15:13 MDT]** **🧪 AWAY MODE ENDED (Jon back) — both P1 gaps FIXED + combined P1 LIVE ON TEST; pre-prod matrix RE-RUN in progress.** Gap #1 keep-alive (`d1b1b07e`) + Gap #2 Fix B eligibility-gated claim (`a7c4bbf2`, reviewer becomes holder during review) → **Coach combined re-review PASS (`a5d33dab`) + admin co-sign (`2541cd52`)**. Marc deployed combined P1 to test in away-mode (`7c696356`), verified serving; **PROD clean/untouched (v1.23.4)**. **NOW:** Jon + Andrew re-running the pre-prod gate — **L6** (reviewer-approve = Fix B check) + **L5** (async writeback = keep-alive check; may hit B021 pricing hang, separate) + **L1/L3/L8** regression re-checks. **Prior results stand:** L1/L2/L3 PASS, L5 load-bearing PASS, L7 PASS (no-clobber). **NEXT GATE:** green matrix → Jon prod sign-off → merge/deploy P1 to prod → RELAX containment → then P2/P3/P4 (request-grant / force / priority-hold). **Open bugs this session:** B019, B020, **B021 (BC-fetch timeout)**, G008 (test CORS) + F009/F010/F012 (F011 shipped v1.23.4). **Phase B (PR #5) shelved. Containment HOLDS until P1 ships on prod.** Prod = v1.23.4, master `c8a69538`.
- **[2026-07-09 22:21 MDT]** **🔧 P1 = PASS-PENDING 2 FIXES (matrix surfaced a 2nd small gap — working as intended).** Both gaps small + Coach-owned: **GAP #1 keep-alive heartbeat** (nav-away → lease stale in ~90s → could reject an orphaned writeback) — **FIX BUILT `d1b1b07e`, Coach re-reviewing**; **GAP #2 (L6) reviewer can't approve/resolve tech-review while someone holds the lock** (lease lacked the review-lock's reviewer exemption; Marc+Coach verified e2e) — **Jon APPROVED Coach's Fix B** (eligibility-gated claim: only an eligible editor holds the lease → reviewer becomes holder during review) — **Marc BUILDING.** **Matrix so far:** L1/L2/L3 PASS · **L5 load-bearing PASS** (no lease rejection; e2e confounded by B021 pricing hang + G008 CORS) · **L6 FAIL→Fix B** · L7 in progress (Jon role-swapped the offline step; Andrew stays a simple user) · L8/L8b solo next. **NEXT:** Coach re-reviews both deltas → Marc re-deploys to test → Jon re-runs L5+L6 (+ finish L7/L8/L8b) → prod gate. **Open items logged this session:** B019/B020/B021/G008 + F009/F010/F012 (F011 shipped v1.23.4). **Phase B (PR #5) shelved. Containment HOLDS until P1 ships on prod.** Prod = v1.23.4, master `c8a69538`.
- **[2026-07-09 22:07 MDT]** **🔧 MILESTONE — P1 = PASS-PENDING keep-alive fix; ★★L5 assertion PASSED.** Live matrix (test, Jon+Andrew): **L1/L2/L3 PASS**; **L5 load-bearing assertion PASS** — no lease rejection, the orphaned writeback was NOT blocked (the core B012 guarantee held), though the end-to-end run was confounded by a pre-existing pricing hang (**B021**) + test-channel CORS (**G008**). **L6/L7/L8 in progress (Jon).** **★ P1 GAP (Coach C140 addendum, `69434745`):** keep-alive heartbeat missing (plan §7b) — nav-away during a bg task lets the lease go stale in ~90s → a 2-user claim could reject the orphaned writeback ⇒ **P1 verdict = "PASS PENDING the keep-alive fix," required before prod.** **Jon APPROVED the fix → Marc BUILDING the module-scoped keep-alive + the G008 CORS fix now** (parallel to L6–L8) → Coach re-reviews the delta → re-deploy to test → re-run L5 clean → prod gate. **New items this session:** B021 (pricing hangs at 95%, MED, pre-existing NOT P1), G008 (test channel missing from Storage CORS allowlist, MED test-env), plus F012/B019/B020 already logged. **WHO:** Jon → L6/L7/L8 · Marc → keep-alive + G008 · Coach → on deck for the keep-alive delta re-review · Freddy → routing. **Phase B (PR #5) shelved. Containment HOLDS until P1 ships on prod.** Prod = v1.23.4, master `c8a69538`.
- **[2026-07-09 21:49 MDT]** **🧪 LOCK-MATRIX IN PROGRESS (L1/L2/L3 PASS) · 🎉 F011 SHIPPED v1.23.4 · catch-up on Freddy-stamped items.** **Prod now v1.23.4** (`c8a69538`) — **F011** (CSV BOM export: add Supplier / Lead Time / Priced Date columns) shipped `ebe0cbc2`, Jon-confirmed, RESOLVED. **B012 P1** still TEST-only (prod client INERT); **live lock-matrix (Jon+Andrew) results so far: L1 ✅ · L2 ✅ · L3 ✅ · L5 ⚠ blocked-retry** (★★ load-bearing — extraction path, `originalPdfPath`) **· L6 🔄 in progress · L7/L8/L8b ⏳ pending.** **Freddy stamped + logged straight to TODO.md this session** (Jon reported mid-matrix; Freddy is the allocator — no INBOX capture on Dez's side): **F009** (Debug Mode) [Backlog·parked] · **F010** (Suggested alternates) [Backlog] · **F011** [RESOLVED v1.23.4] · **F012** (BOM column-sort, non-destructive) [Backlog] · **B019** (extraction progress bar non-monotonic) [OPEN·LOW/cosmetic·deferred behind P1] · **B020** ("BC Purchase Price Updates" modal re-prompts every project open) [OPEN·MED·xref B016/B012]. **Still in INBOX (unstamped):** BUG "Login 'Continue with Microsoft' misleads existing users". **NEXT GATE:** matrix green (esp. ★★L5) → Jon prod sign-off → P1 to prod → RELAX manual containment. Phase B (PR #5) shelved+retained. **Containment HOLDS.** Prod = v1.23.4, master `c8a69538`.
- **[2026-07-09 20:47 MDT]** **🧪 MILESTONE — HARD-LOCK P1 DEPLOYED to test → live lock-matrix RUNNING (Jon + Andrew).** P1 live on **matrix-arc-test.web.app** + **Firestore rules project-wide**; **prod verified INERT** (zero lease markers in prod bundle, behaves as before). Run sheet delivered to Jon (`P1-Lock-Matrix.docx`). **Matrix cases:** L1 different-user view-only lockout · L2 clean-exit handoff · L3 crash→reclaim ≤90s · **L5 async-writeback survives (★★ load-bearing)** · L6 reviewer-approve survives · L7 offline stale-writer rejected · L8 same-user 2nd-tab modal · L8b reopen→editable. **Load-bearing: L1/L3/L5/L8.** **⚠ Run on ISOLATED throwaway projects** (rules project-wide). **WHO:** Jon → driving matrix (needs Andrew for cross-user L1/L2/L3/L6/L7) · Marc → standby to turn findings fast (F011 CSV-export queued next) · Coach → P1 done C140, re-reviews P2/P3/P4 later · Freddy → routing, will take matrix results → Jon prod-deploy sign-off. **NEXT GATE:** matrix green → Jon prod sign-off → P1 to prod → RELAX manual one-editor containment. Phase B (PR #5) shelved+retained. F009/F010/F011 in Features backlog. Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 20:35 MDT]** **🚀 MILESTONE — HARD-LOCK P1 passed Coach C140 (no blockers) + Jon approved test deploy → Marc DEPLOYING P1 to test.** Deploy scope: client → **matrix-arc-test** + **Firestore rules project-wide** (backward-compat/inert on prod, verified). **P1** (core lock + read-only + 3-state open modal) delivers the B012 guarantee alone. **NEXT:** live lock-matrix on test (**Jon-driven, needs a 2nd user**) — **L1** different-user view-only lockout · **L3** crash→reclaim ≤90s · **L5** async-ownership writeback (★ load-bearing) · **L8** same-user 2nd-tab modal → Jon prod sign-off → prod deploy → RELAX manual containment. **⚠ Matrix must run on ISOLATED throwaway projects** — rules are project-wide, so a test-channel lock could reject a prod user's edit on a shared real project. **WHO:** Marc → deploying P1 to test · Freddy → building the lock-matrix run sheet + routing deploy/prod gates to Jon · Coach → P1 done (C140), re-reviews P2/P3/P4 as they land · Jon → approved test deploy, will drive the matrix. **Phase B (PR #5)** still shelved+retained as backstop. F009/F010 parked in Features backlog. **Housekeeping:** COACH.md ~1565 lines (>1500 budget) — review-pass flagged for close-out. Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 20:03 MDT]** **🔨 MILESTONE — B012 HARD ONE-EDITOR LOCK: Coach C139 v2 plan Jon-APPROVED (ship-P1-first); Marc BUILDING P1.** **P1** = core lock + read-only mode + 3-state open-time modal → **delivers the B012 data-loss guarantee on its own.** Follow-on gated phases: P2 request/grant · P3 force-takeover+warning · P4 priority-hold+admin. **Lock model:** held-while-open (no idle release); ~90s heartbeat = crash detection; 30-min inactivity = force-eligibility; first-accessor edits, others view-only; ownership (Salesman) permanent+separate; request→grant hand-off; force after 30-min + warning/grace; "hold priority while away" blocks takeovers (admin override); crash+hold frees after ~90s. **Phase B (PR #5)** still shelved+retained as backstop. **WHO:** Marc → building P1 · Coach → on deck for P1 rules+client review at code-complete · Freddy → routing (P1 diff → Coach, then deploy decision → Jon) · Jon → approved, awaiting P1 review/deploy gate. **NEXT GATE:** Marc P1 code-complete → Coach review → Jon deploy sign-off (TEST channel first, client+rules together) → P1 ships → RELAX manual one-editor containment. **📥 Intake triaged by Freddy:** F009 (Debug Mode) + **F010 (Suggested alternates)** promoted to Features backlog + parked; INBOX now 1 (Login "Continue with Microsoft" BUG pending B-number). Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 19:12 MDT]** **🔁 STRATEGY PIVOT (Jon) — B012 fix is now a HARD ONE-EDITOR LOCK, not the Phase B merge.** New approach: a **server-enforced editing lease** on the project doc → true hard one-editor lock. **Why:** current guardrails don't actually hard-block concurrent BOM edits (Owner Priority Mode allows row edits; a hard lock today needs manual `ownerLockActive`) — the exact gap that let B012 clobber PRJ402096. **Phase B (PR #5) SHELVED** — retained as a possible backstop, NOT merged/deployed. **Live matrix + watcher STOOD DOWN** (watcher complete, read-only, parked). Brief written: `docs/B012-HARD-LOCK-BRIEF.md`. **WHO:** Coach → investigating lock stack + Detailed Plan (+C-finding) · Freddy → Brief done, will take 5 open rulings to Jon after Coach's feasibility pass · Marc → stood down, standing by to build after plan+approval · Jon → chose strategy, awaiting rulings · Dez → board+intake. **NEXT GATE:** Coach Detailed Plan → Jon rulings (TTL / same-user-two-tabs / granularity / read-only scope / takeover) → Jon approve → Marc build (H-item pipeline) → Coach review → Jon sign-off. **CONTAINMENT: manual one-editor policy HOLDS until the hard lock ships** (then it becomes automatic); Jon holding sales off ARC. **Intake:** Debug Mode stamped **F009** (parked behind the hard lock); Login-button BUG still pending triage. Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 19:02 MDT]** **🛠️ PHASE B MATRIX — IN PREP (correction: NOT running; the 17:08 "running" note was premature).** Prep hit a blocker + pivoted: **ARC has NO Save button — every BOM change auto-persists**, so the old "add-then-save" test coordination didn't map. Marc delivered the authoritative save/sync model → `docs/PHASE-B-SAVE-SYNC-MODEL.md` (@`11326170`). **Freddy** is rebuilding the run sheets for the auto-save reality (concurrency via timing + wait-for-propagation; **T3 = "the wait IS the test"**; **T7** needs timing-race or DevTools Offline; no user-held unsaved state). **Marc** building a read-only ground-truth **WATCHER** (Jon's instrument-first approach — one watcher on the shared project doc sees both users' writes). **Jon** creating BC-sandbox test items (J1/J2/DEL-J/J4/J-UNSAVED · A1/A3/DEL-A/A7[priced]/A9 · S1–S3) and will drive. **NEXT GATE:** watcher + rebuilt sheets → Jon+Andrew run on matrix-arc-test → results → Coach C138 PR re-review → Jon prod sign-off → merge PR #5 + deploy → relax containment. **⛔ Containment holds; PR #5 held (no auto-merge).** **📥 Intake now 2 pending:** BUG (Login "Continue with Microsoft" misleads existing users) + FEAT (Debug Mode — persistent cross-user activity tracing, extends debug-logging, parked behind Phase B) — both awaiting Freddy F/B-number. Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 17:08 MDT]** **🧪 PHASE B LIVE MATRIX RUNNING (Jon-driven).** The B012 fix validation is now in flight: Jon is running the **2-session live matrix** (**P-AB + P-SOLO**, load-bearing **T3/T4/T7/T9**) on **matrix-arc-test** with a 2nd edit-role user. **WHO:** Jon → driving matrix · Freddy → holding for results to route to Coach · Coach → on deck for **C138 PR re-review** once matrix passes · Marc → standby · Dez → board+intake live. **NEXT GATE:** matrix results → Coach C138 PR re-review → Jon prod sign-off → merge **PR #5** + deploy → relax the one-editor containment. **⛔ Containment still LIVE; do NOT auto-merge PR #5.** Prod = v1.23.3, master `db974e86`.
- **[2026-07-09 16:54 MDT]** **🟢 SESSION START (2026-07-09) — team up + comms-checked; board refreshed.** Prod **v1.23.3** (no deploy), live master **`db974e86`** (== origin/master, tree clean; 2 doc-only commits landed since the 07-08 close-out — `2dd2e468` triage + `db974e86` tip-correction). **Team booted + comms-checked** over the bus: Freddy (hub) · Marc · Coach · Dez — all baseline-matched (v1.23.3 / `db974e86`). **TOP OF QUEUE = SHIP PHASE B** (B012 concurrent-edit BOM data-loss fix): BUILT + HELD on **PR #5** (`claude/phase-b-bom-merge` @ `bd9134a9`), 25/25 unit + Coach C138 PASS + on TEST. Remaining gate (in order): **live 2-session matrix** on matrix-arc-test (T3/T7/T9 + Coach's A2) → Coach C138 PR re-review → Jon prod sign-off → merge+deploy → relax containment. **⚠ Matrix is JON-DRIVEN** (2 distinct logins; Marc's tab can't reach the test host). **Do NOT auto-merge PR #5.** **⛔ CONTAINMENT LIVE:** one editor/project, ALL projects, until Phase B ships; Jon holding sales off ARC. Deferred under B012: B013 (chronic BC)/B014/B015/B016/B017/B018 + #182; also queued #192, G005 §10-8 smoke, F004/F005/F007/F008, B008/B011/B005, G005 Phase 2. **📥 Intake: 0 pending** (INBOX empty). **Next Jon touchpoint:** run the Phase B live matrix now, or take a different item first. **WHO:** awaiting Jon's first work instruction — no role building yet. Prod = v1.23.3.
- **[2026-07-08 19:08 MDT]** **🌙 SESSION END — B012 CRITICAL fix BUILT + HELD on PR #5 (not deployed).** Prod = **v1.23.3** (no deploy this session), master **`7aa55219`**, handoff files updated (Freddy close-out). **B012** (concurrent-edit BOM data loss — full-doc save, no row merge; **item 534013 PROVEN clobbered**): Phase B row-level merge + Phase A stale-save-on-open removal built as ONE PR (#5), Coach's 3 defaults (preserve-null / adds-only M3 / append-end). **Next session's FIRST task = the live 2-session test matrix on the test channel (★ T3 delete-safety + T9 metadata load-bearing) → Jon prod sign-off → deploy.** **⛔ CONTAINMENT still in force (carries over): 1 editor/project, ALL projects, until Phase B ships; Jon holding sales off ARC.** **B-numbers stamped this session (Freddy, TODO.md, deferred under B012):** B012 (CRITICAL), B013 (ESCALATED → chronic BC connection), B014 (Codale parse-gap), B015 (lock reactivity), B016 (RE-SCOPED → mutation write-race), B017/B018 (new); + #182 re-push remediation. **Recovery DE-SCOPED** (Jon self-re-adds; PRJ402096 snapshot 155 rows; minor dup cleanup). **Intake:** 1 GEN still pending (`a1932435` TEST-upload cleanup); today's 2 bug captures promoted → B013/B014. DEFERRED: G005 §10-8, #192, roadmap. Prod = v1.23.3.
- **[2026-07-08 17:52 MDT]** **✅ B012 Phase B APPROVED + BUILDING (Phase A folded in, one PR).** Jon approved **Phase B — row-level BOM merge-on-save** (the real B012 fix) with Coach's 3 defaults: **bias-to-preserve null / adds-only M3 / append-end**; Phase A (remove stale save-on-open @37471) folded into the same branch → one PR, no separate deploy. **Marc BUILDING now.** Next: Coach diff-review → test-channel **2-session matrix** (★ T3 delete-safety + T9 metadata load-bearing) → Jon prod sign-off. **⛔ CONTAINMENT STILL IN FORCE:** 1 editor/project, ALL projects, until Phase B ships. **RECOVERY DE-SCOPED (Jon):** re-adds the few missing parts himself; PRJ402096 current state fine (snapshot 155 rows); minor panel-1 BUYOFF/CRATE dup cleanup when convenient — no reconcile task. **B-numbers stamped this session (Freddy, in TODO.md, deferred under B012):** B013 (BC-401 UX/resilience), B014 (Codale multi-result parse-gap), B015 (lock reactivity); + #182 stale-LT re-push remediation. **WHO:** Marc → Phase A+B build · Coach → diff-review then 2-session matrix · Freddy → coordinate + triage · Jon → next = test matrix → prod sign-off. DEFERRED: G005 §10-8, #192, roadmap. Prod = v1.23.3.
- **[2026-07-08 17:07 MDT]** **🚨 CRITICAL MILESTONE — concurrent-edit BOM DATA LOSS root-caused + staged fix approved.** Root = wholesale full-document BOM save with **NO row-level merge** (Coach C137 + Marc runtime confirm); affects **ANY project edited by 2 users concurrently**, not just PRJ402096. 3 mechanisms: M1 save-on-open of stale cache · M2 overwrite-on-save · M3 soft-apply-replace — amplified by burst full-doc saves overflowing the Firestore client write queue (resource-exhausted). **⛔ CONTAINMENT (platform-wide, effective NOW): ONE editor per project, ALL projects, until Phase B ships** — concurrent editing destroys rows. **Fix APPROVED (Jon), staged:** Phase A = remove stale save-on-open (@37471, ~5 lines) → BUILDING (Marc) → Coach review → Jon deploy checkpoint (TEST CHANNEL first); Phase B = row-level merge-on-save + delete-safe baseline-id + soft-apply merge → plan drafting (Coach, `docs/BOM-MERGE-PHASE-B-PLAN.md`). **Data:** current state PRESERVED → `docs/PRJ402096-BOM-SNAPSHOT-2026-07-08T2259Z.md` (155 rows); prior lost states GONE (no Firestore history); panel-1 duplicate BUYOFF/CRATE cleanup pending; recovery = reconcile + re-add via app AFTER fix ships. **Surfaced today, pending Freddy triage into TODO (deferred under the data-loss fix):** BC-401 UX/resilience bug (was PRJ402096 Issue 1); Codale multi-result parse-gap (was Issue 2); C136 lock-reactivity gaps (activeExtractions no-TTL / dead per-panel code / orphan docs); #182 stale-LT re-push remediation. **WHO:** Marc → Phase A build · Coach → Phase A review + Phase B plan · Freddy → coordinate + B-triage · Jon → next = Phase A deploy checkpoint (test channel). DEFERRED: G005 §10-8, #192, roadmap. Prod = v1.23.3.
- **[2026-07-08 13:52 MDT]** **🟢 MILESTONE — both PRJ402096 issues ROOT-CAUSED.** **Issue 1** (61-item "BC Sync Incomplete") = **TRANSIENT BC OAuth token expiry mid-push** (401 storm ~17:47Z, auto-recovered — auth healthy now). NOT G005, NOT unmatched items. Real bug = no token auto-refresh / no retry-on-401 + misleading UX (stale "BC Connected" green while 401ing; "use Item Browser" modal wrong for a 401; no retry affordance) → **new B pending Freddy triage.** **Stuck item 3044636 (60D lead time) = #182** (ItemVendorCatalog PATCH compound-key bug): row created 60D 05-12, 10D (05-13)+21D (05-18) updates FAILED on old key → BC stuck 60D, re-price clobbers manual 21D. **#182 fix IS LIVE in v1.23.3** but item not pushed since 05-18 (pre-fix). NOT #188 (vendor V00373 valid). **Issue 2** (5 Codale rows no pricing) **BLOCKED on Jon** — needs the 5 exact PNs + force-refresh state; live evidence says those rows are BC-priced (scraper skips when BC has a price) → likely not a scraper parse fail. **Confirmed (Jon):** prod ARC ↔ BC sandbox (`MATR_SndBx_01152026`) intentional pre-launch. **Follow-up to scope:** items written before the #182 fix + never re-pushed may be silently stuck at stale LTs at scale → possible one-time "re-push all lead times" remediation. **WHO:** Jon → live-push 3044636→21D (proves #182 heals) · Marc → verify it lands · Coach → holding, fix-scoping next · Freddy → coordinate + B-triage. DEFERRED: G005 §10-8 smoke, #192. Prod = v1.23.3.
- **[2026-07-08 12:05 MDT]** **🔴 SESSION START (2026-07-08) → PRJ402096 prod investigation is the ACTIVE thread (preempts queue).** Team re-booted (prod v1.23.3, master `c8a0a6bd` at boot). Jon reported 2 prod bugs on **PRJ402096 (a REAL customer project)** → both captured to INBOX (`5269a92c`), Freddy to stamp B-numbers at triage. **Issue 1:** "Push Lead Times to BC" failed — "61 items could not be pushed to BC" after manual edits cleared red; **G005-refactor regression suspected** (all 14 BC writes rerouted through `bcGatedFetch` last cycle); adjacent to #188 (stale bcVendorNo, different case). **Issue 2:** 5 Codale-sourced rows show no price/LT though data exists on Codale's site (Jon spot-checked) → scraper health in question. **WHO:** Marc → live Debug Logs + console (Issue 1) + Codale fn logs (Issue 2), read-only no re-push · Coach → code-trace BC LT-push path + Codale scraper path · Freddy → coordinate + reconcile + B-number triage · Jon → next = findings review (HOLD before any fix). **DEFERRED (preempted):** G005 §10-8 prod smoke, #192 auto-revert build. **Intake:** 1 GEN still pending Freddy triage (`a1932435` TEST-upload cleanup). Prod = v1.23.3.
- **[2026-07-07 21:38 MDT]** **🌙 SESSION END — ✅ G005 Phase 1 SHIPPED v1.23.3 (`d373a510`).** Test-env firewall live: `IS_TEST_ENV` gates BC→sandbox / email suppressed / test company / TEST-MODE banner (14 BC write sites); Functions + client both deployed. **Money-path / ERP-write harm PROVEN CLOSED.** DEFERRED to next session / Phase 2: (1) §10-8 prod-regression smoke (FIRST task next session); (2) mutating-tail live demo (blocked — needs a dedicated test USER, 1-company-per-user); (3) Phase 2 = separate Firebase project + test user (real data-collision fix). **Session shipped 9 releases** (F002→G005 Phase 1) + legacy backlog categorized/reconciled + B009 closed. **1 intake carries over** (GEN TEST-upload cleanup, `a1932435`). Team STANDING DOWN; resume from SESSION-STATE.md. Prod = **v1.23.3**.
- **[2026-07-07 21:19 MDT]** **📥 New intake captured (1 pending).** `GEN — "Remove leftover TEST upload section from Upload Supplier Quote modal"` (source: Jon, INBOX `a1932435`) — the modal shows a leftover orange "TEST | Test Upload to BC (PRJ402096) → Crum Electric Supply · 2760726-00" bar (old test scaffolding); remove it. Note: it hardcodes PRJ402096 (a real customer project) → ties to the G005 test-data-hygiene theme. Dedup-checked: no existing item. Holding for Freddy triage. No change to G005 (server half building).
- **[2026-07-07 20:46 MDT]** **G005 CLIENT half Coach-APPROVED (`bda1c9c7`); SERVER half building · 🤖 subagent tagging legacy backlog.** G005 Phase 1: client-side gates Coach-approved; Marc now building the server Functions half; Coach standing by for the server review → replay-verify → Jon deploy (hosting + functions). **🤖 NEW subagent (Freddy-spawned, Jon-greenlit):** categorizing all ~101 OPEN legacy `#N` items into the B/F/G taxonomy — TAG-only, keeps `#N` (no renumber) → proposal table → Freddy reviews + applies. Dez NOT pulled on (stays on the board); logged per the subagent transparency guardrail. Prod v1.23.2, master `64a471ca`. WHO: Marc → G005 server · Coach → server review on-deck · Freddy → hub (+ subagent output review) · Jon → G005 deploy checkpoint next.
- **[2026-07-07 20:39 MDT]** **🔎 Legacy-reconciliation ACTIONED by Freddy (`337f7886`).** #190 + #195 FLIPPED to RESOLVED (v1.23.2). The 5 code-confirms (#73/#65/#84/#71/#58) recorded as a tracker callout — wait for a G005 break (Coach+Marc heads-down on the build). Relevance/staleness pass noted as the deferred bigger lever → teed up post-G005 (Freddy + Coach/Marc). Closes the Jon-directed legacy-review task. No deploys since v1.23.2.
- **[2026-07-07 20:34 MDT]** **🔨 G005 scope grew 2→14 BC sites · 📋 full backlog scoped into Briefs · #192 ship-now approved (#2).** Marc still building G005 Phase 1 — the plan under-counted BC write sites (2→14, incl. money-path); now gating exhaustively. Freddy (while waiting) scoped the ENTIRE backlog into build-ready Briefs: Features F004/F005/F007/F008 (`docs/F004-F005-F007-F008-CLUSTER-BRIEF.md`), MED features F006/#197/#198 (`docs/F006-197-198-SCOPING-BRIEF.md`), bugs B008/B011/B005 (`docs/B008-B011-B005-BUGS-SCOPING-BRIEF.md`), + firmed the #192 top-regression fix (`docs/192-FREDDY-FIX-BRIEF.md`). **Jon: #192 SHIP-NOW approved → slots #2 right after G005; 9-item roadmap sequenced behind G005.** Prod v1.23.2, master `b869f6fd` (nothing new deployed since the batch). Dez legacy-recon screen complete (no silent fixes; 2-flip/5-confirm shortlist ready for Freddy). WHO: Marc → G005 build · Coach → on-deck · Freddy → hub · Jon → G005 deploy checkpoint next.
- **[2026-07-07 20:10 MDT]** **✅ Quick-win BATCH SHIPPED v1.23.2 · 🔨 G005 Phase 1 firewall now BUILDING.** All 6 (G006/#190/#195/B002/B001/B003) live on prod (release `9d89f26b`), Coach-approved, tracker RESOLVED. **G005 Phase 1 (Test-Env Isolation firewall)** now building (Marc): Step 1 = verify BC sandbox liveness, then `IS_TEST_ENV` gates (BC→sandbox+belt / email suppressed / test company / TEST-MODE banner) + Functions threading. MEDIUM build → Coach review → near-miss-replay verify → Jon deploy (**hosting + functions**). Prod now **v1.23.2, master `4c20cdb4`**. WHO: Marc → G005 Phase 1 build · Coach → on-deck (review + replay verify) · Freddy → hub · Jon → next = G005 deploy checkpoint.
- **[2026-07-07 19:48 MDT]** **🔨 Quick-win BATCH → Marc BUILDING (sequenced FIRST) · G005 Phase 1 plan APPROVED + queued next.** Freddy sequenced the 6-item batch (G006/#190/#195/B002/B001/B003) FIRST → Marc building as one patch → Coach diff-review → one deploy. B003 rescoped per Jon (RFQ Apply modal bottom not-quoted list). B011 + B005 excluded. **G005 Phase 1** plan APPROVED (Coach plan + Freddy review + Jon's 5 rulings all done) → QUEUED to build right after the batch. Prod stable **v1.23.1, master `549a530a`**. WHO: Marc → batch build · Coach → on-deck (batch review, then G005) · Freddy → hub · Jon → next = batch deploy checkpoint.
- **[2026-07-07 19:42 MDT]** **🧺 Jon APPROVED a 6-item quick-win BATCH → one bundled patch.** **G006** (portal loading copy) + **#190** ("Save Defaults"→"Save" relabel) + **#195** (Print-as-Firm false BUDGETARY checklist entry) + **B002** (approved-state TR block message) + **B001** (auth redirect trailing-dot strip) + **B003** — bundle, one Coach diff-review, one deploy. **B003 scope CLARIFIED by Jon:** it's the **Supplier RFQ "Apply" modal** — a box at the BOTTOM lists parts that were NOT quoted (they're from other suppliers) → just noise; remove/hide that list. (Original B003 wording said "Review Supplier Quote modal" — Freddy to update the tracker entry to the Apply modal.) Routed to Freddy to scope + sequence against active G005. WHO: Coach → G005 Phase 1 plan (in flight); batch queued behind/alongside per Freddy's routing.
- **[2026-07-07 19:33 MDT]** **🔨 G005 Test-Env Isolation → ACTIVE WORK (phased, Jon-approved).** Jon approved the **PHASED strategy: Phase 1 = `IS_TEST_ENV` side-effect firewall** (BC no-op / emails suppressed / TEST-MODE banner) + dedicated **test company**; **Phase 2** (separate Firebase project) later. Coach scoping Phase 1 feasibility + build plan (`docs/G005-COACH-BUILD-PLAN.md`) → Freddy Analyst-Review → Jon approve → Marc build. Brief: `docs/G005-TEST-ENV-ISOLATION-BRIEF.md`. B009 fully closed. Prod stable **v1.23.1, master `92d39150`**. Jon-owned tail: delete today-dated synthetic BC entries (tomorrow). WHO: Coach → G005 Phase 1 plan · Marc → standby · Freddy → hub · Jon → next = approve Phase 1 plan.
- **[2026-07-07 19:20 MDT]** **🅿️ B009 FULLY CLOSED · ⚠ G005 ESCALATED to URGENT.** B009: parked (not active — awaited-save prevents the race) + belt fix plan **BANKED** (ready if it recurs) + cleanup **COMPLETE/verified** (11 crossed rows on PRJ402096 restored, no data lost). **⚠ G005 escalated:** the cleanup revealed **PRJ402096 is a REAL CUSTOMER project** → the shared test/prod Firestore is a **LIVE hazard**, not pre-launch-theoretical (test mutations hit real customer data). Now urgent infra; flagged to Jon. **Prod stable v1.23.1, master `a1572caf`; all work threads CLEAR, team on STANDBY** awaiting Jon's next pick. 2 low-pri residuals on Jon's plate: row `3516000` `leadTimeSource` label; BC vendor spot-check. WHO: Marc → standby · Coach → standby · Freddy → hub · Jon → steering (G005 decision + next pick).
- **[2026-07-07 18:57 MDT]** **🅿️ B009 PARKED (Jon: "it's good") — all threads now clear.** Conclusively NOT reproducible in v1.23.1 (Marc's 4 scenarios incl. 3-sequential all clean). Root: the **awaited `safeSave` (B004 fix)** already prevents the hypothesized stale-input race → **NO fix; defensive belt HALTED** (would guard a race the code already prevents). Instrumentation kept on TEST for opportunistic capture; Marc sweeping the synthetic repro-data on PRJ402096. **Prod = v1.23.1, stable, nothing in flight.** Backlog: Features F004–F008; Bugs B003/B005/B008/B011; General G006; carry-forwards #192/#200/#193/#197/#198; 3 F001 forward-items. WHO: Marc → sweep then standby · Coach → standby · Freddy → hub · Jon → steering next move.
- **[2026-07-07 18:54 MDT]** **🔴 B009 = DECISIVE NEGATIVE → shipping a defensive belt instead.** Marc+Jon couldn't reproduce in v1.23.1 — 3 scenarios clean, no clobbering writer fires, Jon can't manually repro his original. Followed "prove active before fixing" → **no targeted fix.** Jon chose **(B): ship Coach's writer-AGNOSTIC defensive TR-preserve BELT as prevention** (guards the send-gate field vs any future stale writer) + keep instrumentation on TEST for opportunistic capture. → Coach writing the belt fix plan (careful: must NOT block legit TR changes) → Analyst Review → Jon approve → build → deploy. **🧹 Loose end (G005-adjacent):** synthetic B009-repro data on PRJ402096 (several imported submissions + crossed rows `3516000`/`1032264CS`/+6) — cleanable. Prod = v1.23.1. WHO: Coach → belt fix plan · Marc → holding (build next) · Jon → steering · Freddy → hub.
- **[2026-07-07 16:38 MDT]** **✅ B010 SHIPPED to prod v1.23.1, RESOLVED (release `6e82ea5e`; prod-verified fix present + probe-free).** Manual-upload crash cleared. **🔴 B009 now the ACTIVE thread:** test re-armed w/ probes (`window.__B009=1`); Jon picking the repro path (fresh RFQ→portal→submit-substitute→Apply, OR reuse an existing submitted-portal-quote-with-variance) + co-driving a portal submit → the log names the clobbering writer → Coach's pre-staged fix (input-freshness + save-time preserve belt). Prod = v1.23.1. WHO: Marc → ready for B009 repro co-drive · Coach → holding for writer-ID→fix plan · Jon → steering B009 · Freddy → hub.
- **[2026-07-07 16:29 MDT]** **🚀 B010 → deploying v1.23.1 (Coach-approved, Jon-released).** Coach APPROVED the fix (data-safe) → Jon RELEASED → Marc deploying to prod as **v1.23.1** (patch). Guardrail: keep B009 test-probes OUT of the prod build. **G006** (Supplier Portal loading copy → "Matrix ARC AI is currently scanning your quote") triaged [Backlog · cosmetic] — INBOX clear. **B011** noted (`_nullifyUndefined` caveat: don't reuse on non-plain/Date payloads). **B009** repro NEXT after v1.23.1 — Marc re-arms the test build w/ probes; Jon picks path + co-drives a portal submit. Prod = v1.23.0 → v1.23.1. WHO: Marc → deploy then re-arm test · Coach → holding (B009 fix plan) · Jon → steering · Freddy → hub.
- **[2026-07-07 16:26 MDT]** **📥 New intake captured (1 pending).** `FEAT — "Supplier Portal 'Analyzing Your Quote' copy → 'Matrix ARC AI is currently scanning your quote'"` (source: Jon, INBOX `cfaf4cea`) — reword the portal quote-analysis loading state. Small copy change (Freddy may class GEN/cosmetic). Dedup-checked: no existing item. Holding for Freddy triage. No change to B010 (Coach review → v1.23.1 deploy) or B009 (repro next).
- **[2026-07-07 16:24 MDT]** **✅ B010 fix built + RE-TEST PASSED live → Coach review → v1.23.1 deploy.** Jon re-tested live (PRJ402096: drop → no crash → price-review UI; runtime artifact confirmed). Data-safe coerce fix committed clean (`b3f79473`, B009 probes stripped). → Coach diff-review (save-path sanity) → then Jon v1.23.1 deploy checkpoint. **B011** captured [Backlog] — harden other `supplierQuotes` undefined sites (Marc B010 follow-up). **B009** repro NEXT after B010 — Jon picks path + co-drives a real portal submit (test armed, `window.__B009=1`). Prod = v1.23.0. WHO: Coach → B010 diff · Marc → holding (B009 repro next) · Jon → steering · Freddy → hub.
- **[2026-07-07 16:02 MDT]** **🔨 B010 building (→ v1.23.1) · 🔴 B009 repro next.** (Jon back from ~3h lunch; team held correctly — both threads were Jon-gated.) **B010** — manual Upload-Supplier-Quote crash (undefined BC field → Firestore reject): Jon APPROVED → Marc BUILDING a data-safe coerce fix → v1.23.1 (fix → Jon re-test → Coach quick-review → Jon deploy). **B009** repro is NEXT after B010 — needs a REAL portal submission (manual path can't repro; auto-stamp is portal-only `@39085`); Jon picks the path + co-drives a portal submit; instrumentation on TEST (`window.__B009=1`). Prod = v1.23.0. WHO: Marc → B010 fix · Coach → holding (B010 diff, then B009 fix plan) · Jon → steering · Freddy → hub.
- **[2026-07-07 12:35 MDT]** **🎉 F001 SHIPPED to PROD v1.23.0, RESOLVED (release `2d29d5d1`, prod-verified, DEPLOY Pushover fired).** The full ARC **"clean up the BOM" arc is now shipped end-to-end:** F002 (v1.21.26) → F003 (v1.22.0) → checkbox/B006/B007 (v1.22.1–3) → F001 (v1.23.0). 3 non-blocking F001 forward-items captured in TODO.md (focus-trap, checkpoint hardening, finer anchors). **B009:** Marc PIVOTING NOW to the live repro (prove active + pin the exact clobbering writer via Coach's instrumentation + Debug Logs) → then Coach's targeted fix (input-freshness + preserve belt). **Intake:** F008 (new-version refresh notification) + F007 triaged to Features [Backlog]; INBOX clear. Prod = v1.23.0. WHO: Marc → B009 repro · Coach → holding for writer-ID→fix plan · Freddy → hub · Jon → steering.
- **[2026-07-07 12:34 MDT]** **✅ F001 Coach-APPROVED + Jon RELEASED → 🚀 Marc DEPLOYING v1.23.0.** Coach final full-diff review = APPROVE (both Jon rulings structurally enforced; backward-compat airtight). Jon released the deploy → Marc deploying to prod as **v1.23.0** (minor — new feature). 3 non-blocking forward-items noted (focus-trap a11y, checkpoint double-advance LOW-risk, deferred sub-field anchors). After deploy → Marc pivots to the **B009 live repro** (fix pre-staged: input-freshness + save-time preserve belt). WHO: Marc → deploying F001 then B009 repro · Coach → done F001, holding for B009 fix plan · Jon → released deploy · Freddy → hub. Prod v1.22.3 → v1.23.0.
- **[2026-07-07 12:32 MDT]** **📥 New intake captured (1 pending).** `FEAT — "New-version-available notification prompting users to refresh (work-safe)"` (source: Jon, INBOX `d1ee8fc7`) — on a new site push, ARC polls the source (compare client `APP_VERSION` vs currently-served; deploy.sh already bumps it) and shows a notification on ALL users' pages to refresh + applies updates, reassuring that work is saved / nothing lost. Dedup-checked: no existing item. Holding for Freddy triage. No change to F001 (Coach final review → Jon deploy) or B009 (fix pre-staged).
- **[2026-07-07 11:51 MDT]** **✅ F001 VERIFY COMPLETE → Coach final review → Jon deploy checkpoint (v1.23.0).** ALL step-types PASS live or code-verified — A3 gated flow, **narrated sends never-auto-fire (STRUCTURAL)**, checkpoints, a11y, and **#2 state-driven resume PASSED live** (self-healing: resumed Step 2 not the saved Step 5). Jon created PRJ402134 through the tour. 5 real UI issues caught + fixed live (spotlight timing, modal masking, modal-grow, 2 bubble-clips). Master tip `f3154196`, all on test. → now with Coach for final H-item review → then Jon deploy checkpoint (new feature → **v1.23.0**). B009 fix pre-staged (ref-lag; input-freshness + save-time preserve belt); Marc's live repro queued next after F001. WHO: Coach → F001 final review · Marc → holding (then B009 repro) · Jon → next = F001 deploy checkpoint · Freddy → hub. Prod = v1.22.3 (held).
- **[2026-07-07 10:51 MDT]** **🎯 F001 #1 gated flow FULLY PASSES live.** The tour drove Steps 1→1b→2→3; **Jon created PRJ402134 THROUGH the walkthrough**, auto-advanced to the Step-3 extraction checkpoint. 4 real issues caught + fixed live (A3 spotlight `useLayoutEffect`, Step-1b modal spotlight, ResizeObserver modal-grow, bubble on-screen clamp — `b834660d`/`9f52b642`/`0862d00d`/`18d64f6c`/`7cd91f3a`, all on test). Now at Step 3; remaining verify: checkpoint resume, narrated sends (4Ba/7), a11y. **B009:** fix pre-staged (ref-lag stale-input; input-freshness + save-time preserve belt), gated on Marc's live repro (queued after F001 verify). WHO: Marc+Jon → F001 verify (Step 3) · Coach → holding for B009 fix plan · Freddy → hub. Prod = v1.22.3 (held).
- **[2026-07-07 10:32 MDT]** **🔴 B009 CODE-TRACED (Coach Analyst PASS, `docs/B009-COACH-TRACE.md`) + F001 A3 PASSED.** B009 mechanism: a background REPRICE ~seconds post-apply re-saves the BOM off non-spread rows → drops the TR flag (`techReviewFlag` not preserved by pricing code; crossed rows left unpriced bait the auto-reprice). **B004 + F003 hypotheses REFUTED with proof** (RED-not-YELLOW = genuine clear). Fix direction: preserve the 5 TR fields in the reprice writer (metadata rule). Awaiting Marc live repro (after F001) to confirm ACTIVE + pin the exact writer. **F001 verify:** A3 gated click-through **PASSED** ✅; new bug caught — New-Project modal blocked by the tour backdrop → Marc fixing now, then checkpoint-resume / narrated-sends / a11y. WHO: Coach → done tracing (standby for fix plan) · Marc → F001 modal-block fix then B009 repro · Jon → co-driving F001 · Freddy → hub. Prod = v1.22.3 (held).
- **[2026-07-07 10:20 MDT]** **🔴 B009 opened (potentially HIGH) + F007 triaged.** **B009** — Jon LIVE-observed on prod v1.22.3: after uploading 4 supplier quotes, the auto-stamped TR checkmarks on supplier-crossed rows CLEARED after a few seconds (reload/reprice race); rows stayed red → **undermines the TR send-gate** (F003/#199). Echoes **B004**. → Coach TRACING (source-level, parallel); Marc runs live repro + Debug Logs after the F001 verify. **F007** (order main-page projects by last-accessed) triaged to Features [Backlog] by Freddy (distinct from #200); INBOX clear (0 pending). F001 still in live-verify (A3 click-through, Marc mid-work). WHO: Coach → B009 trace · Marc → F001 verify then B009 repro · Jon → co-driving F001 + reported B009 · Freddy → hub. Prod = v1.22.3.
- **[2026-07-07 10:08 MDT]** **📥 New intake captured (1 pending).** `FEAT — "Order main-page projects by last-accessed (bump just-exited project to top)"` (source: Jon, INBOX `613d72a2`) — on exiting a project, move it to the top of the main-page list; order the whole list by last-accessed (most-recent first). Needs a per-project last-accessed timestamp (additive/data-safe) + dashboard sort. Dedup-checked: distinct from #200 (tile COLOR by quota-age, not order) and the archive-view `archivedAt` sort. Holding for Freddy triage. No change to F001 (live-verify in progress).
- **[2026-07-07 09:59 MDT]** **✅ F001 CODE-COMPLETE (headless) → 🔍 live-verify starting (Marc+Jon).** Marc drove F001 to code-complete while Jon was away — Tracks A/A5/A6/B/C committed `0c0dba74` (`docs/F001-BUILD-RESULTS.md`). Prod UNTOUCHED (v1.22.3); backward-compat verified (existing Full/Sales tours unchanged). Jon back → live-verify now (controlled tab). #1 watch: **A3 gated click-through** — Marc flagged a headless rect-null anomaly (likely headless-launch timing; needs Jon's real-use confirm; fix candidate ready if real). Deviations: Step 2 narrated (no pre-quote modal exists); New-Project sub-field anchors deferred (Step 1 button-level v1). WHO: Marc+Jon co-drive · Coach standby (review after) · Freddy hub. Prod HELD.
- **[2026-07-07 10:02 MDT]** **📥 Intake triaged → B008 [Bugs · Backlog], INBOX clear.** Freddy reclassified the RFQ-History-portal-link capture FEAT→Bug and promoted it to **B008** (`08dfaf99`) — link opens the pre-submission (blank/email) portal, should open the SUBMITTED portal so the user sees the supplier's entered pricing + lead times. Intake now 0 pending. ⚠️→✅ Flagged the B008 tracker entry had stray B006/B007 debris appended (copy-paste artifact); Freddy fixed + pushed (`93359dff`) — B008 scope now clean, B006/B007 descriptions restored to their own lines.
- **[2026-07-07 09:58 MDT]** **📥 New intake captured (1 pending).** `FEAT — "RFQ History 'Supplier Portal' link should open the SUBMITTED portal (post-fill)"` (source: Jon, INBOX `739ca538`) — in RFQ History → View Received Quotes, each quote's "Supplier Portal" link currently opens the portal BEFORE data was entered (blank/email state); it should open the portal AFTER submission so the user can see the pricing + lead times the supplier actually entered. Bug-adjacent (link points at the wrong state) — Freddy may reclassify B/F at triage. Note: Jon said "customer" — in this domain the RFQ recipient who fills the portal is the SUPPLIER. Dedup-checked: distinct from B003 (a different modal — Review Supplier Quote decluttering). Holding for Freddy triage. No change to F001 (still building).
- **[2026-07-06 17:43 MDT]** **✅ F001 build Plan = Analyst-PASS + Jon APPROVED → 🔨 Marc BUILDING.** Extends the existing `TourOverlay` engine (not new build): **Track A** (engine — gated/checkpoint step-types, click-through spotlight, state-driven resume) ∥ **Track B** (~15–19 `data-tour` anchors) → **Track C** (7-step array; Send-RFQs + Send-Quote NARRATED per Jon). Pipeline: validate_jsx → live-verify (Marc+Jon) → Coach review → Jon deploy. Prod = v1.22.3; no open bugs; backlog F004/F005/F006. WHO: Marc → building · Coach → standby (review after) · Freddy → hub · Jon → next = live-verify with Marc.
- **[2026-07-06 17:01 MDT]** **✅ F001 engine Supplement = Analyst Review PASS → Coach writing build Plan.** HEADLINE: a tour engine **already exists** (`TourOverlay`, v1.19.667) → F001 **EXTENDS** it rather than building new (big de-risk). Jon rulings: run on the user's **REAL project** (no sandbox); the **Send-RFQs + Send-Quote steps are NARRATED, not gated** — so a guided run can never misfire a real supplier/customer email. → now with **Coach writing the build Plan** → Freddy Analyst Review → Jon approves Plan → Marc builds. Prod = v1.22.3; no open bugs; backlog F004/F005/F006; G002/G004 closed. WHO: Coach → Plan · Marc → standby · Freddy → hub · Jon → next = approve F001 build Plan.
- **[2026-07-06 16:12 MDT]** **🔨 F001 STARTED (Interactive quote-building walkthrough).** Jon delivered the walkthrough content (ARC Training.docx → `docs/F001-WALKTHROUGH-STEPS.md`, full 7-step quote lifecycle) + ruled the design: **ONE re-entrant walkthrough with mixed step-types** — gated / narrated / checkpoint (for async-external waits). → now with **Coach for the engine Supplement** (feasibility + whole-app `data-tour` anchor inventory). Next: Coach Supplement → Freddy Analyst Review → Jon approves F001 Plan → Marc builds. Prod = v1.22.3; no open bugs; backlog F004/F005/F006. WHO: Coach → Supplement · Marc → standby · Freddy → hub · Jon → next = review Supplement / approve Plan.
- **[2026-07-06 16:10 MDT]** **🚫 Jon CLOSED G002 + G004 (won't-do).** **G002** (automate 4-session team startup boot) — **not moving forward** (Jon 2026-07-06); the launcher effort is dropped. **G004** (tool-permission allowlist for suppressible prompts) — **won't work** (Jon 2026-07-06); its only verification path was the G002 reboot anyway, now moot. → Routed to Freddy to mark both **CLOSED / WON'T-DO** in the TODO.md General tracker (Freddy owns TODO.md; Dez recorded the decision here). No code impact (both were dev-tooling/infra). Prod still v1.22.3.
- **[2026-07-06 16:06 MDT]** **✅ v1.22.3 SHIPPED to prod (release `9bb6388b`, master `572d0f8c`).** **B007 fix** — dropped the 0.5 unflagged-row opacity → TR checkbox now FULL-WHITE in both states (fill was always correct; opacity was the culprit). **B007 = RESOLVED.** Checkbox saga CLOSED: v1.22.1 checked-white → v1.22.2 both-states-faint + B006 → v1.22.3 full-white. Prod now = **v1.22.3**. ⚠️ Jon to hard-refresh (dim screenshots were v1.22.2 + cache). Backlog: F004/F005/F006; F001 🔓 unblocked/next-up. INBOX clear. **No open bugs.** WHO: Marc + Coach standby · Freddy hub · Jon deciding next (F001 / close out / backlog).
- **[2026-07-06 15:55 MDT]** **✅ v1.22.2 SHIPPED to prod (release `5cb5c392`, master `8bd75f38`).** Bundled: white TR checkbox (BOTH states; Jon kept faint-white-when-unflagged) + **B006 Scan Results dismiss-by-report** (option B). **B006 = RESOLVED.** v1.22.2 combined the two queued patches (v1.22.2/v1.22.3) into one ship. Prod now = **v1.22.2**. Backlog: F004/F005/F006 (features); F001 🔓 unblocked/next-up. INBOX clear. WHO: Marc standby (shipped) · Coach standby · Freddy hub · Jon deciding next (F001 / backlog / close out).
- **[2026-07-06 15:45 MDT]** **🔨 Live iteration — v1.22.2 (checkbox) + v1.22.3 (B006) building.** **B006** (Scan Results banner → Dismiss): Jon DECIDED **option B** = per-project dismiss-by-report (✕ hides this scan's banner; a new extraction resurfaces it). Locate finding: banner isn't empty, it's NOISY (benign auto-fixed info dominates). → Marc building. **White TR checkbox:** v1.22.1 covered the checked state; a follow-up for the UNCHECKED state (`accentColor` is checked-only) is queued → **v1.22.2**. Build order: Marc → checkbox-unchecked (v1.22.2) → B006 option B (v1.22.3). Coach caught up + standby (confirmed nothing held; verified F002 Rev1 shipped-in-v1.22.0). Prod = v1.22.1. F001 unblocked/next. WHO: Marc building · Coach standby · Freddy hub · Jon iterating live.
- **[2026-07-06 15:40 MDT]** **✅ v1.22.1 SHIPPED (cosmetic patch — white TR checkbox fill), prod-verified (release `1048d635`).** Prod now = **v1.22.1**. **B006 — Scan Results concern window → add Dismiss button** (source Jon) tracked to Bugs [Backlog]; **Marc in LOCATE phase.** F001 (walkthrough) still 🔓 unblocked / next-up feature. WHO: Marc → locating B006 · Coach → catching up/standby · Freddy → hub · Jon → eyeballing v1.22.1 + deciding next (F001 / B006 / other).
- **[2026-07-06 15:17 MDT]** **🎉 F003 SHIPPED to PROD v1.22.0, RESOLVED (release `2d24f5a2`).** Prod-verified via artifacts; DEPLOY Pushover fired. The full "clean up the BOM" arc is now DONE: **F002 shipped v1.21.26 + F003 shipped v1.22.0.** **F001** (interactive quote-building walkthrough) → 🔓 **UNBLOCKED** (both prereqs shipped), ready to start — targets the final BOM UI + `data-tour` anchors. **📥 Intake CLEAR (0 pending):** F004, F005, F006 (Qv.## Hist. button) all triaged to Features [Backlog]. WHO: Marc standby (deployed) · Coach standby · Freddy hub — awaiting Jon's next direction (start F001 / close out / other) · Jon deciding next move. Prod = v1.22.0.
- **[2026-07-06 15:13 MDT]** **📥 New intake captured (1 pending).** `FEAT — "Qv.## Hist. button — per-quote send history with document previews"` (source: Jon, INBOX `832e2cfa`) — a "Qv.## Hist." button next to Send/Resend/Print that opens all historical sends for the quote, each with a preview of the document actually sent. Dedup-checked: data feed exists (#193 writes `quote_send` → `project.qvHistory[]`); distinct from #194 (global email-metrics/click-tracing consumer) — this is a per-quote history VIEWER + stored sent-doc snapshot. Holding for Freddy triage. No change to F003 (still at Jon's deploy checkpoint, prod HELD).
- **[2026-07-06 15:02 MDT]** **✅ Coach APPROVED F003 for deploy → 🚩 Jon's deploy checkpoint.** Coach final review of the actual diff = **APPROVE FOR DEPLOY** (`docs/F003-COACH-REVIEW.md`). → now at **Jon's deploy checkpoint** — F003 to prod, **v1.22.0 minor**; Marc ready to run `deploy.sh`. Prod HELD until Jon releases. F001 unblocks on ship. **📥 Intake CLEAR (0 pending):** Freddy triaged both captures → **F004** (portal submit confirmation) + **F005** (Print-Only button in locked-quote overlay, sibling to #196), Features [Backlog]. WHO: Freddy hub (holding for release) · Marc standby (deploy-ready) · Coach done · Jon → deploy decision.
- **[2026-07-06 14:56 MDT]** **✅ F003 VERIFY COMPLETE + signed off → Coach final review.** Full T1–T11 + all 4 Rev-A items **PASS** (Marc live, `docs/F003-VERIFY-RESULTS.md`); Jon SIGNED OFF the brighter-yellow eyeball hue. → now with **Coach for final diff+results review** → then Jon's **deploy checkpoint (likely v1.22.0 minor)**. F001 unblocks once F003 ships. Prod HELD. WHO: Coach → reviewing F003 · Marc → standby (verify done) · Freddy → hub holding for Coach's verdict · Jon → next = deploy checkpoint.
- **[2026-07-06 14:48 MDT]** **🔧 F003 Rev-A BUILT + deployed to test (`dae43068`, `docs/F003-VERIFY-RESULTS.md`).** ✅ LIVE-PASSED: bold 2px engineer circle · "Status"→"Issues" header rename · sign-off FINAL/uncheckable (Jon's interpretation confirmed) · T1/T2/T4/T9/T10/T11; sweep-removed + label-removed confirmed in bundle. ⏳ Last 4 pending a clean-reset (both test flags resolved → no unresolved row): brighter-yellow eyeball (Jon tuning hue), resolve-click, approve-gate, send-gate-on-yellow. Marc cueing Jon through ~4 clicks (Reject → flag fresh row → Send-for-TR-assign-self → Marc ref-clicks circle). Prod HELD — Jon's checkpoint. WHO: Marc+Jon co-drive final pass · Coach standby (review after) · Freddy hub.
- **[2026-07-06 14:01 MDT]** **🔍 F003 verify PARTIAL → Marc+Jon co-driving live.** ✅ PASSED live (Sales/non-pending view): C6 no-label · role-gating/C7-fix (admin sees checkbox not circle when not pending) · Q2b checkbox-enabled; deployed test bundle confirmed carrying all F003 code. ⏳ BLOCKED on a live fixture — C8 yellow / engineer circle / resolve-stamp / approve-gate / send-gate are code-verified + deployed but need a real flagged row (row-8 flag didn't persist; **Marc correctly refused to hand-write it into shared prod Firestore — data-retention rule**). → Jon co-driving 2 real clicks (flag a fresh row + Send-for-Tech-Review) to unlock; Marc finishes the engineer-circle half headless. NOT deployed — prod = Jon's checkpoint. WHO: Marc+Jon co-drive · Coach standby · Freddy hub.
- **[2026-07-06 13:50 MDT]** **📥 New intake captured (1 pending).** `FEAT — "Print-Only button in locked-quote (Quote Sent) blocker overlay"` (source: Jon, INBOX `6643f578`) — after a quote is sent, the blocker covers Print/Send; add a "Print Only" button alongside "Verify with Project Owner & Enable Edits" so the user can print to PDF WITHOUT approval or unblocking (read-only output). Dedup-checked: distinct from #196 (overlay covers Receive PO — thematic sibling, different button) and #63 (archive gating). Holding for Freddy triage. No change to F003 (headless re-verify still running).
- **[2026-07-06 13:45 MDT]** **✅ F003 CODE-COMPLETE → 🔍 headless re-verify running.** Marc: 7 edit sites, +72/−38, `validate_jsx` clean; committing-to-master for durability + running HEADLESS re-verify on matrix-arc-test (Jon at lunch): engineer green-circle sign-off (button ref-clickable), C8 yellow, approve-gate, send-gate, role boundary, backward-compat on row 8. **Deferred to Jon's return:** the user-checkbox real click (React controlled input — needs a real click). **NOT deployed** — prod is Jon's checkpoint. WHO: Marc → headless verify · Coach → standby (review after) · Freddy → hub holding for results · Jon → at LUNCH, on return = 1 checkbox click + deploy checkpoint. F002 SHIPPED v1.21.26; F004 Backlog; F001 ⛔ blocked.
- **[2026-07-06 13:40 MDT]** **📥 Intake triaged → INBOX CLEAR (0 pending).** Freddy promoted the held capture to **F004 — "Portal submit confirmation shows ARC user + email-copy notice"** [Backlog] in the TODO.md Features tracker (`048863cc`); INBOX bullet cleared + triage-logged. F004 sits behind active F003 (not scheduled). Freddy also back-filled F001/F002/F003 into the Features tracker (were only in SESSION-STATE + docs). No change to who's-doing-what: Marc still BUILDING F003; Freddy hub holding for code-complete; Jon's next = F003 live re-verify.
- **[2026-07-06 12:30 MDT]** **✅ F003 build APPROVED (Jon) → 🔨 Marc BUILDING.** Marc coding the 7 edit sites per `docs/F003-COACH-BUILD-PLAN.md`. Pipeline: validate_jsx → live re-verify (Marc+Jon, matrix-arc-test) → Coach review → Jon deploy checkpoint. WHO: Marc → building · Coach → standby (review after build) · Freddy → hub, holding for code-complete · Jon → next = live re-verify with Marc. F002 still SHIPPED v1.21.26; F001 still ⛔ blocked by F003.
- **[2026-07-06 12:25 MDT]** **🏁 F003 milestone — build plan ready, awaiting Jon's build-approval.** Coach's BUILD PLAN complete (`docs/F003-COACH-BUILD-PLAN.md`, 7 edit sites); Freddy Analyst Review = **PASS** (the MEDIUM-risk sweep-removal checked SAFE). → **NOW AT JON'S BUILD-APPROVAL GATE** (before Marc codes). WHO: Coach → plan delivered, standby · Marc → standby (awaiting approval) · Freddy → hub, presented plan to Jon · Jon → DECISION PENDING (F003 build-approval). F002 still SHIPPED v1.21.26; F001 still ⛔ blocked by F003.
- **[2026-07-06 12:23 MDT]** **📊 Board back live — first status ping of the session (Freddy hub).** Prod now **v1.21.26** (master `be1124fd`). **F002** (BOM column reorg / indicator cleanup) **✅ SHIPPED v1.21.26, CLOSED** — decluttered columns (# Ref TR Qty Status 🔍 Part#), unified tri-state BC circle, AI+manual grey-italic pricing. **F003** (Role-Differentiated Tech Review) **ACTIVE** — design LOCKED (Jon ruled 8 Qs: 1a/2b/3b/4a/5a/6b/keep/none); **Coach writing the build plan** → Freddy Analyst Review → Jon build-approval → Marc builds. **F001** (interactive quote-building walkthrough) **⛔ BLOCKED by F003** (parked). WHO: Coach → F003 plan · Marc → standby · Freddy → hub holding for plan · Jon → next = approve F003 plan. Earlier this session: team re-linked after 3-day gap; **G001 re-confirmed** (Bypass Permissions does NOT suppress the `send_message` Allow-Once — hardcoded); **startup-process bug fixed** (primary session is now always Freddy, not Marc). **📥 Intake:** 1 new capture this session — `FEAT — "Portal submit confirmation shows ARC user + email-copy notice"` (source: Jon, `647370b1`), awaiting Freddy triage.
- **[2026-07-02 20:31 MDT]** **✅ #199 SHIPPED to PROD — SESSION END (close-out in progress).** Full live pass complete; deployed **v1.21.25** (master `333f385d`) bundling #199 + await-fix (`41824f6c`) + #17. INBOX cleared at close-out — all 4 items triaged/promoted to TODO.md: G005 (test-env shares prod Firestore), B003 (Review-Supplier-Quote lists unquoted parts), B004 (portal-Apply reload-race → RESOLVED `41824f6c` / shipped v1.21.25), B005 (resolved-TR-row can't re-arm, LOW/tuning). G002 team-boot launcher deferred to a clean boot (Ctrl+N mechanic confirmed). Dez board + intake clear.
- **[2026-07-02 18:55 MDT]** **✅ #199 await-fix SIGNED OFF by Coach** (commit `41824f6c` / docs `41ddfc28`) — correct + sufficient on code-verify; immediate-reload empirical proof NOT required (no fresh supplier PDF needed). All analysis + fix gates CLEARED. ONLY remaining: T10 + T13/T16-send/T17 on PRJ402097 — Marc confirming Send proceeds unflagged first (the "1 item incomplete" is a scan-results advisory per Jon, not a send-block). Full pass → deploy.sh ships #199 + await-fix (`41824f6c`) + #17. Deploy gate = these 4 live tests only; no further approval gate. Deploy imminent on pass.
- **[2026-07-02 18:52 MDT]** **#199 analysis items ALL CLOSED.** T3 code-confirmed covered-by-design (flag rides `isCrossed` lifecycle — `pn_changed`-accept drops via whitelist @48067, reject/qty-only persist; no gap). T12 N/A-by-design note applied to plan (Coach `46b8c326`). Tuning residual captured to INBOX. REMAINING: T10 + T13/T16-send/T17 live on PRJ402097 (Marc+Jon); await-fix being committed (Marc, explicit pathspec) → Coach re-verify (last gate); Coach setting whether an immediate-reload empirical proof is required (may need Jon fresh-PDF drop). Deploy HELD pending: final live chunk pass + await-fix committed + Coach sign-off.
- **[2026-07-02 18:49 MDT]** **#199 fixture set — PRJ402097** (fully-priced/sendable) for the last live chunk: T10 (approve-sweep) + T13/T16-send-button + T17 (preReview compose). Marc confirms pricing-clean first, then runs those (Jon clicks checkboxes, strict stop-loop). Then: apply await-fix (@38302) → re-verify → Coach sign-off → deploy #199 + await-fix + #17. 13/18 green; T12 resolved; T3 pending Coach leg code-confirm. Deploy HELD (full pass + Coach sign-off).
- **[2026-07-02 18:48 MDT]** **#199 live pass — 13 of 18 GREEN** (added T16 uncheck-clears). T12 RESOLVED (Coach): ship read-only resolved — §2.2-compliant + SAFE (supplier re-cross @38978 unconditionally re-arms a resolved row = automatic re-review trigger). T3 covered-by-design, pending Coach code-confirm of the `pn_changed`-accept leg. REMAINING live: T10 (approve-sweep) + T13/T16-send-button/T17 — need a FULLY-PRICED sendable fixture (PRJ402111 has 10 pricing-incomplete → confounds the Send block); Freddy asking Jon: existing sendable project vs build ZZ-TEST-199 scratch. Plus: apply await-fix (@38302) + Coach re-verify (last gate) before deploy. NEW tuning follow-up captured to INBOX: resolved-TR row can't be manually re-armed/re-flagged (read-only) — defer to broader Tech-Review tuning (auto re-arm on supplier re-cross still fires). Deploy HELD (fixture + await-fix + Coach sign-off).
- **[2026-07-02 18:46 MDT]** **DECISION (Jon): apply the await fix (`app.jsx:38302`) PRE-deploy.** The "Portal Apply unawaited-save → reload-race" BUG now ships WITH #199 (not a fast-follow); INBOX item marked IN-PROGRESS, Freddy RESOLVES w/ deploy SHA at triage. Plan: Marc finishes live pass → applies Coach's one-liner (`.catch → await`) → re-verifies (wait-then-reload + immediate-reload) → Coach re-verifies (last gate) → deploy #199 + await-fix + #17. Deploy HELD pending Coach sign-off + full pass.
- **[2026-07-02 18:43 MDT]** **#199 live pass — 12 of 18 GREEN.** Added T4 (manual flag sets badge + trips gate) + T2 (manual flag PERSISTS through reload via `onSaveImmediate`). Persistence question fully closed — #199's own save path is sound; earlier supplier revert = benign reload-race (Coach-confirmed). T12 DISCREPANCY: impl makes a RESOLVED row's checkbox read-only (no manual re-open); plan §2.2 marks re-open "optional." Freddy's lean: read-only = plan-compliant → T12 N/A-by-design, manual re-open deferred to Tech-Review tuning; Coach confirming plan intent + whether a re-cross re-arms a resolved row (safety) — HELD pending Coach. OPEN: T16 (co-drive now), T3, T10 (approve-sweep scratch), T13/T16-send/T17 (scratch), T12 (Coach call). Deploy on full pass (gate already cleared).
- **[2026-07-02 18:40 MDT]** **✅ #199 DEPLOY-GATE RESOLVED — Coach REFUTED the ship-blocker** (`docs/199-PORTAL-PERSIST-VERIFICATION.md` `186da1fe`). BENIGN: `doApplyPortalPrices` DOES persist (`safeSave` @38291-93, TR fields preserved/not stripped) but the save is unawaited/fire-and-forget → Marc's immediate reload beat the in-flight write. Pre-existing reload-race (since v1.19.722), affects ALL portal-apply data, NOT #199-specific. → #199 CLEAR to deploy on full live pass. Logged NEW LOW capture to INBOX: "await safeSave in portal Apply handler (close reload-race)" (fast-follow hardening). Marc + Jon finishing live tests (manual-flag T2 w/ pause, T3, T4/T12/T16, T10 approve-sweep, T13/T16-send/T17) → deploy #199 + #17.
- **[2026-07-02 18:34 MDT]** **🔴 #199 DEPLOY HELD — potential SHIP-BLOCKER (T2 persistence), code-grounded.** The supplier variance-apply CROSS branch (`app.jsx ~38967-38997`) updates React state only — NO explicit Firestore save; relies on `doApplyPortalPrices` to persist. If it doesn't persist crossed rows, the supplier auto-stamp won't survive a reload → gate reads "no unresolved TR" → quote sendable without sign-off. Empirically cross+flag+prices all reverted on reload. NUANCE: update()-only pattern is PRE-EXISTING (#199 may expose, not introduce, a broader supplier-cross persistence gap). ACTION: Coach tracing `doApplyPortalPrices` (gates ship vs fix); Marc+Jon running clean manual-flag isolation (does manual flag persist via `onSaveImmediate`?). Supplier cluster behaved correctly in-session (8 tests); open risk is purely PERSISTENCE. Deploy HELD.
- **[2026-07-02 18:33 MDT]** **⚠ #199 T2 (persistence) ANOMALY, being isolated (not a fail yet).** After supplier apply + immediate reload, the WHOLE panel write reverted (prices + cross + TR flag) — NOT #199-specific; likely a premature reload before the debounced save flushed. Action: (a) Marc re-runs T2 cleanly via a MANUAL flag (also covers T4) — Jon clicks → wait 5s → reload → verify flag persists; (b) Coach code-checking the supplier-apply persistence path in parallel to rule out a real gap. Supplier cluster (8 tests) still stands. This gates the deploy — Freddy confirms benign before ship. Marc → clean T2/T4 co-drive w/ Jon; Coach → apply-persistence code-check; deploy held pending both.
- **[2026-07-02 18:28 MDT]** **#199 live pass — 10 of 18 GREEN. Supplier cluster fully passed on PRJ402111** (cross manufactured per Jon): T1 auto-stamp · T5 supplier-lock/MED-1 · T14 composed block message · T8 reviewer Resolve · T18 MED-3 7th-surface gate · T9 Resolve · T11 predicate excludes resolved · T15 resolve clears gate · T6 legacy render · pricing no-regression. Money-path gate confirmed firing + clearing across all 7 surfaces. REMAINING: T2 (reload, driving now), T3 (reconciliation spot-check), T10 approve-sweep (Marc scratch), T13/T16-send + T17 (scratch), T4/T12/T16-checkbox (need Jon's real React clicks). Marc finishing solo + will co-drive 3 checkbox toggles w/ Jon. Full-pass → deploy (#199 + #17).
- **[2026-07-02 18:18 MDT]** **#199 T1 fixture RESOLVED — Jon approved (a).** Marc manufactures a cross by re-mapping a BOM-Match dropdown in PRJ402111's variance-review → creates a genuine cross → exercises the auto-stamp path. T1/T3/T5/T18 now UNBLOCKED. Full T1–T18 co-drive proceeding (Jon clicks checkboxes; Marc ref-clicks buttons + re-map/apply + verifies). Full-pass → deploy (#199 + #17).
- **[2026-07-02 18:15 MDT]** **#199 co-drive underway** (Jon clicks React checkboxes, Marc ref-clicks buttons + verifies). VERIFIED: T6 render (47 TR checkboxes, 0 console errors) + no-regression on pricing banner. NOW: co-driving the non-supplier set (T2/T4/T6/T8–T17) on Marc's fully-priced scratch. T1/T3/T5/T18 (supplier cross) HELD — PRJ402111's submission is exact-match/price-only (no cross → won't trigger T1); Jon deciding the T1 fixture (manufacture a cross via re-map, or a real-substitution submission). Full-pass → deploy (#199 + #17).
- **[2026-07-02 18:05 MDT]** **#199 live pass → CO-DRIVE mode (Jon + Marc).** 12px TR controls too small for reliable extension-clicks → Jon clicks in the monitored tab + Marc verifies each step (canonical live-testing model). Already verified: T6 (legacy render — 47 rows show the TR control, zero console errors) + no-regression on pricing send-block. Now co-driving full interactive T1–T18 (fully-priced scratch for send-gate isolation; PRJ402111 RFQ for supplier tests). FULL-pass → deploy (#199 + #17); Coach standby for results verify.
- **[2026-07-02 18:03 MDT]** **#199 live pass — supplier fixture resolved.** Jon: PRJ402111 has a pending RFQ to process/import → Marc's T1/T3/T5/T18 fixture. Marc now: process/import PRJ402111's RFQ → variance-apply → verify T1 (auto-stamp) / T3 (reconciliation) / T5 (supplier flag disabled) / T18 (Quoted-BOM block); non-supplier T2/T4/T6/T8–T17 on the scratch → FULL T1–T18 coverage. FULL-pass → deploy (#199 + #17) → SHIPPED. Coach standby for results verify.
- **[2026-07-02 17:57 MDT]** **#199 live pass — split to unblock.** Marc using a FRESH scratch project for mutation tests (contained; T10 BC upload → sandbox task). NOW running the non-supplier set on scratch: T2/T4/T6/T8–T12/T13–T17. Incidental positive: existing PRICING send-block banner still renders correctly with the #199 build (no-regression signal). HELD: T1/T3/T5/T18 (supplier auto-stamp etc.) need a pending supplier submission — Jon's fixture call pending. Deploy on FULL-suite pass; Coach standby.
- **[2026-07-02 17:54 MDT]** **#199 live pass UNBLOCKED — Data-Retention block lifted.** Jon confirmed PRJ402xxx are all test data (pre-launch). Marc running FULL T1–T18 on one contained test project. FULL-pass → deploy (#199 + #17); Coach standby for results verify. Also captured GEN "matrix-arc-test shares PROD Firestore (not data-isolated)" to INBOX — infra follow-up to fix before launch (safe now, pre-launch; source Marc).
- **[2026-07-02 17:51 MDT]** **#199 live pass FULLY UNBLOCKED.** Jon confirmed admin/reviewer on test (T8–T12 drivable) + any test project is fair game for the supplier fixture (T1/T3/T18). Marc now running the FULL T1–T18 in the controlled tab (visual/behavioral). FULL-pass → deploy (#199 + #17) → SHIPPED. Coach standby for results verify; deploy gated on FULL-suite pass (incl. T1 supplier auto-stamp). G002 full run deferred.
- **[2026-07-02 17:47 MDT]** **#199 live pass STARTED (test target).** Env: #199 deployed to matrix-arc-test only (prod untouched), controlled tab authenticated + BC green, v1.21.24. Marc driving the fully-drivable set (visual/behavioral — JS field-reads blocked by harness auth-token filter, so behavior is the proof): T4/T5/T6/T13–T17/T2-reload-proxy. T8–T12 pending Jon's admin/reviewer role confirm; T1/T3/T18 held pending a supplier-submission fixture (Jon's call). Deploy only on FULL-suite pass; Coach standby for results verify.
- **[2026-07-02 17:29 MDT]** **#199 live pass UNBLOCKED — tab-control snag resolved.** No re-auth needed (ARC login persists across tabs + BC auto-connects), so Marc opens a fresh CONTROLLED tab in his own session → matrix-arc-test ARC (clean URL, no trailing dot per B001) → auto-auth + BC → runs T1–T18. Coach standby for results verify; ALL-PASS → deploy (#199 + #17) → SHIPPED. G002 full run deferred.
- **[2026-07-02 17:24 MDT]** **#199 → LIVE ACCEPTANCE PASS underway (Jon linked the ARC tab).** Marc running live T1–T18 in the controlled ARC tab (matrix-arc-test + scratch): P1 stamp/save-reload, P2 Resolve/approve-sweep, P3+MED-3 send-block on all 7 surfaces. ALL-PASS → deploy (#199 + #17). Coach on standby for results verify; on all-pass → deploy → #199 SHIPPED. G002 full run deferred to clean boot.
- **[2026-07-02 17:23 MDT]** **#199 FULLY CODE-COMPLETE + Coach-verified (MED-3 PASS, `75ecfd46`).** All 7 customer-facing surfaces gated on unresolved Tech Review. P1✓ P2✓ P3✓ + MED-1/2/3 ✓, cross-check GREEN. Jon pinged to connect the controlled ARC tab → Marc runs live T1–T18 → deploy (#199 + #17). Marc + Coach on standby for the live pass. MED-4 = LOW follow-up. G002 full run deferred to clean boot.
- **[2026-07-02 17:21 MDT]** **#199 MED-3 fix BUILT → Coach re-verify (final piece).** MED-3 (gate the Quoted-BOM send) = BUILT (Marc, `c46184aa`) — `handleBomSend` now blocks on unresolved TR (mirrors verification gate). #199 FULLY CODE-COMPLETE (P1+P2+P3 + MED-1/2/3 + L-items). T18 added to live batch. Marc holding; Coach re-verifying MED-3; on PASS → Freddy pings Jon to connect ARC tab → live T1–T18 → deploy (#199 + #17). G002 full run deferred to clean boot.
- **[2026-07-02 17:19 MDT]** **Jon's session tests DONE → #199 finish line kicked off.** MED-3 fix (gate the Quoted-BOM send) routed to Marc (one-line add) → Coach re-verify → then #199 fully code-complete → live T1–T17 → deploy (#199 + #17). Triage: B001 (trailing-dot redirect URI, LOW) + B002 (approved-state TR block message, LOW) promoted to TODO Bugs list; INBOX now empty. Marc → building MED-3 fix; Coach → on-deck to re-verify; #199: P1✓ P2✓ P3✓, MED-3 in progress. G002 full run deferred to clean boot.
- **[2026-07-02 17:16 MDT]** **#199 — MED-3 ruled; team held CLEAR for Jon's session tests.** MED-3 ruled by Jon = GATE the Quoted-BOM send too (one-line add) — QUEUED + HELD so the team stays idle for Jon's tests. MED-4 = LOW follow-up (captured). TEAM CLEAR (Marc + Coach idle); Jon running session tests now; Freddy holding MED-3. After Jon's tests: MED-3 fix (Marc) → Coach re-verify → live T1–T17 (controlled ARC tab) → deploy (#199 + #17). G002 full run still deferred to a clean boot.
- **[2026-07-02 17:13 MDT]** **#199 P3 = code-verify PASS (Coach, `docs/199-P3-VERIFICATION.md` `30995eb9`) → CODE-COMPLETE, all phases verified.** Team CLEAR (Marc + Coach idle). 2 pre-deploy items for Jon: MED-3 (customer-facing Send-Quoted-BOM bypass) → Jon's ruling (gates deploy); MED-4 → LOW follow-up. Jon pinged (P3 complete — session-test window open). Next after MED-3: live browser T1–T17 → deploy. (Also captured 2 LOW bugs to INBOX this update: trailing-dot redirect + approved-state TR block message.)
- **[2026-07-02 16:31 MDT]** **#199 CODE-COMPLETE (P1+P2+P3) → Coach final verify.** P3 (hard gate) = BUILT-PENDING-VERIFY (Marc, `a0e39335`): 6-surface send-block + distinct message + TR-bucket label fix + MED-2 fix + L4/L5 (dead-end audit PASS). #199: P1✓ P2✓ P3-verify. Coach final-verifying P3 (money-path gate); on PASS → Freddy pings Jon (session-test window) → live browser T1–T17 → deploy. G002 full run still deferred to a clean boot.
- **[2026-07-02 15:57 MDT]** **#199 P2 PASS → P3 AUTHORIZED.** P2 verified PASS (Coach, `docs/199-P2-VERIFICATION.md` `10920f61`). Marc now building P3 (hard gate — last phase) + folding in Coach's MED-2 fix (approve-sweep partial-write guard, protects gate integrity) + L4/L5. Coach verifies P3 next. #199: P1✓ P2✓ P3-building; after P3 code+verify → browser T1–T17 + deploy; Jon runs session tests after P3-complete.
- **[2026-07-02 15:51 MDT]** **#199 P2 BUILT → Coach verify.** P2 (Resolution) = BUILT-PENDING-VERIFY (Marc, `a5253d42`): MED-1 lock + indicator-through-predicate + per-row Resolve (stamps who/when) + approve-sweep across all panels. L3 decided (TR show-set broader than red = dead-end guard; Marc+Freddy concur, Coach confirming). Marc holding P3 for Coach-clear + Freddy GO; Coach verifying P2; #199: P1✓ / P2-verify / P3-next; Jon runs session tests after P3; runtime T1–T12 batched for browser pass.
- **[2026-07-02 15:44 MDT]** **#199 — MED-1 DECIDED → P2 AUTHORIZED.** Jon locked MED-1: supplier flags clear only via audited Resolve. Marc now building P2 (Resolution): MED-1 P1-lock fix + indicator-through-predicate refinement → `_isTechReviewer` repoint (26308 only) → per-row Resolve control (stamps who/when) → approve-sweep. Coach verifies P2 when it lands. #199 BUILDING (P1 done, P2 in progress, P3 next); live T1–T7 pending browser; G002 mechanic confirmed / full run deferred.
- **[2026-07-02 15:42 MDT]** **#199 P1 = code-verify PASS (Coach, `docs/199-P1-VERIFICATION.md` `310c9246`).** One MED policy item now with Jon, GATES P2: **MED-1** — P1 lets a reviewer clear a supplier flag with no audit stamp. P2 HELD; Marc holding for P2 GO; Coach P1-verify done, on-deck for P2; live T1–T7 pending browser. G002 `Ctrl+N` mechanic CONFIRMED, full run deferred to a clean boot.
- **[2026-07-02 15:32 MDT]** **#199 P1 BUILT — now in Coach code-verify.** P1 (Data + capture, inert) = BUILT-PENDING-VERIFY (Marc, `13f06fcf` + `66494253`); §5 pre-build gate PASS. Coach code-verifying P1, then P2 (Resolution); live T1–T7 batch when browser's up. G002 v3 — Jon's live calibration (`Ctrl+N` flow) ongoing. Marc holding for Coach's verdict to start P2; #199 BUILDING (P1 done, P2 gated on Coach).
- **[2026-07-02 15:22 MDT]** **BOTH queued items now RUNNING (Jon: "run both").** #199 Tech-Review flag BUILD STARTED — Marc building phased in the standing session (P1 data+capture → P2 resolution → P3 hard gate; R2-enforced), Coach verifies each phase (HIGH-stakes lane). G002 v3 — Jon running the live calibration (`Ctrl+N` + paste flow) on his desktop; #1 risk = `Ctrl+N` create+focus + paste landing. Marc → #199 P1; Coach → #199 §2.3 doc-note then phase-verify on-deck; Jon → G002 calibration; Freddy hub-coordinating. #199 now BUILDING (no longer queued).
- **[2026-07-02 15:05 MDT]** **HYBRID team model ADOPTED (Jon); subagent trial complete; HOLD LIFTED.** #17 fixed friction-free via subagents (Marc-subagent applied `.catch`; Coach-subagent + pre-commit both APPROVE; committed `0651a73c`) → **#17 RESOLVED**. Decision: HYBRID — subagents for LOW-stakes/mechanical, 4 standing sessions for HIGH-stakes/cross-checked. Coach documenting the rule in CLAUDE.md. Back to G002 v3 re-calibration (Jon, when ready) + #199 (Jon's build-gate).
- **[2026-07-02 14:56 MDT]** **SUBAGENT TRIAL — all other work HELD.** Jon testing whether role-subagents (zero Allow-Once friction) behave as well as separate sessions. Dez suggested trial candidate **#17** (fire-and-forget `_showPopupBlockedFallback` in `arcDocOpen` — LOW, single-line `.catch` fix, no pricing/Firestore surface, Coach-reviewable in seconds). G002 re-calibration + #199 paused until the trial runs.
- **[2026-07-02 14:45 MDT]** **G002 v3 REWORKED for single-window CCD.** Marc delivered v3 (`c18a0d24`) — `Ctrl+N` + paste ×4 in one window, window-handle machinery dropped; session1 pauses for Jon to tear-off + title peers then runs comms-check. Coach doing quick v3 re-review; Jon re-calibrates next (new risk: `Ctrl+N` create+focus + paste landing). #199 still verified & queued for build-gate.
- **[2026-07-02 14:22 MDT]** 🔴 **G002 — calibration found window-model mismatch; launcher rework needed.** Jon's live calibration revealed CCD = single-window tabbed sessions (`Ctrl+N` switches in-place) + manual `Shift`+drag tear-off, NOT window-per-session as the launcher assumed. Marc REWORKING (`Ctrl+N` + paste ×4 in one window; tear-off + titles stay manual); then Coach re-review → Jon re-calibrates. #199 still verified & queued for build-gate.
- **[2026-07-02 14:12 MDT]** **G002 — Coach APPROVED v2; Jon's calibration greenlit.** Coach v2 re-review APPROVED (`20fa8b79` — all 3 HIGH fixes sound, 2 LOW live watch-items). Jon on Stage A (safe dry-run + config `$CcdExe` / `$NewSessionMethod`); real calibration run is NEXT. #199 still VERIFIED & queued for build-gate.
- **[2026-07-02 14:07 MDT]** **G002 BUILT-PENDING-CALIBRATION v2** — Marc delivered v2 (`2d1cb97c`) with all 12 Coach findings fixed; Coach doing a quick v2 re-review of the diff before Jon's live calibration. Jon's calibration pass is NEXT. #199 still VERIFIED & queued for build-gate. (G004 tool-permission allowlist committed, verifies at reboot.)
- **[2026-07-02 13:52 MDT]** **G002 — static review found 3 HIGH; Marc fixing before live calibration.** Coach static-review DONE (`docs/G002-LAUNCHER-STATIC-REVIEW.md`, `4240bd2f`); Freddy resolved the H3 design fork (session1 = trimmed Freddy "launcher-mode" block, not full `/team-startup`); Marc now fixing H1/H2/H3 + 5 MED + 4 LOW; Jon's calibration DEFERRED until "BUILT-PENDING-CALIBRATION v2"; #199 still verified & queued for build-gate.
- **[2026-07-02 13:43 MDT]** **G002 BUILT-PENDING-CALIBRATION** — Marc DELIVERED the launcher (`tools/team-boot/`, commit `1ba33d58`); needs one calibration + test pass on Jon's desktop. Coach queued to static-review the script; Jon to run calibration; #199 still VERIFIED & queued behind the launcher test for Jon's build-gate.
- **[2026-07-02 13:41 MDT]** Status ping — **G002** in progress: Marc BUILDING the launcher (PowerShell+AutoHotkey, ~6 one-time clicks); Coach idle (verifies after build); Freddy hub-monitoring; #199 (Tech-Review flag) VERIFIED & queued behind G002 for Jon's build-gate.
- **[2026-07-02]** G003 board stood up. G001 CLOSED (Allow-Once prompt hardcoded, accepted limitation). G002 [Decided] Option A → Marc building launcher. #199 verified, queued for Jon's build-gate. Plan: finish G002 → close-out → teardown → fresh boot via launcher (startup test) → #199.

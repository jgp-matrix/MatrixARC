# 📥 Intake Inbox — awaiting Freddy triage

> **Owner (sole writer): Dez (Dezzie Arnez, Intake).** This is a dedicated single-writer file
> so it cannot collide with TODO.md on the shared working tree (per DOC-OWNERSHIP-PROPOSAL §2 +
> Freddy's INBOX.md-split refinement, approved 2026-07-02).
>
> **Dez:** drop raw bug/feature captures here — timestamped, **un-numbered** — one bullet each,
> dedup-checked against existing `#N` in TODO.md + SESSION-STATE.md first. Commit `-- INBOX.md`
> and push immediately after each capture. Do NOT assign `#N` (Freddy is the sole allocator →
> avoids multi-session number collisions).
>
> **Freddy:** on triage passes, pull from here, dedup-confirm, assign the next `#N`, promote the
> item into the TODO.md numbered tracker, then delete the bullet here. (Freddy edits this file
> only to clear routed bullets; Dez owns all appends.)
>
> Capture format: `- [YYYY-MM-DD] BUG|FEAT|GEN — "<short title>" — <desc> — reported via Intake (source: Jon|Marc|Coach|Freddy)`
>
> Category (B/F/G taxonomy, replace-going-forward, approved 2026-07-02 — Freddy stamps the number at triage):
> `BUG → B###` · `FEAT → F###` · `GEN` (General — neither bug nor feature) `→ G###` · `METRICS → M###` (analytics/engagement telemetry; new category added 2026-07-10, see NUMBERING-CONVENTION.md). Every item carries a short
> identifying title. Existing `#1–#198` keep their numbers (no retroactive renumber; open items get tagged in Freddy's triage pass).

---

- [2026-07-31] BUG — **B077** "Non-legacy 0-BOM panel passes send-gate" — [✅ BUILT 2026-07-31, commit `4d4c8e66`, awaiting Coach review + Test] non-legacy sibling of #119: a panel WITH an extractionReport but 0 BOM rows and no `manualVerifyRequired` passed the send-gate → blank quote sendable. Broadened the #119 guard. — reported via Intake (source: Marc→Freddy)
- [2026-07-31] FEAT — **F083** "Item Browser: prompt to add extraction-identified MFR to BC" — On extraction ARC now populates the manufacturer on BOM rows for items NOT in BC (Jon: nice surprise; from Bug 1b `_withMeta`/`mfrCodes`). Feature: when the user opens the BC Item Browser for such an item to create/assign it in BC, PRE-FILL / PROMPT the extraction-identified MFR so it's added to the BC item card in one action (don't make them re-enter). Builds on the MFR-on-row data + the Item Browser create/MFR-assign flow (v1.24.49 mfr-guard, v1.24.50 Bug 1b). Money-path-adjacent (BC data quality). — reported via Intake (source: Jon 2026-07-31)

- [2026-07-31] FEAT — **F084** "Price-Date hover: current source + alternate-suppliers list" — reorganize the BOM Price-Date hover popover to show the currently-selected source/price/date at top, then a list of the item's alternate BC suppliers with price + lead-time (via `bcFetchPurchasePricesMultiVendor` keyed on `bcNo`, lazy per-row cache). BUILDING 2026-07-31. — reported via Intake (source: Jon)
- [2026-07-31] DECISION-PENDING — Secondary-vendor RFQ **default ON** (opt-out) so secondary quotes auto-solicit → populate BC prices/LT. Jon parked the how (hardcoded vs admin-setting) — awaiting his call; feature verified on Test V.103, not yet merged to prod. — (source: Jon)

- [2026-07-31] FOLLOW-UP (F069) — add soft env-guard `!_bcEnvMismatched(project)` to the 2 BACKGROUND BC-sync paths Coach flagged: [BG PRICING] planning-lines sync (~17175) + contingency auto-sync (~30278). Pre-existing gap (not a regression; F069 shipped v1.24.66 guarding the 6 user gestures). NOTE: verify `project` is in scope at each site before adding (scope-bug risk — that's why it wasn't hand-added). — (source: Coach review)
- [2026-07-31] FOLLOW-UP (F084) — 2 non-blocking nits: (1) exclude the current vendor from the alt-list by `bcVendorNo` (exact) instead of case-insensitive name-match (brittle on remapped names); (2) add ~150-250ms hover-intent debounce to `_loadAltSuppliers` so a fast mouse-sweep doesn't queue an uncached lead-time fetch per row (BC throttle). — (source: Coach review)
- [2026-07-31] FOLLOW-UP (secondary-RFQ) — when the ONLY secondaries are API vendors (DigiKey/Mouser), the "no secondary suppliers found" message should say "only API vendors here — priced via Get Prices, not RFQ'd" so it's not confusing. — (source: Jon test)

- [2026-07-31] BUG — **B078** "Multi-page add saturates Firestore write-queue → extraction results silently lost" — [SCOPED — Coach, docs/B078-B079-B080-write-batching-scaling-scope.md] ROOT CAUSE (Coach-verified): NOT a save-per-page. An 18-page add = 1 PDF + 18 Storage uploads saturating the uplink WHILE ~7 PanelCard background `_noBumpWrite` effects + the extraction saves each rewrite the WHOLE near-1MB project doc → WriteStream can't ack → `resource-exhausted`. Part 2 = the extraction `save` wrapper (~16269) **swallows the error to `console.warn`** → UI shows "✓ N items" but Firestore got nothing → reload = 0 items/no report (Ryan's exact symptom). FIX (priority): **B078-1 (S/Low) unswallow+retry the extraction save + surface a fail banner — SHIP FIRST**; B078-2 (M) coalesce the 7 bg autosaves; B078-3 (S) decouple save from upload spike + fix the high-water silent success-shape (~10570). **Money-path/save-path → Coach review → Jon approve → Test → prod.** Evidence: PRJ402141, Ryan, 2026-07-31. — reported via Intake (source: Jon live-diagnosis → Freddy)
- [2026-08-03] BUG — **B081** "Auto-Add BOM Items config shows MTX# instead of Vendor Part#" — [SCOPED — docs/B081-vendor-partnumber-scope.md] Fix = Option (a): the BC search already carries the vendor part# (`_vendorItemNo`); `addBcItem` (~20580) discards it. Capture `vendorPartNumber:item._vendorItemNo||""` at add + render `{item.vendorPartNumber||item.partNumber||"—"}` (~20823) + swap the search dropdown (~20789). Mirrors proven fuzzy-box fix `1075d007`. Low risk, ~3 lines. ✅ BUILT + staged (branch `marc/b081-vendor-partnum` `7ce575ba`). Jon 2026-08-03: show BC vendor part# (his Buyoff/Crate DO have populated vendor#s — scope's "blank" guess was wrong); MTX# fallback for any blank; Jon will re-add his 2 existing rows (no backfill). Display-only, low-risk — Test/prod whenever. — Settings → Configuration → AUTO-ADD BOM ITEMS list renders `item.partNumber` (the BC Item No, e.g. `MTX-114593` Buyoff/Service, `MTX-114825` Crate) in the PART # column at src/app.jsx:20785. Jon: these should show the **Vendor Part #**, not the internal MTX# — even for non-inventory items the MTX# shouldn't show. OPEN QUESTION for scope: do these auto-added BC items carry a vendor part# field (or need a lookup), and what shows for Matrix-internal service items (Buyoff/Crate)? Ties to the earlier `claude/mtx-vendor-display` (fuzzy-box MTX→vendor) theme. Low-stakes display. — reported via Intake (source: Jon 2026-08-03, screenshot confirmed)
- [2026-08-03] FEAT — **F085** "Prompt to sync unsynced ARC→BC changes when leaving a project" — [✅ SHIPPED prod v1.24.70 (`afd32532`), Coach-reviewed + Jon Test-verified. Incl. loop-fix (unpriced→Later-only) + BC-block reword. Follow-ups F1/F2 (LOW) still open below.] [SCOPED — Coach, docs/F085-bc-sync-on-leave-scope.md] Jon: on leaving a project with unsynced ARC→BC changes, PROMPT "Sync now vs. wait until next open." CORRECTED FINDING: the core BOM→BC planning-lines sync is NOT lost on leave — a durable per-panel `bomSyncHash` auto-recovers it on next open (+ pre-print catches it); what's lost is the on-screen ⚠ reminder. Plus a 3s auto-push exists (28875) but is cancelled if you leave <3s. Design: extend the existing leave-gate `checkQuoteRevWarn` (54133) with a `checkUnsyncedBcWarn` driven by ONE `_hasUnsyncedBcChanges(project)` predicate (durable, hash-based); "Sync now" flushes / "Later" is safe (recovers next open). B078 no-silent-swallow lesson applies to the Sync-now failure path. Effort S–M. Money-path (BC). ⭐Jon decisions: (1) hash-based-now vs widen-hash (coordinate w/ B067); (2) beforeunload native nudge y/n. — reported via Intake (source: Jon 2026-08-03)
- [2026-08-03] BUG — **B082** "Sell-price/markup-only edit dropped to BC if user leaves <2s" — [MED · found in F085 audit] The sell-price/markup→BC line-10000 PATCH debounces 2s (28884–28902) and its timer is cleared on unmount; `computePanelBomHash` (10417) EXCLUDES sell price/markup → the on-open auto-sync never re-pushes it. Narrow window (leave <2s after a markup-only change, no accompanying BOM change) but **no recovery path** — genuinely dropped. Fix ties to widening the panel hash (coordinate w/ B067). CLARIFIED (Jon 2026-08-03): the editable trigger is the **Margin** (users edit only BOM costs + Margin; sale price is auto-computed from Margin and pushed to BC line 10000). So B082 = change Margin → leave <2s → the derived sale-price push to BC is dropped + not hash-recovered. Recommended fix = targeted flush-on-leave (small, no shared-hash change) rather than hash-widening. ⏳ Awaiting Jon: build the targeted flush now, or park (narrow window). Money-path (BC). — reported via Intake (source: Coach F085 audit → Freddy)
- [2026-08-03] FOLLOW-UP (F085) — Coach APPROVE-WITH-FIXES; M1 fixed (relocated both leave modals out of the `navTab==="projects"` gate so they render from any origin tab — also fixes a pre-existing latent revWarnModal gate-trap). Two LOW non-blockers: (F1, pre-existing) a PARTIAL planning-lines sync still flashes a green "✓ Synced to BC" bg pill (~29038) while `syncFailedAlert` shows — gate `bgDone` on the outcome; (F2) "Sync now" can't report lead-time BC-writeback failures (`_flushLeadTimeBcQueue` only console.warns) — consistent w/ B083 contract, copy just implies a complete push. — (source: Coach F085 review)
- [2026-08-03] FEAT — **F086** "Admin Global Message + forced-refresh-on-new-version broadcast" — [SCOPED — docs/F086-global-broadcast-scope.md. ~70% exists: a "new version" banner + version-detect + running-task guard already built; KEY BUG = its "Refresh Now" does a PLAIN reload not a hard reload → the stale-bundle behind Ryan's problem. Plan = restyle→top-right flashing + hard-reload upgrade + `_system/globalBroadcast` doc + admin top-bar button. 2 phases, S-M, LOW risk. 6 Jon decisions incl. rules-hardening. Awaiting Jon.] Two-part: (1) **Auto version broadcast** — when a NEW PROD VERSION deploys, every logged-in ARC client shows a flashing top-right modal ("finish current work and click here to refresh and load the new version") with a button that does a HARD reload (unregister service worker + clear caches + reload — NOT a simulated Ctrl-Shift-R, which JS can't do). Motivation: Ryan likely ran a STALE cached bundle during last week's problems (recurring SW-cache-staleness theme). (2) **Admin Global Msg** — a "Send Global Msg" button in the Admin home top bar to broadcast a manual message to all logged-in users; the same delivery channel ARC uses for version notices. **Folds in existing F008** (new-version-available refresh notification — the version-detect half). Caution flagged by Jon: be careful with workflow-affecting changes. Design Qs: version-detect mechanism (version.json poll vs push), real-time delivery to all clients (Firestore doc all clients subscribe to?), admin-only gating, modal UX (dismiss vs force). Money-path-adjacent (workflow disruption risk) → scope → Jon approve → build. — reported via Intake (source: Jon 2026-08-03)
- [2026-08-03] BUG — **B083** "Lead-time BC writeback dropped on hard tab-close within 30s batch" — [LOW · found in F085 audit] `_leadTimeBcQueue` value saves to Firestore immediately and the 30s timer survives in-app nav, but a HARD tab-close inside the 30s batch loses only the BC writeback (visibilitychange flush is best-effort, async fetch won't finish before teardown). Manually recoverable via "📤 Push Lead Times to BC." Low, but real. — reported via Intake (source: Coach F085 audit → Freddy)
- [2026-08-03] ⬆ **B080 → HIGH PRIORITY (Jon 2026-08-03: "fix now, don't get blindsided as we add users").** Reframed: NOT a user cap — graceful scaling. ✅ BUILT + Coach APPROVED deploy-ready (branch `marc/b080-anthropic-backoff` `a37af45f`; docs/B080-anthropic-backoff-scope.md): deadline-aware retry-with-backoff+jitter on the 3 extraction Anthropic calls so 429s retry instead of failing silently; +CONCURRENCY 4→3; +fixes a latent `pageResults` ReferenceError. Coach verified it CANNOT retry past the function kill + cost ledger/caps intact; unit test 13/13. Key finding: extraction uses the COMPANY key (`resolveAnthropicKey`), per-user-key fallback already exists. LOW follow-ups (non-blocking): F1 add test-retry.js to .gcloudignore, F2 drain body on supplier-404 continue. ⭐AWAITING JON: (1) ceiling = tier-bump (central billing) [rec] vs per-user keys; (2) `firebase deploy --only functions` go (no Test channel for functions → deploys to prod + post-deploy smoke). (Note: Jon's "3-user issues" were most likely the B078 save-loss, now fixed — asked him to confirm no other multi-user problems.)
- [2026-07-31] BUG — **B080** "Extraction not safe at ~10 concurrent — shared Anthropic key, zero 429 handling" — [SCOPED — Coach, same doc] The real multi-user ceiling (answers Jon's "10 users at once"). `extractBomPage`/`extractBomBatch` have NO 429/Retry-After/backoff — a rate-limited page just fails silently; ALL users share ONE org `ANTHROPIC_API_KEY`; ~20 concurrent Opus(64k-tok) calls breach org TPM/RPM → cascading 429s → failed pages across everyone. CF `maxInstances` (5×4 batch / 10 page) throttles but is NOT the wall. Firestore per-doc fine (separate docs). Fix (CF-only, independent of B078, M): (1) 429/overloaded retry w/ exp backoff+jitter; (2) concurrency cap/queue keyed to org Opus TPM; (3) **Jon decision: per-user API keys / tier bump?** — reported via Intake (source: Coach scaling audit → Freddy)
- [2026-07-31] BUG — **B079** "addPanel names lines `Panel ${length+1}` → duplicate 'Panel 4'" — [SCOPING — Coach lane] `addPanel()` (src/app.jsx ~38697) names a new line `Panel ${(project.panels||[]).length+1}`, recomputed from the current count every add, so any add-after-delete reuses the number. PRJ402141 ended with THREE panels all named "Panel 4" → lines can't be told apart, and the naming signals the add-line flow isn't tracking stable per-line identity. Fix = stable unique identity (persisted monotonic counter / drawingNo / positional "Line N"), checking BC-task-description consumers of `panel.name` first. Low-stakes but touches BC binding. — reported via Intake (source: Jon live-diagnosis → Freddy)

- [2026-08-04] FOLLOW-UP (F085-F2 / B083) — [LOW · pre-existing, found in Coach review of the 8/4 LOW batch, NOT introduced by it] `_flushLeadTimeBcQueue` (src/app.jsx ~30416) sets `q.flushing=true` then `=false` at ~30456, but the vendor-persist block at ~30445-30455 is OUTSIDE the per-write try/catch — if it throws, `flushing` sticks true and blocks later flushes. Recovery button still works + caller catches the throw, so low impact. Fix = wrap the flush body in try/finally to always reset `flushing`. — reported via Intake (source: Coach review → Freddy)

- [2026-08-04] BUG — **B084** "Pin to Top checkbox can't be unchecked" — [NEW, Jon 2026-08-04 · money-path-adjacent (board/F027)] Clicking the "📌 Pin to top" checkbox (src/app.jsx:39870, F027) pins fine but the box CANNOT be unchecked. Mechanism: controlled checkbox on `project.priorityPinnedAt`; onChange writes a narrow `_pinRef.update()` (pin=timestamp, unpin=null) and only flips local state inside `.then()`; `.catch` just `console.warn("[F027] pin update failed:")`. So the unpin write is failing OR the null-state isn't propagating back to the row. Firestore rules checked = SYMMETRIC (isOnlyPriorityPinUpdate allows same keys pin/unpin; manager/admin arm bypasses locks identically) → NOT the obvious cause. DECISIVE SIGNAL: check Debug Logs for `[F027] pin update failed`. Marc lane root-causing. — reported via Intake (source: Jon 2026-08-04)

- [2026-08-04] FEAT — **F087** "Email Customer Confirmation modal on project creation" — [NEW, Jon 2026-08-04 · money-path-adjacent (customer comms / Outlook / email send)] When a Project is created, pop an "Email Customer Confirmation" modal that confirms with the User that an email be sent to the Requestor saying "the RFQ has been received to the Sales queue and someone from Sales will be responding soon." Modal should mirror the SEND QUOTE modal pattern and let the User pick from recent Outlook email chains (builds on the F029 Outlook integration — Slice-1 email panel shipped v1.24.13). [SCOPED — docs/F087-email-customer-confirmation-scope.md] Coach lane: the Send-Quote `reply`-mode Outlook chain-picker (QuoteSendModal ~37779, graphSearchEmails/graphReplyToMessage) is directly reusable; trigger from `handleCreated` (~54482); recipient = selected chain sender → bcContactEmail → free-type. 6 Jon decisions (trigger moment; requestor source [no dedicated field today]; Graph-reply [rec] vs SendGrid; user-gated [rec]; per-project de-dupe + pref; editable body). Effort M. Comms-safety: external email on routine action → user-gated + Skip + `customerConfirmSentAt` de-dupe; test-env already suppresses. Awaiting Jon decisions before build. — reported via Intake (source: Jon 2026-08-04)

- [2026-08-04] FOLLOW-UP (B084) — [LOW · pre-existing, surfaced in Coach review of the B084 fix, NOT introduced by it] `saveProject` is lock-free (doesn't acquire `_panelSaveLocks`), so a residual TOCTOU remains: a pin `_pinRef.update` landing between saveProject's `ref.get()` (~10614) and `ref.set()` (~10910) could be mirrored-back-stale. This is a general property of saveProject's full-doc-replace for EVERY field it writes, not pin-specific; the B084 3b guard actually shrinks the pin's exposure vs. the old conditional guard. Possible future hardening: route the pin write's lock through saveProject too (or give saveProject the same mutex). — reported via Intake (source: Coach B084 review → Freddy)

- [2026-08-04] FEAT — **F088** "Per-user settings store (member-doc `prefs` map); Pin-to-Top = first consumer" — [NEW, Jon 2026-08-04 · follows B084] B084 shipped pins GLOBAL (shared on the project doc `priorityPinnedAt/By`) — Jon wants pins PRIVATE per user (Andrew must not see Jon's pins). KEY FINDING: the existing `config/*` store is COMPANY-shared (`_appCtx.configPath=companies/{cid}/config` @54116), NOT per-user → can't hold per-user settings. DESIGN (Jon approved direction): add a per-user `prefs` map to the member doc `companies/{cid}/members/{uid}` (already per-user: permissions/bcUserCode/displayName; rules already allow self-edit of own record ~414-416). `prefs.pinnedProjects={projectId:pinnedAt}`; board reads/sorts/📌 from CURRENT user's prefs → private automatically; extend rules self-edit whitelist to `prefs`; migrate pins off the project doc (retire `priorityPinnedAt/By`); solo users (no company) fall back to `users/{uid}/config` (already per-user there). Removes the shared-doc clobber race entirely. [SCOPED — docs/F088-per-user-settings-store-scope.md] ⚠ CORRECTION found: members do NOT have member-doc self-edit today (whole `allow update` is admin-only, rules:414) → F088 needs a NEW any-member self-edit rule scoped `hasOnly(['prefs','updatedAt'])` on own doc (the security-sensitive piece). Design: pins → `members/{uid}.prefs.pinnedProjects` (solo → users/{uid}/config/prefs), read live via existing member onSnapshot (54128) into new `myPins` state, sort 50284 + tile 51683 read myPins. B084 `_panelSaveLocks` + both save-guards become DEAD (member doc has no bg clobber writer) → remove. Grep clean: zero functions/** refs. 5 Jon decisions (D1 any-member-can-pin[rec] / D2 leave old fields orphaned[rec] / D3 dedicated myPins prop[rec] / D4 live read[rec] / D5 defer rules cleanup[rec]). Effort M. Rules deploy separate from hosting; both Jon-gated. Money-path NO. Awaiting Jon decisions + go. — reported via Intake (source: Jon 2026-08-04)
- [2026-08-04] BUG — **B085** "Re-link a Project fails silently + doesn't persist if User leaves the Project" — [NEW, Jon 2026-08-04 · MONEY-PATH / DATA-SAFETY · violates the Async Project Ownership Rule in CLAUDE.md] When a Project is RE-LINKED (to BC), leaving the Project mid-operation BREAKS the link and fails SILENTLY. **Immediate instance: PRJ402135 failed re-link → did NOT finish populating the BOMs in BC.** Sequence per Jon: (1) REMEDIATE PRJ402135 (finish the BC BOM population — investigate what's missing first; live BC write likely needs Jon's session); (2) FIX the re-link function's persistence (scope the async op to projectId + survive leave + surface failure loudly, not silent); (3) → G023 systemic audit. Sibling of B078 (silent save-loss) / F085 (leave-sync) family. Marc lane investigating (root cause + PRJ402135 state + remediation plan) — NO live mutation without Jon. [INVESTIGATED — docs/B085-relink-persistence-scope.md] ROOT: `relinkToBC` (43011, inside ProjectView) never registers a bg-task → beforeunload guard (54488) is false → hard-nav mid-relink kills it silently; no logDebugEntry/bgError anywhere (43116). Half-finished state: bound to NEW bcProjectNumber, lines partially populated, stale bcLineNo/bcTaskNo on rows. PRJ402135 REMEDIATION (Jon live session): do NOT re-run Re-link (dups the BC project via bcCreateProject 43023) — instead re-run per-panel ⇅ Sync BC (syncPlanningLinesToBC 29119, idempotent diff + B065 delete guard); check #168 unpriced gate first. FIX (Part C, design ready): convert relinkToBC → module-scoped bg-task keyed by projectId (bgStart) + identity-guard writes + loud-fail/Debug-Logs (mirror B078-1) + move stale-clear before the loop. Coach review REQUIRED before build (ordering = #66/B065 orphan/dup risk). — reported via Intake (source: Jon 2026-08-04)
- [2026-08-04] GEN — **G023** "Audit ALL Project functions for leave-persistence + no-silent-fail" — [NEW, Jon 2026-08-04 · follows B085, step 3] Systematic sweep: every Project operation (extraction, pricing, BC sync, re-link, attach, archive/restore, copy, import, lead-time writeback, margin/quote writes, RFQ, etc.) must PERSIST even if the User leaves the Project, and must NOT fail silently. Grounded in the Async Project Ownership Rule (CLAUDE.md CRITICAL: capture projectId/panelId at op start; verify identity before write; never write to whatever project is open). Enumeration-is-a-floor: independent grep of every async op + its completion handler + leave/unmount path. Classify each: persists-safely / drops-on-leave / fails-silently. Findings + fix recommendations; audit only. — reported via Intake (source: Jon 2026-08-04)
- [2026-08-04] GEN — **G022** "Audit ARC per-user settings → consolidate into the per-user prefs framework (F088)" — [NEW, Jon 2026-08-04] Companion to F088. Sweep ARC for ALL settings that are (or should be) per-USER, classify each: (a) already correctly per-user (member doc, users/{uid}/…), (b) currently COMPANY-shared but arguably should be per-user (UI/dashboard/notification/To-Do prefs?), (c) per-user but scattered elsewhere. Recommend which to consolidate into the new F088 per-user `prefs` store to organize the framework. Enumeration-is-a-floor: independent grep of every settings read/write site; a plan is a floor. NOT a blind migration — produce classified findings + recommendations for Jon to pick from (money-path settings like pricing/laborRates are intentionally company-shared — do NOT propose moving those). Audit only; no code. [DONE — docs/G022-per-user-settings-audit.md] Shortlist: #1-3 ephemeral view prefs (groupBy/navPinned/myProjectsOnly) = highest payoff + lowest risk (additive, no migration); #4-10 device-bound localStorage UI prefs; LEAVE push toggle/offline-queue/version-ack/caches + all money-path config. "Should-be-per-user" bucket was rare; config/* correctly company-shared. Awaiting Jon consolidation picks. — reported via Intake (source: Jon 2026-08-04)

- [2026-08-04] BUG — **B086** "Re-link / BC project create doesn't populate Salesperson / Project Manager / Designer in BC" — [NEW, Jon 2026-08-04 · money-path-adjacent (BC project metadata)] `bcCreateProject` (src/app.jsx:4953) POSTs only `{number,displayName}` (4990-4995); the Step-2 ProjectCard PATCH sets the customer, NOT the people fields. So the BC project card's Salesperson/PM/Designer stay blank after re-link (Jon confirmed empty in BC). ARC HAS the data: `bcSalespersonCode` (+ Person_Responsible/CCS_Salesperson_Code read at 9460), and PM/Designer codes on the project. FIX: PATCH salesperson (+ PM/Designer) onto the BC project card during create/relink. SCOPING NOTE: salesperson has a clear BC field (Person_Responsible / CCS_Salesperson_Code); PM/Designer may be BC custom fields (CCS_*) — confirm which fields BC exposes (quick live-BC check) before building. Recommend BUNDLING into the B085 relink rebuild (same function). — reported via Intake (source: Jon 2026-08-04)

- [2026-08-04] ✅ **B085 + B086 — SHIPPED prod v1.24.86, Jon-CONFIRMED on 3 projects** (relink survives leave + fails loudly; Salesperson/PM/Designer populate on BC card). Ready to promote to TODO.md as RESOLVED.
- [2026-08-04] BUG — **B087** "Main board shows a just-relinked project as still-unlinked until refresh" — [NEW, Jon 2026-08-04 · LOW · display-only, data safe · side-effect of B085] After a relink completes, the main board can still show the project as unlinked until a manual refresh (then all show correctly). ROOT: B085 gated the relink-completion `onChange(_relinkBound)` behind `_alive()`; if the relink finishes AFTER the user navigated back to the board, the parent-list update (handleChange :54610, merges by id) is dropped → board keeps the stale copy until Firestore re-read. Data always persisted correctly (Firestore has the binding). FIX: ungate the parent-list `onChange` propagation (handleChange merges by id → Async-Ownership-safe) while keeping `setProject` (ProjectView's own unmounted state) gated; optionally guard handleChange's `setOpenProject` when not viewing. Low-risk. — reported via Intake (source: Jon 2026-08-04)

- [2026-08-04] ✅ **G023 audit COMPLETE — docs/G023-project-persistence-audit.md.** Every project async op graded vs the B085/B078 gold-standard (survives-leave + fails-loudly). Net-new gaps stamped below (B088/B089/B090 + RFQ-send trace). Ranked worst-first for Jon; known items (B082/B083/F085/B078/B085/B087) not double-logged. Awaiting Jon priority pick.
- [2026-08-04] BUG — **B088** "F075 'Get Prices' (runApiPricingOnPanel) drops on leave + fails silently" — [NEW, G023 · money-path] :31987 — Mouser/DigiKey API pricing runs multi-second remote calls with NO bg-task (lost if user leaves) and failure → 6s toast + console.error only (:32095), no bgError/logDebugEntry. FIX: wrap in bgStart(_bgKey(projectId,panel.id)) at :32014 + bgError+logDebugEntry on catch; mirror runPricingOnPanel :31336/:31826. — reported via Intake (source: Coach G023 → Freddy)
- [2026-08-04] BUG — **B089** "buildAndAttachPdf (BC drawing attach) drops on leave + weak failure surface" — [NEW, G023 · money-path/BC doc] :28808 — no bg-task; failure → 6s toast + console.error, no logDebugEntry (:28996). FIX: register bg-task + bgError/logDebugEntry; mirror relinkToBC :43031 + addFiles logDebugEntry :28025. — reported via Intake (source: Coach G023 → Freddy)
- [2026-08-04] BUG — **B090** "archiveProject silently loses subcollections on fetch failure + interrupt leaves invisible orphan" — [NEW, G023 · data-integrity] :11594 — ECO/snapshot fetch failures swallowed to console.warn (:11605/:11612) → archive proceeds MISSING subcollections without telling the user; interrupt leaves `_archiveComplete:false` orphan silently. FIX: surface fetch failures (don't archive-partial silently); consider bg-task for interrupt safety. — reported via Intake (source: Coach G023 → Freddy)
- [2026-08-04] FOLLOW-UP (B082/B083) — G023 residuals: B082 = sell-price BC PATCH failure still silent console.warn (:29082) though the drop-on-leave half is fixed; B083 = `_flushLeadTimeBcQueue` debounce/visibilitychange paths silent console.warn (:30476) + no unmount flush. Fold into their tickets. — (source: Coach G023)
- [2026-08-04] FOLLOW-UP (G023) — RFQ send / `rfq_history` WRITE path not traced this session (read helpers swallow to console.warn :2781/2788). Queue a focused trace pass. — (source: Coach G023)
- [2026-08-04] ✅ **B087 — BUILT (branch `marc/b087-board-stale` `8ee6cfce`), awaiting Jon deploy go.** onChange fires unconditionally (board list updates after leave) + handleChange focus-safe (setOpenProject only if it's the open project). Display-only, validators clean.

- [2026-08-04] ✅ **B088 + B089 — SHIPPED prod v1.24.88, Coach APPROVE** (F075 Get-Prices + BC drawing-attach survive leave + fail loudly). ⏳ Jon live-verify owed. Ready to promote to TODO.md RESOLVED.
- [2026-08-04] FOLLOW-UP (B088) — [LOW · Coach nit, non-blocking] `runApiPricingOnPanel` has no A2-style re-entrancy guard like `runPricingOnPanel` (:31360); a 2nd concurrent invocation would share `_apBgId` and interleave terminals. Real risk low (button disabled via `aiPricing` while running; not a new hazard — no task existed pre-B088). Consider adding the A2 guard. Also cosmetic: catch calls `_pp(...)`→bgSetPct after bgError (overwrites msg, status stays "error"); + comment line-ref drift. — reported via Intake (source: Coach B088 review → Freddy)

- [2026-08-04] BUG — **B091** "Version 'new version available' modal appears twice (single tab)" — [TABLED — Jon low-priority 2026-08-04, monitoring] Since a recent update, accepting the first version modal reliably produces a second identical one; second accept closes+refreshes. Single tab (Jon confirmed). Investigation RULED OUT: duplicate component (single App :53440 / single render :54725 / single `broadcast` state), HTTP stale (index.html/bundle/sw.js all no-store), SW cache (push-only SW, no fetch handler, deletes caches on activate), snooze timing (4min, correct), id-mismatch (Jon: same version shown). LEADING SUSPECT: dual version sources — `version.json` (deploy.sh) vs `_system/version` (admin-stamped APP_VERSION :54401); the snapshot trigger (:54404) SKIPS on match but never CLEARS a showing modal, so a stale `_system/version` re-pops post-reload. DECISIVE ARTIFACT NEEDED (deferred): version number shown in the 2nd modal (matches current v1.24.88 or older?) + current `_system/version` doc value. Fix candidate: snapshot handler should setBroadcast(null) for a version-broadcast once remote===APP_VERSION + reconcile the two sources. LOW / display-only (data safe). — reported via Intake (source: Jon 2026-08-04)
- [2026-08-04] BUG — **B092** "Just-extracted project with unmatched (blue-BC) rows routes to RFQ SEND/RECEIVE, not (BOM) IN PROCESS" — [NEW, Jon 2026-08-04 · PRJ402505 · money-path/kanban] After extraction, PRJ402505 has many blue-BC-circle rows (blue = 'click to match in BC', i.e. NOT auto-matched/unpriced; :33691) and the project shows in RFQ SEND/RECEIVE. Jon expects: blue-BC / unmatched-unpriced rows → (BOM) IN PROCESS (they need BC matching, not RFQs). Likely `computeProjectEffectiveStatus` (:18662) emits "rfqs" (→ process_rfq col :18494) for unpriced rows that should read as still-in-process. Trace the rfqs-vs-in_progress rule + how blue-BC/unpriced rows factor. Cross-ref B044 (red/unpriced wrongly shown READY). — reported via Intake (source: Jon 2026-08-04)
- [2026-08-04] BUG — **B093** "Extraction BC auto-match missed a broad set of in-BC items (blue-BC circles); Item Browser finds them instantly" — [NEW, Jon 2026-08-04 · PRJ402505 · money-path · likely = known bcFuzzyLookup-miss] Blue-BC rows on PRJ402505 (IDEC SH1B-05C/SH2B-05C/SH3B-05C + ALD2QH211DNU* + AVD302NUR/ASD340NU; Phoenix 3044102/3044128/3047028/0800886/2910386; Schneider RE17RMMW) are ALL in BC (Item Browser finds immediately) but extraction's BC match (runPricingOnPanel BC phase → bcFuzzyLookup) left them unmatched. DEDUP: strongly matches the KNOWN bcFuzzyLookup-miss item (PLAN READY docs/B-bcFuzzyLookup-misses-fix-plan.md — steps 1-4 query only items.number; Item Browser uses field:"both"; Fix 1 = add a post-step-4 bcSearchItems(field:"both") auto-accept on exact normalized-equality). NEW EVIDENCE: the SCALE here (broad miss) — confirm it's the same root cause vs the match phase not running/partially running (cf. the all-red-zero-match note :6285). — reported via Intake (source: Jon 2026-08-04, screenshot)
  → [INVESTIGATED — docs/B092-B093-prj402505-scope.md] ⚠ NOT the known fuzzy-miss (that renders RED; these are BLUE = `bcVerify` UNSTAMPED → the BC-match/pricing PHASE never ran to Phase-6). Fuzzy Fix-1 (cross-field) is already live + WOULD match these parts if the phase ran. Mechanisms: (1) `runPricingOnPanel` early-out on `!_apiKey` :31345 [most likely — extraction is server-side, auto-pricing bails on empty client key]; (1b) pricing threw→rethrown :31985→swallowed :28688; (2) yellow→blue color-gate inversion :33609-33610 (separate real bug). NEEDS DECISIVE LIVE ARTIFACT (Jon re-extract PRJ402505, console: `!!_apiKey`, presence of "BC: Matching…" line, a blue row's `.bcVerify`) before building — mechanism determines the fix. NOT a dup of the fuzzy plan.
  → **B092** [INVESTIGATED] root cause computeProjectEffectiveStatus :18721 `if(hasBom&&(anyRedRow||hasActiveRfqs))return"rfqs"` — `anyRedRow` ALONE (no RFQ sent) flips to RFQ col; unpriced=red. F026/B044 overcorrection. Partly a symptom of B093 (fix B093 → reds clear). Fix = reserve "rfqs" for hasActiveRfqs; red-but-no-RFQ → in_progress. Product-call tradeoff for Jon (should priced-but-red-needing-vendor-quote show in RFQ pre-send?).
  → 🔴 **B093 ROOT CAUSE CONFIRMED via live console/Firestore probe (Freddy, controlled tab, 2026-08-04):** PRJ402505 doc `QWPa6BLuyAFr…` — `bcVerify` is **ABSENT on ALL 26 priceable rows** (8 priced via ARC-Cross/alternates independently; 18 unpriced/blue). Debug Logs show `bcPricing: "18 PN substitution(s) during pricing"` (pricing STARTED) then a cluster of **`FirebaseError [code=resource-exhausted]: Write stream exhausted maximum allowed queued writes`** (20:25–20:43 window). ⇒ **the pricing/BC-match phase ABORTED mid-run on resource-exhausted, BEFORE the Phase-6 bcVerify stamp**, and the throw was SWALLOWED (silent). Matcher/fuzzy is fine (would've matched these). NOT the fuzzy-miss, NOT the color-gate. **This is a B078 RECURRENCE** — the coalescer (B078-2 v1.24.71) did NOT prevent write-queue saturation on this extraction (exactly the SESSION-STATE #3 "watch for recurrence" case). Two fixes: (1) RE-OPEN B078 — resource-exhausted still happens; (2) pricing/BC-match phase must FAIL LOUDLY + retry on resource-exhausted (B078-1/B085-class silent-swallow), not abort silently. Remediation for PRJ402505: re-run pricing (Get Prices/re-price) once — matcher will now match the in-BC parts (Jon-gated, prod write).
  → ⬆ **B078 RE-OPENED (2026-08-04):** resource-exhausted "Write stream exhausted maximum allowed queued writes" recurred on PRJ402505 extraction+pricing (live-confirmed). B078-2 coalescer insufficient for this case; + the pricing phase swallows the resulting throw (silent). Needs re-scope.

- [2026-08-04] BUG — **B094** "Exact cross-field (Vendor Part#) BC matches held back as suggestions on manual-verify panels (not auto-applied)" — [NEW, Jon 2026-08-04 · PRJ402505 · money-path · ROOT CAUSE CONFIRMED live] After the B093 remediation re-price, 3 in-BC parts (49046A→MTX-112370, 679001→MTX-112657, 592273→MTX-112572; OVIVO nameplates/tags, Matrix Systems supplied, flagged "? PN" suspect) stayed unmatched/blue. LIVE console proof: `bcFuzzyLookup` matches ALL THREE EXACTLY via the Vendor_Item_No cross-field step (step 4b). ROOT: the F1 guard (src/app.jsx:31492-31493) `_isExact=result.type==="exact"; _needsReview=!_isExact&&_manualVerifyRequired` — a cross-field match is typed `"fuzzy-crossfield"` (:5872), NOT `"exact"`, so on a manualVerifyRequired panel it's held as a SUGGESTION (fuzzySugg 31496) instead of applied. But the cross-field path ONLY returns on EXACT normalized equality (localNorm===wantNorm) — it's definitively correct, not the OCR-noisy-fuzzy case the guard protects against. NOT about the Matrix Systems supplier. FIX (small): treat a cross-field exact-normalized match as exact for the F1 guard (either bcFuzzyLookup returns type "exact"/a flag for the normalized-equal cross-field case, or the 31493 guard accepts "fuzzy-crossfield"). Money-path → Coach review. — reported via Intake (source: Jon 2026-08-04, Freddy live-confirmed in controlled tab)

- [2026-08-04] BUG — **B095** "BC lead times not pulled on auto-match (extraction/pricing) — only price; rows stay red for missing firm LT" — [NEW, Jon 2026-08-04 · PRJ402505 · money-path] BC items HAVE lead times, but neither extraction nor Get Prices pulled them into the matched BOM rows → nearly every row red (missing firm lead time). Selecting the item via BC Item Browser (commitBcItem) DOES stamp leadTimeDays + clears the red. Jon: LT should be pulled when the item matches BC (CLAUDE.md Item Lead Times: fetch "piggybacks on runPricingOnPanel", precedence supplier→scraper→bc_vendor→bc_item→ai). Jon hesitant to "Push Lead Times to BC" (would push empty LTs). HYPOTHESES: (a) the LT-fetch piggyback in runPricingOnPanel didn't run (same B078 phase-abort as B093) even on re-price; (b) the auto-match apply path (bcMap stamping ~:31500) sets price/bcNo but NOT leadTimeDays, unlike commitBcItem; (c) "Get Prices" (runApiPricingOnPanel API) ≠ the BC-match phase, so it wouldn't pull BC LT. Freddy live-investigating in controlled tab + code trace. — reported via Intake (source: Jon 2026-08-04)
  → [LIVE ARTIFACTS — Freddy controlled-tab probe, PRJ402505] 26 priceable rows: **22 priceSource=bc but NO bcNo + no LT**; **3 bcNo-bound but STILL no LT**; 1 complete (bcNo + LT=30, src bc_vendor). All vendor V00261. `bcFuzzyLookup('SH1B-05C')`→MTX-117010/V00261 but **`bcLookupLeadTime('MTX-117010','V00261')` returns null** — auto-fetch LT source has nothing, yet Item Browser (commitBcItem) DOES pull an LT → **commitBcItem uses a DIFFERENT LT source than the auto-match/bcLookupLeadTime.** Two-part: (1) auto-match prices without binding bcNo for 22/26 → LT can't be looked up; (2) even bcNo-bound rows get no LT + the LT source diverges from commitBcItem's. Coach code-trace lane running (commitBcItem LT source vs the piggyback; fix = mirror commitBcItem in auto-match + bind bcNo). Also verify the Push-Lead-Times blank-LT guard (Jon's concern re pushing empty LTs).
  → [ROOT CAUSE CONFIRMED — Coach trace] TWO divergences from commitBcItem, both break the vendor-catalog LT hit: (1) **no bcNo bind:** the re-price already-bc branch (:31448-54) short-circuits `if(priceSource==='bc'){bcLookupItem(pn) exact-only; continue;}` — for rows whose extracted PN ≠ BC surrogate No (MTX-…), bcLookupItem misses + the row NEVER reaches bcFuzzyLookup rebinding → 22/26 stuck with no bcNo → LT keyed on raw PN → miss. (2) **wrong vendor:** the LT piggyback (:31549-96) looks up `bcLookupItemVendorLeadTime(bcKey, row.bcVendorNo=V00261)` — but commitBcItem (via _drainLtQueue :25632-36) uses the item's PRIMARY vendor `bcGetItemVendorNo(itemNo)`, which HAS the LT in ItemVendorCatalog. V00261 (uniform across all rows) has no LT record → null. FIX (mirror Item Browser, additive read-only): (1) on the already-bc branch, when bcLookupItem misses + no bcNo, fall through to bcFuzzyLookup + stamp bcNo (behind the _manualVerifyRequired F1 guard); (2) in the LT loop, fall back to `bcGetItemVendorNo(bcKey)` primary vendor before the ItemCard lookup (mirror _drainLtQueue :25632-36 / commitBcItem :30693). Anchors: bcLookupItemVendorLeadTime :5531, bcGetItemVendorNo :7321. Sequence AFTER B078-3 (same runPricingOnPanel region). ✅ **Jon's "Push empty LTs?" — NO, double-guarded:** pushAllLeadTimesToBc only enrolls `ld>0` (:31224-32) + bcUpsertItemVendorLeadTime hard-rejects `<=0` (:5438-39) → null-LT rows skipped, no blank-write risk. Push is SAFE.

<!-- Triage log:
     2026-07-02 — G001 (Allow-Once → Verified/not-fixable), B001 (trailing-dot redirect URI, LOW), B002 (approved-state TR block message, LOW) promoted by Freddy.
     2026-07-03 — G005 (matrix-arc-test shares PROD Firestore), B003 (Review-Supplier-Quote modal lists unquoted parts), B004 (portal-Apply unawaited-save reload-race → RESOLVED 41824f6c / shipped v1.21.25), B005 (resolved-TR-row can't re-arm, LOW/tuning) promoted by Freddy at #199 close-out.
     2026-07-06 — F004 (Portal submit confirmation shows ARC user + email-copy notice) promoted to TODO.md Features [Backlog] by Freddy. Also back-filled F001/F002/F003 into the Features tracker (were only in SESSION-STATE + docs).
     2026-07-06 — F005 (Print-Only button in locked-quote blocker overlay) promoted to TODO.md Features [Backlog] by Freddy (sibling to #196).
     2026-07-06 — F006 (Qv.## Hist. button — per-quote send history w/ document previews) promoted to TODO.md Features [Backlog] by Freddy (feed = #193 qvHistory; distinct from #194 global metrics).
     2026-07-07 — B008 (RFQ History "Supplier Portal" link opens pre-submission portal, not submitted) promoted to TODO.md Bugs [Backlog] by Freddy (classified BUG: link points at wrong state).
     2026-07-07 — B010 (manual Upload-Supplier-Quote undefined crash) + B011 (harden other supplierQuotes sites) + F007/F008 + G006 (portal loading copy, cosmetic) promoted by Freddy.
     2026-07-07 — F007 (order projects by last-accessed) + F008 (new-version-available refresh notification) promoted to TODO.md Features [Backlog] by Freddy.
     2026-07-07 — F007 (Order main-page projects by last-accessed, most-recent on top) promoted to TODO.md Features [Backlog] by Freddy (distinct from #200 color-by-quota-age).
     2026-07-08 — B013 (BC-401 hidden; from the "61 items could not be pushed" bullet — root cause OAuth token expiry, NOT G005) + B014 (Codale multi-result parse-gap; from the "5 Codale no pricing" bullet) INBOX bullets cleared (both stamped earlier @9a511e07). G007 (remove leftover TEST upload section) promoted to General by Freddy at close-out. INBOX now empty.
     2026-07-09 — F009 (Debug Mode — persistent cross-user activity tracing; parked behind the B012 hard-lock work) + F010 (Suggested alternates — multi-alternate ALT picker + supplier-portal alternate sub-lines) promoted to TODO.md Features [Backlog] by Freddy; both bullets cleared. STILL HELD for a full triage pass: BUG "Login 'Continue with Microsoft' misleads existing users" (2026-07-09, source Jon) — needs B-number + dedup.
     2026-07-10 — All 4 pending bullets TRIAGED + promoted by Freddy (Dez cleared bullets per Freddy's request): F014 (Customer-specific payment-terms note on ALL quotes + BC storage field — CRITICAL, classed FEAT, slotted TOP of post-P1 queue; Jon-flagged critical, Dez proactive-surfaced) · B022 (Login "Continue with Microsoft" misleads existing users, MED) · B023 (Quote Summary line-row overflow clips total price → truncate "IN PRE-REVIEW"→"IN ETR", LOW/cosmetic) · BOM-row-RED-despite-reqs on PRJ402100 3044102 (1-cent clears) FOLDED INTO B018 (decisive repro added; same _isBomRowFlaggedRed priceDate-staleness root; no new number). INBOX now empty.
     2026-07-10 — ★ NEW CATEGORY established by Freddy: METRICS (prefix M) added to NUMBERING-CONVENTION.md (own sequence + ranking, same pattern as B/F/G). Use METRICS/M at intake going forward for analytics/engagement items (Freddy stamps M###). First item promoted: M001 (user engagement telemetry — page-access / activity cadence / per-project time) → TODO.md [Backlog], scoped by Freddy (privacy guard: cadence-not-verbatim-keystrokes; overlaps F009 capture pipeline — design decides shared-vs-standalone). Dez cleared the M001 bullet. INBOX now empty.
     2026-07-23 — F041 (Primary/Secondary supplier matrix — BOM items default to PRIMARY supplier sourcing; Purchasing may source from a secondary) stamped + captured by Freddy (source: Jon). Money-path (supplier/pricing/BC). In ANALYSIS (Coach lane tracing current model + BC capability) — NOT yet promoted to TODO.md as a build item; awaiting synthesis + Jon direction on the Sales↔Purchasing flow.
     2026-07-23 — F042 (To-Do pane NEEDS ATTENTION list: show ALL projects incl. yellow/green sorted by time — not just red; sort by Requested Ship Date + Est. Prod. Done date + untouched-duration) stamped + captured by Freddy (source: Jon). Dashboard UI, low-stakes. In SCOPING (Marc lane) — NOT yet promoted to TODO.md build item; awaiting scope report + Jon sort-tiebreak confirm.
     2026-07-23 — G016 (shrink top To-Do status tiles) BUILT + shipped Test V.040 (source: Jon, live). G017 (To-Do side pane scroll separate from main page — likely the F030 MY DASHBOARD page; standing rail already scrolls independently) + F043 (hover right-pane tile → highlight corresponding project on main screen; F034-family) stamped + captured by Freddy (source: Jon). G017/F043 awaiting Jon surface-confirm before build/scope.
     2026-07-23 — 🚨 PRJ402119 PRICING INCIDENT (money-path emergency, source: Jon). Quote sent with wrong pricing/red rows ($6000 item entered as $12). Freddy stamped: **F044** (Hard-Fix 1: block Quote send if ANY red row, any reason) · **F045** (Hard-Fix 2: Budgetary checkbox = Manager/Admin only, greyed + tooltip for others, + rules guard) · **F046** (Hard-Fix 3: full user audit trail — trace all mutation paths by user) · **F047** (Soft-Fix 1: Priced-Item hover shows the user who approved the BC price; depends on F046). Plus an INVESTIGATION (trace who priced Line 3 Rows 4 & 54 + correlate with the BC-write-failure window) — Coach lane running (code feasibility), live-data trace pending Jon. NOT promoted to build; awaiting investigation + Jon priority.
     2026-07-23 — 🚨 PRJ402119 incident cont'd (source: Jon): BOM-lock requirement. Freddy stamped **F048** (lock a sent BOM completely — qty/price/lead-time/vendor — + suppress auto-pricing incl. auto-check on project open; the only sanctioned post-send change is via F049) · **F049** (PO-receipt modal: quoted-BOM cost vs current-BOM cost, salesman confirms BEFORE accepting the customer PO; prerequisite = capture a quoted-BOM snapshot at send, which may not exist today). Coach lane #2 investigating whether sent BOMs lock today + whether a quoted snapshot exists to check if PRJ402119's BOM changed post-send. Possible actual root cause of the incident. NOT promoted to build; awaiting investigation + Jon priority.
     2026-07-23 — G018 (Debug Logs viewer capped at 500 rows / can't reach older entries — reaches only ~July 14 despite older logs likely still in Firestore; add date-window filter or pagination) stamped + captured by Freddy (source: Jon, surfaced during PRJ402119 investigation). Also NEW fix candidate (unnumbered, pending Jon go): pricing PLAUSIBILITY / quoted-vs-current divergence check — the real gap that catches a wrong-magnitude price regardless of source (block-on-red does NOT). To be numbered when Jon greenlights the reframed build order. F045 (budgetary manager-gate) BUILT client-side, on Test V.044.
     2026-07-23 — Jon greenlit the reframed ASAP order (all 5 decisions). Freddy stamped **B052** (5-min BC poll divergence guard — the PRJ402119 root-cause fix: don't auto-apply a large downward BC price swing; keep current price, flag `bcPollDivergence`, log durably) — BUILT, on Test V.045, Coach reviewing. **F050** (send-time price plausibility / divergence check — belt-and-suspenders, may consume the B052 flag). Build order: B052 → F050 → F044 → F048 → F049 → F045 server-hoist.
     2026-07-23 — F051 (send-time freshness-through-validity gate: force new pricing+lead-time if any row would go stale within the quote's validity window; `priceDate+staleDays < sendDate+validity`) stamped by Freddy (source: Jon). Pairs with the sent-lock-vs-staleness fix (F048-adjacent) into one coherent quote-freshness/lock policy. Send-gate (hard block). Scope folded with the staleness trace.
     2026-07-23 — F051 EXPANDED (Jon): also check ON PROJECT OPEN (assume send-today) → notice + push Get New Pricing for items that'd expire within validity; plus the send-time hard block. F052 (Jon) stamped: expired-quote handling at PO receipt — PoReceivedModal warns if quote expired + two paths (accept-as-is w/ manual order adjustment to match PO amount; or re-quote → resend → request updated PO). Pairs with F049. All part of the "Quote Lifecycle & Lock" epic (F048+F049+F051+F052+staleness-suppress); scoping as one combined sequenced plan.
     2026-07-23 — B053 (Jon) stamped: auto-price-check-on-open (a) confirmed as the mechanism that bumped sent quotes' rev → IN-PROCESS demotion; (b) repeat-nag bug — opening the same project 3× re-shows the same "Accept new prices" modal for the same items (phantom diff / no persistence / no dismissal). Jon: feature gets in the way more than helps. Coach diagnosing root cause + sane-behavior recommendation. Related to F048 (suppress on locked) but distinct (unlocked-quote repeat-nag + possible rethink of on-open auto-check).
     2026-07-23 — F060 (Jon, live) stamped + BUILDING (Marc lane): To-Do / Dashboard restructure (DISPLAY-ONLY). (A) Side-panel To-Do rail: NEEDS ATTENTION capped to top 5 + clean QUOTES SENT EXCLUDED (Jon decision: option 1 of 3 — sent-clean belongs in the new expiring list, not Needs Attention) + NEW "QUOTES EXPIRING" list below (top 5 sent quotes within 7 days of quoteExpiresAt, incl. already-expired, soonest-first). (B) Dashboard MY DASHBOARD page: REMOVE the Email column (noise, not useful yet — underlying outlook state left intact); REPLACE "MY PROJECTS" list with QUOTES EXPIRING; THREE EQUAL columns (NEEDS ATTENTION | QUOTES EXPIRING | ARC NOTIFICATIONS, flex:1 1 0); add total price (_projectSellTotal = sell/quote total via computeBasePanelSellPrice) to the NEEDS ATTENTION + QUOTES EXPIRING rows (dashboard/pageMode only, not the narrow rail). New shared helpers: _projectSellTotal + _quotesExpiring. Origin: the NEEDS-ATTENTION-shows-clean-QUOTES-SENT finding (F042 dropped the old yellow+red-only filter → showed ALL bucketed projects). ✅ BUILT (Marc lane) + FREDDY-REVIEWED (diff verified data-safe display-only; _hlBoardTile scope confirmed @:45385 F043 helper; helpers pure) + Freddy added a dashboard empty-state ("✓ No quotes expiring soon.") so the column stays balanced when empty (rail still hides it). validate_jsx → JSX OK. ALSO folded in: service-card LINE-ITEM status pill removed (:35035, F058 sibling; Quote Summary sc.status Badge @:37279 kept). Committed to master, NOT deployed — awaiting Jon deploy go. Minor note: totals use _projectSellTotal (SELL/quote total, not the old MY-PROJECTS Σqty*unitPrice COST) — intentional (quote value is more meaningful for these lists).
     2026-07-23 — F061 (Jon, live) stamped + BUILT (reviewing→deploy): remove the ACTIVE ECO status-column filter from the main board so ECO projects fall into their REAL status columns (RFQs/In Process/etc.) and follow the normal process (they have RFQs) — KEEP the red ECO tile border for quick recognition. Reverses F024. Edits: computeProjectEffectiveStatus dropped the `if(computeActiveEco)return"active_eco"` short-circuit (~:16955); _todoBucketOf (:16779) + _todoClockStart (:16812) now use the real status (activeEco kept only for the Won/PO carve-out); ACTIVE ECO column removed from the main board order+labels (:45760/61) + the rail Sales-Pipeline buckets (:45301). RED BORDER UNTOUCHED — verified it's driven independently by `_hasActiveEcoTile=!!computeActiveEco(p)` at the tile render (:47211), NOT by status==="active_eco". validate_jsx → JSX OK. + F060 REFINEMENTS same batch (Jon live): (a) Dashboard NEEDS ATTENTION shows the FULL list (top-5 cap now rail-only); (b) rail QUOTES EXPIRING shows the "✓ No quotes expiring soon." placeholder when empty (was hidden); (c) rail NEEDS ATTENTION count moved to the RIGHT of the header as "5 of 15" and made a LINK to MY DASHBOARD (new onOpenDashboard prop → setView('dashboard')+setNavTab('user_dashboard')); bottom footer removed. RFQ LABEL — RESOLVED (Jon): rail "Pending RFQs" → "RFQs to Send" (one edit at _todoBuckets :45297 → flows to both the Sales-Pipeline pill AND the attention-row label via _bucketLabels :45330). Board column label left as "RFQs Send/Receive" (Jon scoped the rename to the rail). Jon's model: base = "RFQs to Send"; when quotes come back = "RFQs to Accept" → see F062.
     2026-07-23 — F062 (Jon, live) stamped — FOLLOW-UP (needs data plumbing, NOT yet built): dynamic rail RFQ label — flip "RFQs to Send" → "RFQs to Accept" per project when supplier quotes have RETURNED and await acceptance. BLOCKER: the To-Do rail only has project/BOM data; "received awaiting acceptance" = an rfqUploads submission with status==="submitted" (vs "imported"=applied), which lives in a SEPARATE collection loaded per-project (ProjectView portalSubmissions listener :38496) or globally in RFQ History (subByToken, received-count logic :45208-45215) — NOT in the rail's `projects` prop. From the rail's data alone a sent-awaiting row and a returned-awaiting-acceptance row are identical (both rfqSentDate + no price). FIX PATH: plumb a per-project "hasSubmitted-unapplied quote" flag into the rail/dashboard data (app-level rfqUploads rollup keyed by projectId, or extend the projects load) → then the attention/pill label reads "RFQs to Accept" when received>0, else "RFQs to Send". Scope + build separately.
     2026-07-23 — F059 (Jon, live) stamped — PARKED mid-build (superseded by F060 thread): "Mark Committed" re-anchor — a sent+diverged quote (quoteRev>quoteSentRev, e.g. 402119 Qv.03 vs sent Qv.02) gets a button that re-anchors quoteSentRev=quoteRev (+ quoteRevAtPrint) WITHOUT emailing, moving it to the locked Quotes-Sent column. Jon chose the SIMPLE quoteSentRev-bump approach (accepts the "sent Qv.02" history line then reads Qv.03) + a lightweight committedWithoutResend audit stamp recording the customer's true rev. Build pattern confirmed: mirror the send-anchor write (:34662/:34683 uses safeSave(uid,{...upd,_sendAnchorWrite:true}) to avoid a rev-bump), MINUS the email/quoteSentAt/expiry changes. Button placement: Quote Summary pane near the "Quote sent Qv.NN" line (:37670), shown when quoteSentAt && quoteRev>quoteSentRev. RESUME after F060. ★ SCOPE CONFIRMED MULTI-PROJECT (Jon 2026-07-23): affects at least PRJ402131 + PRJ402119 (+ the known IN-PROCESS 402124/402109/402092) — sent quotes rev-diverging back to IN PROCESS; 402131 newly appeared IN PROCESS after a refresh (Jon says he didn't intend to edit it → possible residual spurious-bump vector even with auto-pricing off — INVESTIGATE; permanent prevention = F048 lock-sent-BOM). F059 = the cleanup mechanism; F048 = the prevention. ✅ BUILT 2026-07-23 (v-pending-deploy): "✓ Mark Committed (no re-send)" button in the Quote Summary pane (inserted after the "✓ Quote sent Qv.NN" line ~:37693), shown only when quoteSentAt && quoteRev>quoteSentRev. Handler mirrors the send-anchor write EXACTLY (update(upd) optimistic + safeSave(uid,{...upd,_sendAnchorWrite:true}) → no rev-bump per B034 @:9406), MINUS the email — sets quoteRev=quoteSentRev=quoteRevAtPrint=rev + quoteLocked + additive committedWithoutResend audit {at,by,byName,customerRev(the OLD quoteSentRev),committedAtRev}. quoteSentAt/quoteSentTo/quoteExpiresAt UNTOUCHED (customer copy unchanged). Owner-priority gated + a single arcConfirm (deliberate state change). Note (Jon-accepted): the "✓ Quote sent Qv.NN" line will then read the NEW rev (quoteSentRev bumped) — the committedWithoutResend audit preserves the true customer rev in data. validate_jsx → JSX OK; update() confirmed @:39356. Clears the 402131/402119/etc. IN-PROCESS backlog. Awaiting Jon deploy + live verify (click on 402119 → moves to Quotes Sent). ⚠ BUG in v1.24.23 (Jon: "did not work") — FIXED: the button's onClick called `update()` INSIDE PanelListView, where update() is a ProjectView-LOCAL fn (out of scope) → ReferenceError on click. validate_jsx (syntax-only) missed it; tools/check-scope.js catches it (was a NEW violation in the v1.24.23 deploy output, unread). FIX: button now delegates to a new onMarkCommitted PROP; the actual re-anchor (update+safeSave _sendAnchorWrite) is implemented in ProjectView at the PanelListView render (mirrors the adjacent onMarkLost handler :40574). check-scope → No NEW violations. LESSON: read the check-scope.js line on EVERY deploy (out-of-scope refs pass validate_jsx but crash at runtime).
     2026-07-23 — F058 (Jon, live) stamped + BUILT + shipped: REMOVE the per-line-card status pill (:29392, was <Badge status={computeProjectEffectiveStatus({panels:[panel]})}/>). Root cause of Jon's status-disconnect report: the line card computed status from a SINGLE-PANEL projection {panels:[panel]} that DROPS project-level quoteSentAt/quoteRev/quoteSentRev → the "already-sent" short-circuit never fired → it showed pre-send readiness (READY TO SEND/RFQS) on an already-SENT+diverged quote, contradicting the QUOTE SUMMARY pane (:37309 = computeProjectEffectiveStatus(project) = TRUTH = IN PROCESS). Jon chose REMOVE (vs. fix-to-match) — rely on QUOTE SUMMARY for status. NOTE latent design point: line-card status was per-PANEL, Quote Summary is per-PROJECT — they were always different by design; the glaring wrong was pre-send states shown post-send. If per-line readiness is wanted back later, feed the quote-send context into the projection (fix-to-match) instead of the removed pill.
     2026-07-23 — F057 EXTENDED (Jon, live): "CS" display now also covers VENDOR=CUSTOMER $0 rows, not just the customerSupplied flag. Discovered via PRJ402119 Row 53 (vendor OVIVO = customer OVIVO) — ARC's OTHER model for "customer provides this part." Condition in both cells (unit-price :30784 + line-total :30801): `row.customerSupplied || (row.unitPrice!=null && +row.unitPrice===0 && _vendorMatchesCustomer(row.bcVendorNo,row.bcVendorName,project.bcCustomerNumber,project.bcCustomerName))`. Reuses the SAME predicate the red-rule uses (SSOT — display + red-exemption can't drift). Behavior: a $0 (explicit) vendor=customer row shows non-editable "CS"; a vendor=customer row with price>0 shows the real (editable) price; an UNPRICED (null) vendor=customer row still shows "—"/editable so a real pricing gap isn't hidden. NOTE: the null-guard (`unitPrice!=null`) was added in a fast-follow after the advisory review caught that `+null===0` would false-positive "CS" on unpriced rows — shipped v1.24.19 without it, v1.24.20 with it. validate_jsx.js → JSX OK.
     2026-07-23 — F051j (Jon) — Expire Junk BC Prices tool BUILT (Marc lane) + COACH-REVIEWED (verdict BLOCK — functional/accountability, NOT data-safety: Coach confirmed it CANNOT corrupt a legit price — PATCHes each record's own scanned @odata.editLink verbatim, bad URL 404s, never key-guesses; does NOT repeat the bcPushPurchasePrice single-key bug). Scope: scan PurchasePrices Direct_Unit_Cost≤25, group by exact cost, suspect=≥8 distinct Item_No OR cost==0.71 (pre-checked); expire=PATCH Ending_Date via editLink+If-Match; audit→companies/{cid}/bcPurchasePriceExpiries; preview-first (scan→check→arcConfirm→expire); yesterday default; admin card in APISetupModal. **B2 FIXED this pass:** added firestore.rules bcPurchasePriceExpiries block (isAdminMember create, field `by`, immutable) — was default-denied+swallowed → zero audit; needs `firebase deploy --only firestore:rules`. **B1 OPEN (the real gate):** editLink mechanism UNPROVEN on this tenant — FIX #182 (bcPushLeadTime :4779) found @odata.id/editLink NULL here → builds keys manually. If null under metadata=full → tool inert (all "not patchable"); if editLink carries a Company() segment → doubled → 404s. RESOLUTION: deploy scan-first (read-only, safe); Jon runs SCAN → notPatchableCount tells us. If patchable → expire OK. If not → rebuild PATCH with manual composite key (#182 pattern; needs full key: Item_No,Vendor_No,Currency_Code,Starting_Date,Variant_Code,Unit_of_Measure_Code,Minimum_Quantity — scan $select currently only pulls a subset). **M-fixes DEFERRED to expire-readiness pass (scan is read-only, don't need them):** M1 per-part visibility (expandable Item_No list before expiring), M2 clamp Ending_Date≥Starting_Date (recent junk → BC 400), M3 test-env fake-200 → phantom success + prod-Firestore pollution (detect _testEnvBlocked). Jon greenlit: B2+deploy+he scans. validate_jsx + check-scope both clean. ★ SCAN RESULT (v1.24.26, Jon live): scan FAILED — "unparseable JSON at position 235". Raw response (diagnostic surfaced): `{"@odata.context":".../ODataV4/$metadata#Company('Matrix Systems LLC')/PurchasePrices","value":[{"@odata.type":"#NAV.PurchasePrices"…` → this is LEGACY NAV-style OData (`/ODataV4/`, `#NAV.*` types) which CHOKES on `odata.metadata=full` (dies right after the @odata.type annotation). ROOT: metadata=full is unsupported here. This ALSO confirms B1 — no editLink under normal metadata (matches FIX #182 for ItemVendorCatalog). REQUIRED REBUILD (both fixes converge on #182's proven pattern): (1) scan uses default `Accept: application/json` (drop metadata=full); (2) $select the FULL NAV PurchasePrice compound key (Item_No,Vendor_No,Currency_Code,Starting_Date,Variant_Code,Unit_of_Measure_Code,Minimum_Quantity) + Direct_Unit_Cost,Ending_Date + capture @odata.etag; (3) build the PATCH key URL MANUALLY like bcPushLeadTime #182 (:4782) `PurchasePrices(Item_No='..',Vendor_No='..',Currency_Code='..',Starting_Date=YYYY-MM-DD,Variant_Code='..',Unit_of_Measure_Code='..',Minimum_Quantity=N)`; (4) NEW safety beyond #182 — read-only GET the constructed key URL FIRST to verify the key format resolves before any PATCH (gates expire; no blind writes). Then PATCH + If-Match etag, body {Ending_Date}. Fold in M1/M2/M3 with this pass. Awaiting Jon go on the rebuild. ✅ REBUILT (Marc lane, Jon chose A): scan now uses default metadata + full compound-key $select + builds record.keyUrl via _bcPpKeyUrl (#182 shape); verifyJunkKeyFormat does a READ-ONLY GET of the first record's keyUrl → keyVerified; expire PATCHes rec.keyUrl (+If-Match etag), M2 clamps Ending≥Starting, M3 catches _testEnvBlocked. Modal: canExpire=anyChecked&&keyOk&&!IS_TEST_ENV (write impossible unless the live key-verify passed + not Test), M1 per-group expandable Item/Vendor/date list, green "key verified" pill / red disabled banner on fail. Freddy reviewed diff; validate_jsx + check-scope clean. Coach's static re-review INTENTIONALLY skipped — the live read-only key-verify is a stronger check of the composite-key correctness than code-reading (only the tenant can confirm the 7-part key). Deploying scan; Jon re-scans → if key verifies, he can expire. ✅✅ DONE (v1.24.28, Jon live "Worked"): scan found 737 candidates / 2 suspect groups — $0.71 (95 records / 81 parts) + $1.24 (15 / 12); key-verify passed; expire initially 409'd on all 110 (NAV list carries no usable etag + If-Match:"*" rejected) → FIXED by fetching a FRESH etag per record via a single GET before each PATCH → expire succeeded, junk EXPIRED (Ending_Date set) + audited to bcPurchasePriceExpiries. Also reworded banner "PRODUCTION"→"connected BC database" (tenant URL is MATR_SndBx_01152026 — a SANDBOX). ⚠ OPEN: this cleaned the CONNECTED BC (the sandbox). If ARC's real/production BC is a DIFFERENT environment, it still has the $0.71/$1.24 junk → re-run the tool connected to that env. Jon (2026-07-23): the sandbox (MATR_SndBx) is the current working DB; a BATCH MOVE of the new Item DB → the other (prod) DB is planned. So: cleaning the sandbox junk NOW means the move carries clean PurchasePrices IF the move includes them; if the target DB's PurchasePrices are separate, re-run F051j connected to that env after/before the move. Tool is reusable + preview-first for that pass. PREVENTION still queued: B057 (write path should expire the superseded price so junk can't re-accumulate) + F041 (primary-vendor pick) before auto-pricing is re-enabled.
     2026-07-23 — B057 (Jon, live — SYSTEMIC, prevention) stamped: the BC PurchasePrice WRITE PATH doesn't retire the superseded price. Jon: "this is supposed to be happening anytime a new price is entered — it should be setting the old price's Ending Date so it's expired." Two linked defects in bcPushPurchasePrice (:5519): (a) the update PATCHes with a SINGLE-segment key `PurchasePrices('ITEMNO')` (:5546) on a COMPOSITE-keyed entity (Item+Vendor+Starting_Date) → the PATCH can misfire → it falls through to POST a NEW record, so multiple dated PurchasePrice rows accumulate per item, NONE expired; (b) it never sets Ending_Date on the prior/superseded record. Result: old + garbage prices pile up as "active," and bcFetchPurchasePrices' most-recent-Starting_Date selection can grab the junk (root mechanism of the $0.71 accumulation). FIX (when pricing re-enabled): correct the PATCH to target the exact record via @odata.editLink (like the F051j expire tool) AND, on a genuine new price, stamp Ending_Date on the superseded record so only the current price is active. Pairs with F041 (primary-vendor pick) + F051j (one-time cleanup). Currently MOOT (scraper write-back + auto-pricing disabled) — this is the re-enable-prerequisite prevention. → BUILDING now (Jon: "do B057 now", 2026-07-23): Marc lane rewriting bcPushPurchasePrice to (1) fix the composite-key PATCH via _bcPpKeyUrl + fresh-etag (F051j-proven), (2) supersede semantics — same-day Starting_Date → update cost in place; OLDER active → set Ending_Date=(pushdate−1) to expire + POST new; UoM-scoped so it never clobbers a different price line. High blast radius (~14 active callers: portal-apply/DigiKey/Mouser/manual/alternates) → Coach review + Jon controlled-test-push before trusted live. ⛔ B057 BUILT (Marc lane) + COACH-REVIEWED → BLOCK (3 ship-blockers, 2 = silent data loss). NOT committed/deployed. F1 (data loss): expire scope keys on UoM ONLY → would expire a legit different-currency/variant/volume-break line at the same UoM; FIX = scope to the full lane ARC writes (UoM + Currency='' + Variant='' + MinQty=0). F2 (price-less item): expire-then-POST → POST failure leaves item with NO active price; worse, bcFetchPurchasePrices (F041) ignores Ending_Date → ARC shows expired cost while BC sees none; FIX = POST-new-FIRST, expire only on cr.ok (both main + same-day paths). F3 (no-ops for scrapers): blank-UoM callers (:29079/:29190/:33574) — BC fills base UoM on the created record but scope looks for blank → never matches → keeps POSTing dups (defeats the fix for the exact scraper accumulators); FIX = scope-UoM must == written-UoM (resolve base UoM or require concrete UoM) + NEEDS a live BC fact: what UoM does BC persist when POST omits Unit_of_Measure_Code? F4 (med): same-day branch returns without running the expire loop → other older-active records never expired; FIX = run expire loop after same-day update too (idempotent). F5 (verify): un-expire via Ending_Date:"0001-01-01" is a NEW op (F051j only ever SET ending) — verify BC accepts it live. Coach cleared Q4-Q7 (concurrency semaphore-bounded, caller contract preserved, date math sound, data-safety OK). ★ Freddy REC: PARK — non-urgent (scraper write-back already killed, nothing accumulating), delicate high-blast-radius money-path deserves a fresh pass (F1/F2/F4 clean; F3 needs a live BC-UoM fact + design choice; then Coach re-review + Jon test push). Jon chose PUSH (2026-07-23). FIXES APPLIED: F1 → scope now full-lane (UoM===bcUom && Currency==='' && Variant==='' && MinQty===0) — can't expire a foreign-currency/variant/volume-break line. F2 → reordered POST-new-FIRST then expire-only-on-cr.ok (POST failure leaves old price active, never price-less). F4 → same-day path now runs _expireOlderActive() after the in-place update. F3 → blank-UoM safely no-ops (full-lane ''-scope won't match BC's base-UoM records → no over-expire) + console.warn; active callers pass concrete UoM so they get the full fix; documented limitation (concrete UoM needed before re-enabling blank-UoM scrapers). F5 → un-expire sentinel "0001-01-01" kept, verify-live (low-freq same-day-re-push-after-expire). Factored a shared _expireOlderActive() closure used by both paths. validate_jsx + check-scope clean. AUTHORITATIVE verification = Jon controlled test push (not a 3rd review — fixes are direct Coach-prescribed): push a price on a test item that has an OLDER existing PurchasePrice → confirm BC ends with the old record Ending_Date=yesterday + the new record active, and other UoM/currency lines untouched. ✅✅ DONE + VERIFIED (v1.24.29, Jon test push "Success"): supersede works — old expired, new active, no dup, no over-expire. B057 CLOSED.
     2026-07-23 — F041 STATUS (Jon: "And F041"): CORE ALREADY LIVE (bcFetchPurchasePrices preferredVendors :5570/5609 + runPricingBackground :16006 + runPricingOnPanel :28826). Remaining = dormant edges only (commitBcItem pass vendorNo, dead-vendor re-verify, 5-min poll, on-open check — all behind gated AUTO_BC_REPRICE) → Freddy to finish after B057 (avoid concurrent app.jsx edits).
     2026-07-23 — F057 (Jon, live) stamped + BUILT (shipped v1.24.18): show "CS" instead of $0.00 for customer-supplied BOM rows, in BOTH the unit-price cell (:30784, new branch beside the labor "— auto" branch; non-editable muted "CS") AND the line-total cell (:30801). Jon scoped it to internal BOM ONLY — NOT the printed customer quote, NOT RFQ/supplier docs. Pure DISPLAY change: stored unitPrice stays 0, red-rule/totals/pricing paths untouched (data-safe, additive). Origin: PRJ402119 Line 3 Row 53 read as $0/no-LT/not-red and confused Jon ("bit me again") — a customer-supplied row is correctly exempt from red (_isExcludedFromPriceCheck :16471) but the $0 display looked like a missing price. validate_jsx.js → JSX OK. Committed to master; NOT deployed (Jon gate + pricing freeze). NOTE: also surfaced a latent red-rule hole — _vendorMatchesCustomer (:16483) fuzzy 3+char-prefix match could false-positive-exempt a real $0 row from red; candidate B-item if it recurs.
     2026-07-23 — B056 (Jon, live — HIGH VALUE, recurring) stamped: trailing-dot hostname (`matrix-arc.web.app.`) breaks the ENTIRE app, not just BC. A dotted FQDN is a DIFFERENT browser origin → separate localStorage/IndexedDB/service-worker/Firebase-auth scope → app loads EMPTY (blank To-Do list, missing status tiles, Firestore write-queue exhausted, "nothing saves") AND BC OAuth redirect_uri mismatch (AADSTS50011) → BC offline / won't reconnect. Cost Jon ~an hour of whack-a-mole this session; recurred (see memory reference_bc_auth_aadsts50011_trailing_dot). FIX: boot-time guard in public/index.html head — `if(location.hostname.endsWith('.')){location.replace(location.href.replace(location.hostname,location.hostname.slice(0,-1)));}` BEFORE the app/bundle loads, so a dotted host self-corrects to the clean origin and the cascade can never start. ~3 lines, origin-safe (only fires on a malformed host), unrelated to the pricing freeze. Build-ready; offer to ship with next deploy. Also: user-side, purge the dotted URL from Chrome autocomplete (Shift+Delete the suggestion) + fix bookmarks/shortcuts.
     2026-07-23 — B055 (Jon, live) stamped: BC Item Browser results table — when MFR + Vendor columns async-populate ("finally update"), the table overflows and pushes the rightmost column (Customer-Supplied toggle) outside the modal; the table/modal does not resize or reflow to fit. Layout/overflow bug in the BC Item Browser results grid. LOW-MED (cosmetic-ish but the toggle becomes unreachable without horizontal scroll).
     2026-07-23 — ✅ SAVE-FAILURE INVESTIGATION — RESOLVED (root cause = STALE EDITING-LEASE GHOST, the known gap #5b bug; NOT a new bug, NOT the kill-switch, NOT data corruption). Crossing a part "not accepting it" + "nothing saved when I closed the tab" were BOTH the single-editor lease (editingBy/editingTabId, LEASE_STALE_MS=90000 @:565): Jon's uncleanly-closed browser left the lease held → reopened session saw a ghost holding it → silently read-only (edit affordances suppressed, writes rejected by the lease guard). Fix in the moment: reload after >90s → tab reclaims the expired lease → editable → cross wrote through (Jon confirmed). PERMANENT FIX ALREADY BUILT + reviewed: branch `gap5b-f015` (BroadcastChannel liveness → instant ghost-adopt, no 90s trap), held only for multi-device verification (G1–G16, needs Jon+Andrew+2nd device). ★ RECOMMEND prioritizing gap5b-f015 verify+ship — this cost Jon real work today.
     2026-07-23 — B054 (Jon, queue-for-later): Auto-assign Suppliers modal suggests vendor "ROYAL - SALT LAKE CITY" (e.g. for PANDUIT) which does NOT exist in BC — should be "Royal Wholesale". Bad/duplicate learned vendor name in manufacturerVendorMap (auto-assign learning). Same vendor-data-quality family as the PRJ402119 incident (sweep also showed "ROYAL - SALT LAKE CITY" as a distinct vendor on 11 rows). NOT urgent — parked; check when there's time (likely a vendor-name normalization/dedup + validate-against-BC-vendor-list fix).
     2026-07-23 — QUICK WINS built + shipping together (Jon: "do the quick wins"): ✅ B056 (trailing-dot boot guard in public/index.html <head> — self-corrects a dotted host before anything loads; Freddy inline). ✅ B055 (BC Item Browser overflow — Marc lane: minWidth:0 on the flex results container so overflow:auto engages horizontally + vendor cell maxWidth:200/ellipsis/title so long async vendor names don't push the far-right "Use" toggle off-screen). ✅ G018 (Debug Logs 500-cap — Marc lane: added a from/to date-window filter to DebugLogsModal — where createdAt >= fromMs / <= toMs, single-field range so no composite index; quick-range buttons + date window are mutually exclusive; reaches entries past the 500-most-recent cap). All display/query-only, validate_jsx + check-scope clean. Freddy reviewed diffs. -->
     2026-07-23 — G019 (Jon, live) stamped: HIDE all Engineering Questions UI (the "## answered" / "N ?" pulsing pill in QUOTE SUMMARY, the questions modal trigger `setEqModalPanelId`, the "Questions for Customer" block on printed quotes, + any dashboard/tile refs) — "develop this out another time." Approach: a single `QUESTIONS_ENABLED=false` flag gating every render surface (reversible — flip to true to restore intact). Display-only, no data change (panel.engineeringQuestions preserved). MUST grep exhaustively (engineeringQuestions / eq / "Questions" / on_quote / EqModal / setEqModalPanelId) so no reference strays through (enumeration is a floor). Queued behind the F044/F046/F047 lane (same file — avoid concurrent-edit clash). ✅ DONE + DEPLOYED: QUESTIONS_ENABLED=false const (near the kill-switches) gates all 6 Questions UI surfaces — Quote Summary pill (→ plain non-clickable Badge), panel button, both EngineeringQuestionsModal renders, printed-quote "Questions for Customer" block + page-count math, send/review gate (eqs issue + eqWarn modal), + a training-tour bullet. Data layer untouched (reversible: flip to true). Grep-confirmed no stray UI refs. NOTE (Marc flagged, out of G019 scope): a SEPARATE showAiQuestions modal exists (fed by panel.aiQuestions, already dead/[] since :15750) — sweep later if desired. validate_jsx + check-scope clean.
     2026-07-23 — F044+F046+F047 cluster ✅ BUILT + Freddy-reviewed + DEPLOYED. F046: per-row priceSetBy/priceSetAt stamped at ~35 price-write sites (BC apply/cross, alternates, templates, contingency, AI, portal, scrapers, manual, poll-accept, ECO w/ base-attribution-inherit, null-on-price-clear) — additive, data-safe, graceful. F047: extended the existing price tooltip → "Priced by {who} · {date} · {source}" with "not recorded" fallback for pre-F046 rows. F044: Send hard-blocks on ANY red row for non-managers (arcAlert+return, buttons disabled); MANAGERS/admins get a destructive force-send arcConfirm (Jon ruling — keep a manager override) with if(!_ok)return; both send paths (QuoteSendModal.handleSend + _doInlineQuoteSend); removed the old auto-flip-to-Budgetary + "Override(Send as Firm)" bypass. Print gate stays soft + "Send Quoted BOM to Customer" stays un-gated (Jon rulings). Limitation: F046/F047 forward-only (can't retro-trace pre-deploy prices). validate_jsx + check-scope clean.
     2026-07-23 — F063 (Jon, live) stamped: (1) REMOVE the QUOTES SENT column from the DEFAULT Sales-board view (clutter once sent) — but make it callable on demand (toggle/filter TBD); (2) add the QUOTES SENT list as its OWN COLUMN on the MY DASHBOARD page (currently 3 equal cols: Needs Attention | Quotes Expiring | ARC Notifications). Display/organization only, low-stakes (F060/F061 family). Touches the board column order/labels (~:45760) + a show/hide affordance + the Dashboard 3-col row (~:50293). DESIGN Qs for Jon: (a) how to "call up" QUOTES SENT on the board — header toggle vs filter chip vs collapsed-expandable column; (b) Dashboard placement — 4th equal column (narrower) vs 2nd row vs replace. Jon (close-out) resolved the design + said build now: (a) a "SHOW SENT" header toggle (default hidden; re-appends the quotes_sent column, status-view only), (b) Dashboard QUOTES SENT column inserted BETWEEN Quotes Expiring and ARC Notifications (→ 4 equal cols). ✅ BUILT (Marc lane) + Freddy-reviewed + DEPLOYED: default board order drops quotes_sent + `if(showSentColumn)order.push`; showSentColumn state + SHOW SENT/HIDE SENT button in board controls; new _quotesSent helper (_isMyProject && _todoBucketOf==="quotes_sent", quoteSentAt desc, uncapped) + TodoRail pageSection="sent" block (dashboard-only) mirroring the expiring column. validate_jsx + check-scope clean.
     2026-07-23 — F064 (Jon, live) stamped: ECO projects (active-ECO / red-border, per F061 now in their REAL status columns) should always sort to the TOP of whatever status column they land in — keeps change-orders prominent. Small sort refinement in the Sales-board column render (pairs with F061). Impl: add active-ECO-first as the primary sort key in the per-column ordering (there's an existing `_priorityPinCompare` from F027 to mirror). Display/sort only, low-stakes. Raised during close-out. ✅ BUILT (Marc lane) + DEPLOYED: `groupProjects` status return now sorts each column `(computeActiveEco(b)?1:0)-(computeActiveEco(a)?1:0)` → ECO-first primary key, stable sort preserves the existing within-column order. validate_jsx + check-scope clean.
     STILL OPEN after 2026-07-23 quick-wins: Quote Lifecycle & Lock epic (F048/F049/F051/F052), F044 (block-send-on-red), F046+F047 (audit trail + hover-approver), F050 (send-time plausibility gate; sweep tool exists), F062 ("RFQs to Accept" dynamic label — needs submission plumbing), gap5b-f015 (editing-lease ghost fix — BUILT on branch, needs multi-device verify), F041 broader supplier-matrix + dormant edges. PARKED: B053, B054. DEFERRED (pricing re-enable): F041b skip-expired, write-side plausibility gate, scraper extraction fix.
     2026-07-24 — F065 (Jon, motivated by PRJ402142) stamped + ANALYZED + BUILD-PLANNED. Cross-Line Part# price/lead-time propagation: within one project, when a duplicate Part# (appears on >1 Line/BOM) is priced/changed/lead-timed, PROMPT to update the matching parts on the other Lines. Design LOCKED (Jon): persisted `crossLineDuplicates` index on the project doc (recompute on BOM-save + project-open), exact `normPart` match (SSOT `_samePart`), prompt-for-ALL edit types incl. lead-time, skip `priceSource:"manual"` rows, ONE `saveProject` write, active-project only. Coach read-only trace → `docs/F065-CROSS-LINE-PROPAGATION-ANALYSIS.md`; Marc build plan → `docs/F065-BUILD-PLAN.md` (found a 4th trigger `updateVendor`:28595 + the PanelCard→ProjectView scope seam). Money-path. AWAITING: 3 Jon micro-decisions (vendor-alone prompt? / qty-break warning? / per-Line granularity?) + Coach review before build. NOT yet a TODO.md build item.
     2026-07-24 — B058 (Jon, live — SYSTEMIC display bug) stamped: the "not in BC" indicator (the BC circle shown when a part is NOT in Business Central) DISAPPEARS when the user manually enters a price on that part BEFORE adding it to BC. Root shape: the indicator's render condition is (wrongly) coupled to price presence / priceSource instead of actual BC membership (bcNo/bcVerify). Jon: "The BC circle should ALWAYS show if the part is not in BC." Fix = drive the indicator solely off BC-membership, decoupled from whether a price exists. Display-logic bug, low-stakes. ✅ FIXED (Marc-lane trace + Freddy applied `ccf9b6f1`): `_bcCircle` (:30799) now early-returns null on `bcNo || bcVerify.status==="in-bc" || priceSource==="bc"` (durable bcNo is the reliable membership signal, never cleared) + dropped the `priceSource!=="manual"` blue-gate. Single render site (F002 folded old pills into one _bcCircle). validate_jsx + check-scope clean. Jon chose DEPLOY TO PROD → deploy in progress.
     2026-07-24 — B060 (Jon, live prod v1.24.34) stamped: changing a PRICE on a BC-circle item makes the BC circle briefly DISAPPEAR then COME BACK (a flicker). End state is correct (circle returns), so cosmetic/low-severity — but worth root-causing since it's in the just-shipped B058 `_bcCircle` area. Freddy hypothesis: B058 made a pre-existing transient VISIBLE — pre-B058 a price edit left the circle gone (no flicker); post-B058 the circle correctly returns, so a transient intermediate render (a moment where priceSource briefly reads "bc" or bcVerify briefly "in-bc" during the edit/save cycle, then settles back to non-BC) now shows as disappear→reappear. Needs instrument/observe (timing bug — don't guess). DEFERRED diagnosis until the F065 build lane finishes editing app.jsx (avoid concurrent-edit + mid-edit reads). Then read-only trace of the price-edit→_bcCircle render path (updateBomRow / onSaveImmediate / any transient priceSource/bcVerify write). Source: Jon. ✅ DIAGNOSED (Marc lane) → `docs/B060-BC-CIRCLE-FLICKER-DIAGNOSIS.md`. ROOT CAUSE = `applyConfirmedPrice` (:28540) optimistic-then-revert: the "✓ Confirm & Push to BC" button stamps priceSource:"bc"+bcVerify:"in-bc" optimistically (circle hides), then bcPatchItemOData fails for a not-in-BC row (no bcNo → _bcNo falls back to partNumber.slice → PATCH targets a non-existent BC item), reverting to "manual" (circle returns). B058 confirmed NOT the cause — it changed the revert END STATE so the circle correctly returns (making the pre-existing transient visible). FIX available (promote-on-success: show typed price as manual immediately, promote to bc only after push confirms — no revert, no flicker) BUT it changes priceSource semantics during the ~1-2s push window (RFQ-eligibility/styling) → MONEY-PATH → Jon decision (fix-with-Coach-review vs leave; LOW sev, end state already correct). Awaiting Jon route.
     2026-07-24 — F066 (Jon, live) stamped: Duct, Din Rail, Duct Cover are regular RE-ORDER-schedule commodity items → EXEMPT them from the priceDate-STALENESS red flag (no need to require a fresh Priced Date on these). ⚠ MONEY-PATH — red gates send-block (F044). SCOPE (Jon locked): TARGETED to just these 3 item types (NOT a global red-rule change); every other row keeps current red rules. They STILL go red on $0.00 price (only the staleness trigger is exempted). IDENTIFY by keyword in **partNumber OR description** (Jon: "keyword for DUCT and DIN is in the Part# too") — case-insensitive "duct" (covers Duct + Duct Cover) + "din"/"din rail" (⚠ guard "din" substring false-matches — prefer token/word-boundary). Impl: new SSOT `_isReorderCommodity(row)` gating ONLY the staleness branch inside `_isBomRowFlaggedRed`/`_effectivePriceDate` (NOT `_isExcludedFromPriceCheck`, which would exempt $0 too). Coach-review (money-path). Source: Jon.
     2026-07-24 — G020 (Jon, live) stamped: HIDE the "Get New Pricing" (+ paired "Refresh All") button while scrapers/auto-pricing are disabled (RFQ-only). Freddy assessment CONFIRMED: with AUTO_PRICING_ENABLED=false the button is neutered — just alerts "send an RFQ", no useful function; manual per-row entry + BC Item Browser + RFQ/portal still get prices in. Approach: hide behind the existing AUTO_PRICING_ENABLED gate (REVERSIBLE — reappears when auto-pricing is re-enabled). Display-only, low-stakes. Trace which buttons ("Get New Pricing"/"Refresh All"; leave manual per-row + portal untouched). Source: Jon.
     2026-07-24 — F067 (Jon, live) stamped — F065 ENHANCEMENT: add an "Auto Approve all lines for the next 3 minutes" option to the F065 cross-line-update prompt, so a user fixing many items in one pass isn't prompted on every edit. Behavior: clicking it sets a session-scoped auto-approve window (now + 3 min); while active, `_maybePromptCrossLine` AUTO-applies the propagation (calls `propagatePartAcrossPanels` directly, [Update all] semantics) instead of showing the modal; after 3 min (or a manual cancel), reverts to prompting. ⚠ MONEY-PATH — during the window, cross-Line price/LT/vendor changes propagate WITHOUT a per-edit confirm (deliberate, user-invoked, time-boxed). Must keep all F065 safety carve-outs inside auto-apply (skip `priceSource:"manual"`, `_isExcludedFromPriceCheck`, owner-priority gate, capturedProjectId identity check). Consider: a small persistent "Auto-approving cross-line updates — 2:59 left [Cancel]" indicator so the user knows it's active. Builds ON the F065 branch (extends the F065 prompt). Coach-review. Source: Jon. ⚠★ COACH CAUTION (F065 pre-prod review 2026-07-24): F065 Bug A REMOVED the `priceSource!=="manual"` carve-out from `propagatePartAcrossPanels`, so an F067 timer that calls it directly (bypassing the modal) would SILENTLY overwrite manually-priced rows on other Lines — the data loss the modal's per-Line disclosure now prevents. F067 auto-apply MUST do ONE of: (a) re-introduce a manual-price skip in the auto-apply path, (b) restrict auto-apply to non-manual targets, or (c) surface a batched, undoable notification naming which manual prices it clobbered. Bake into the F067 build plan.
     2026-07-24 — B061 (Jon, live) stamped + ✅ BUILT (Freddy, master e8abb686): the Ext$ column value (e.g. "$13,557.30") overflowed into the Lead Time column ("$13,557.3060") — cell was width:80 + whiteSpace:nowrap. Jon also asked to match the Unit $ format ("$" fixed-left, number right-justified in its own field), not "$"+value as one right-aligned string. Fixed both: widened cell (width 110/minWidth 96) + restructured to inline-flex $-left + right-aligned number span mirroring Unit $ (:31165). Display-only; validate_jsx + check-scope clean. On master; awaiting Jon deploy decision. [→ shipped prod v1.24.35, then real fix (colgroup <col> width 64→108) landed in the Test batch.]
     2026-07-24 — F068 (Jon, live — F065 EXTENSION) stamped: apply F065-style cross-Line propagation to CROSSED and SUPERSEDED parts. Right now F065 matches EXACT part# only (`_samePartKey`/normPart), so the same physical part on different Lines under different numbers (one crossed/superseded to another) isn't linked. Two interpretations UNDER ANALYSIS (Coach lane): (1) BROADEN THE MATCH — F065's duplicate index also links crossed/superseded equivalents so price/LT/vendor propagates across different-but-equivalent part#s; (2) PROPAGATE THE CROSS OPERATION — crossing/superseding a part on one Line auto-applies the same cross to the matching (old-part) rows on other Lines. Coach tracing crossing mechanics (isCrossed/crossedFrom/commitBcItem/config-alternates+autoReplace/supplierCrossRef) + equivalence-data reliability (is a fresh crossed row back-linkable?) + feasibility/risk per interpretation. Freddy to synthesize options + rec for Jon. NOT scoped/built. Source: Jon. → ✅ SCOPED (Int.2 full-sync, Jon-locked) + ANALYSIS `docs/F068-CROSSED-SUPERSEDED-ANALYSIS.md` + BUILD-PLAN `docs/F068-BUILD-PLAN.md` (Coach plan-review APPROVE-WITH-CHANGES F1-F9 folded) + BUILT (Marc lane) + Coach CODE-review APPROVE + LT-TIMING FIX (re-read source at [Cross all] click) — all on branch **`claude/f068-cross-propagation`**, on **Test V.055** (base v1.24.36). ⏳ NEXT SESSION: **Jon re-verify the CROSS case** on Test (he pivoted to the RE-SELECT scenario mid-test, so the cross-LT re-read fix is applied but Jon-unverified) → then merge to master + prod. NOT on master/prod (master clean @ 7ba59c0f = prod v1.24.36).
     2026-07-24 — 🕐 TABLED for next session (Jon: "table this for next time unless easy/quick" — it's a build, not quick) — LEAD-TIME / BC-ITEM-BROWSER cluster surfaced during F068 testing: (A) **B062 [BUG] BC Item Browser shows NO lead time for any item** — the browser doesn't fetch/display the BC lead time per row, so USE can't carry it. ARC HAS the fetcher `bcGetItemLeadTimeDays(pn)` (ItemCard Lead_Time_Calculation, :4700-4713) + a vendor-catalog variant (:4840) — the browser just doesn't call them per item. Fix = add an LT column (fetch per item; may be N calls — batch/lazy) + carry the LT on USE. (B) **RE-SELECT doesn't push LT** — re-selecting an already-priced+LT'd part via BC Item Browser → USE (commitBcItem, SAME part, not a cross) fires the F065 propagation PRICE-ONLY (the commitBcItem fire at :28653 was made price-only per Coach full-sync-review F2, to avoid propagating a CROSS's stale old-part LT). For a SAME-PART re-select the row's LT is valid → the fire should FULL-SYNC (`_fullCrossLinePatch(_committedRow)`) when NOT a cross, price-only only when it IS a cross. Small conditional fix, but money-path (affects prod v1.24.36's commitBcItem behavior) → do WITH the B062 browser-LT work as one lead-time-flow pass. Source: Jon.
     2026-07-24 — B059 (Marc-flagged via Freddy — LOW / latent, queue) stamped: the "N not in BC" archive COUNTERS can under-count a manually-priced non-BC part. Root: the `bcVerify.status` recompute (:16207/:29364) collapses "not-in-bc"→"manual" when priceSource==="manual", and the counters (:10534/:5204/:25360/:47656) count off `bcVerify.status==="not-in-bc"`. B058 made the visible CIRCLE price-independent (keys off durable bcNo) but the counters still key off the volatile bcVerify.status. Latent today (RFQ-only keeps pricing passes off; applyBudgetaryPrice alone doesn't trigger the recompute). Fix = make the counters use the same bcNo-based membership predicate as the circle. Pairs with auto-pricing re-enable. Source: Marc lane during B058, routed via Freddy (Jon: log & queue).
     2026-07-27 — B063 [BUG] (Jon, live) stamped: make the BC Item Browser SEARCH case-insensitive. Root: `bcSearchItems` (:5011) builds the server-side BC OData PRIMARY-token query as `contains(${field},'${primary}')` (:5062) using the original-case token → OData `contains()` is case-sensitive, so a lower/mixed-case search misses items. The client-side SECONDARY-token filter already lowercases both sides (:5058+), so ONLY the server primary-token fetch is case-sensitive. Fix = case-fold the primary-token filter across the ITEM_CARD_FIELDS fan-out, e.g. `contains(tolower(${field}),'${primary.toLowerCase()}')`. LOW-stakes, NOT money-path (read-only search query). Source: Jon.
     2026-07-27 — 🚨 EMERGENCY diagnostic (Ryan BC disconnect) spawned 3 ARC items (Jon: "all of the above"). **B064 [BUG·HIGH]** — BC failures that bypass the honest pill (404 "segment not found" on a discovered web service + B013-G1 raw-fetch 401s) must SURFACE (admin signal + persistently-failing-endpoint alert); went silent 5 weeks. Extends B013. **B016/B012** — write-exhaustion amplifier (failing BC ops → resource-exhausted/max-backoff → session stall); harden (await/confirm per mutation + churn reduction) — NOT a new number, advances existing B016/B012. **F069 [FEATURE·guard]** — hard-block any BC op when user's/project's env ≠ Settings env (build on `_bcEnvMismatched`+per-project `bcEnv`); Jon directive. Root cause of the emergency itself = BC-side (Project_Planning_Lines_Excel web service 404 in sandbox MATR_SndBx_01152026, chronic since ~June 22, both jon@+Ryan) → Jon (BC admin) republishing. Full: docs/RYAN-BC-DISCONNECT-DIAGNOSTIC-2026-07-27.md. Source: Jon.
     2026-07-29 — F075 [FEATURE] (Jon, live — REFRAMED same day) stamped: **re-enable auto-pricing for API sources ONLY (Mouser + DigiKey); keep scrapers (Royal/Codale/custom) OFF.** Corrected diagnosis (Jon): the PRJ402119 junk-$0.71 came from the SCRAPERS (Royal, possibly Codale), NOT the APIs. API pricing (Mouser/DigiKey) is legitimate → safe to re-enable; scrapers stay disabled. This VALIDATES the Step-2 reconcile design (scraper/ai ranked lowest, never auto-pushed; $0.71 zeroed) — no reconcile change. Architecture: `AUTO_PRICING_ENABLED=false` (:6159) is a SINGLE master flag gating ALL auto-pricing (`runPricingOnPanel` :16665/:30297 + the "Get Pricing" UI :31865); `SCRAPER_BC_WRITEBACK_ENABLED=false` (:6147, Codale/custom scraper→BC writeback) + `AUTO_BC_REPRICE_ENABLED=false` (:6153) are the scraper/auto-reprice offs. The RFQ-SEND API auto-fetch (`isApiVendor`→apiGroups, :21469; Mouser/DigiKey) appears to run independently of the master flag (the screenshot's "obtained automatically via DigiKey API" — so API RFQ-pricing may already work; VERIFY the exact gating). Work = SPLIT the master kill-switch into per-source gates: an API-pricing enable (ON for Mouser/DigiKey) vs scraper-pricing enable (stays OFF for Royal/Codale/custom) + AI + scraper→BC writeback OFF. Touch `runPricingOnPanel`'s source fan-out (searchVendorPricing / mouserSearch / digikeySearch / codale / customScraper) to gate each source by its flag. MONEY-PATH (the exact incident area) → disciplined build: plan → Coach → Test verify before prod. Prior "email API vendors" framing CANCELLED (API auto-fetch is desired, not email). Source: Jon. NOT scoped/built.
     2026-07-29 — F076 [FEATURE] (Jon, live) stamped: **Supplier Portal — "Enter Pricing and Lead Time manually" button.** Today the supplier can only reach the pricing/lead-time entry page by drag-and-dropping a quote PDF (→ AI extraction → review). If they have no droppable PDF there's no path in. Add a button on the portal drop-landing that bypasses the drop/extraction and opens the same editable line-item grid (pre-populated with the RFQ's requested items) for manual price + lead-time entry, then flows into the existing review/submit path. Scope: supplier portal component (rfqUpload page — the drop UI + the line-item entry/validation grid + submit). Public-facing → test carefully (portal is unauthenticated via token). Stakes MEDIUM. Source: Jon. NOT scoped/built.
<!-- (log continues) -->

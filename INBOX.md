# 📥 Intake Inbox — awaiting Freddy triage

> **Owner (sole writer): Dez (Dezzie Arnez, Intake).** This is a dedicated single-writer file
> so it cannot collide with TODO.md on the shared working tree (per DOC-OWNERSHIP-PROPOSAL §2 +
> Freddy's INBOX.md-split refinement, approved 2026-07-02).
>
> **Dez:** drop raw bug/feature captures here — timestamped, **un-numbered** — one bullet each,
> dedup-checked against existing `#N` in TODO.md + SESSION-STATE.md first. Commit `-- INBOX.md`
> and push immediately after each capture. Do NOT assign `#N` (Freddy is the sole allocator →
> avoids multi-session number collisions).
>
> **Freddy:** on triage passes, pull from here, dedup-confirm, assign the next `#N`, promote the
> item into the TODO.md numbered tracker, then delete the bullet here. (Freddy edits this file
> only to clear routed bullets; Dez owns all appends.)
>
> Capture format: `- [YYYY-MM-DD] BUG|FEAT|GEN — "<short title>" — <desc> — reported via Intake (source: Jon|Marc|Coach|Freddy)`
>
> Category (B/F/G taxonomy, replace-going-forward, approved 2026-07-02 — Freddy stamps the number at triage):
> `BUG → B###` · `FEAT → F###` · `GEN` (General — neither bug nor feature) `→ G###` · `METRICS → M###` (analytics/engagement telemetry; new category added 2026-07-10, see NUMBERING-CONVENTION.md). Every item carries a short
> identifying title. Existing `#1–#198` keep their numbers (no retroactive renumber; open items get tagged in Freddy's triage pass).

---

_(No items awaiting triage — all promoted to TODO.md as of 2026-07-10.)_

<!-- Triage log:
     2026-07-02 — G001 (Allow-Once → Verified/not-fixable), B001 (trailing-dot redirect URI, LOW), B002 (approved-state TR block message, LOW) promoted by Freddy.
     2026-07-03 — G005 (matrix-arc-test shares PROD Firestore), B003 (Review-Supplier-Quote modal lists unquoted parts), B004 (portal-Apply unawaited-save reload-race → RESOLVED 41824f6c / shipped v1.21.25), B005 (resolved-TR-row can't re-arm, LOW/tuning) promoted by Freddy at #199 close-out.
     2026-07-06 — F004 (Portal submit confirmation shows ARC user + email-copy notice) promoted to TODO.md Features [Backlog] by Freddy. Also back-filled F001/F002/F003 into the Features tracker (were only in SESSION-STATE + docs).
     2026-07-06 — F005 (Print-Only button in locked-quote blocker overlay) promoted to TODO.md Features [Backlog] by Freddy (sibling to #196).
     2026-07-06 — F006 (Qv.## Hist. button — per-quote send history w/ document previews) promoted to TODO.md Features [Backlog] by Freddy (feed = #193 qvHistory; distinct from #194 global metrics).
     2026-07-07 — B008 (RFQ History "Supplier Portal" link opens pre-submission portal, not submitted) promoted to TODO.md Bugs [Backlog] by Freddy (classified BUG: link points at wrong state).
     2026-07-07 — B010 (manual Upload-Supplier-Quote undefined crash) + B011 (harden other supplierQuotes sites) + F007/F008 + G006 (portal loading copy, cosmetic) promoted by Freddy.
     2026-07-07 — F007 (order projects by last-accessed) + F008 (new-version-available refresh notification) promoted to TODO.md Features [Backlog] by Freddy.
     2026-07-07 — F007 (Order main-page projects by last-accessed, most-recent on top) promoted to TODO.md Features [Backlog] by Freddy (distinct from #200 color-by-quota-age).
     2026-07-08 — B013 (BC-401 hidden; from the "61 items could not be pushed" bullet — root cause OAuth token expiry, NOT G005) + B014 (Codale multi-result parse-gap; from the "5 Codale no pricing" bullet) INBOX bullets cleared (both stamped earlier @9a511e07). G007 (remove leftover TEST upload section) promoted to General by Freddy at close-out. INBOX now empty.
     2026-07-09 — F009 (Debug Mode — persistent cross-user activity tracing; parked behind the B012 hard-lock work) + F010 (Suggested alternates — multi-alternate ALT picker + supplier-portal alternate sub-lines) promoted to TODO.md Features [Backlog] by Freddy; both bullets cleared. STILL HELD for a full triage pass: BUG "Login 'Continue with Microsoft' misleads existing users" (2026-07-09, source Jon) — needs B-number + dedup.
     2026-07-10 — All 4 pending bullets TRIAGED + promoted by Freddy (Dez cleared bullets per Freddy's request): F014 (Customer-specific payment-terms note on ALL quotes + BC storage field — CRITICAL, classed FEAT, slotted TOP of post-P1 queue; Jon-flagged critical, Dez proactive-surfaced) · B022 (Login "Continue with Microsoft" misleads existing users, MED) · B023 (Quote Summary line-row overflow clips total price → truncate "IN PRE-REVIEW"→"IN ETR", LOW/cosmetic) · BOM-row-RED-despite-reqs on PRJ402100 3044102 (1-cent clears) FOLDED INTO B018 (decisive repro added; same _isBomRowFlaggedRed priceDate-staleness root; no new number). INBOX now empty.
     2026-07-10 — ★ NEW CATEGORY established by Freddy: METRICS (prefix M) added to NUMBERING-CONVENTION.md (own sequence + ranking, same pattern as B/F/G). Use METRICS/M at intake going forward for analytics/engagement items (Freddy stamps M###). First item promoted: M001 (user engagement telemetry — page-access / activity cadence / per-project time) → TODO.md [Backlog], scoped by Freddy (privacy guard: cadence-not-verbatim-keystrokes; overlaps F009 capture pipeline — design decides shared-vs-standalone). Dez cleared the M001 bullet. INBOX now empty.
     2026-07-23 — F041 (Primary/Secondary supplier matrix — BOM items default to PRIMARY supplier sourcing; Purchasing may source from a secondary) stamped + captured by Freddy (source: Jon). Money-path (supplier/pricing/BC). In ANALYSIS (Coach lane tracing current model + BC capability) — NOT yet promoted to TODO.md as a build item; awaiting synthesis + Jon direction on the Sales↔Purchasing flow.
     2026-07-23 — F042 (To-Do pane NEEDS ATTENTION list: show ALL projects incl. yellow/green sorted by time — not just red; sort by Requested Ship Date + Est. Prod. Done date + untouched-duration) stamped + captured by Freddy (source: Jon). Dashboard UI, low-stakes. In SCOPING (Marc lane) — NOT yet promoted to TODO.md build item; awaiting scope report + Jon sort-tiebreak confirm.
     2026-07-23 — G016 (shrink top To-Do status tiles) BUILT + shipped Test V.040 (source: Jon, live). G017 (To-Do side pane scroll separate from main page — likely the F030 MY DASHBOARD page; standing rail already scrolls independently) + F043 (hover right-pane tile → highlight corresponding project on main screen; F034-family) stamped + captured by Freddy (source: Jon). G017/F043 awaiting Jon surface-confirm before build/scope.
     2026-07-23 — 🚨 PRJ402119 PRICING INCIDENT (money-path emergency, source: Jon). Quote sent with wrong pricing/red rows ($6000 item entered as $12). Freddy stamped: **F044** (Hard-Fix 1: block Quote send if ANY red row, any reason) · **F045** (Hard-Fix 2: Budgetary checkbox = Manager/Admin only, greyed + tooltip for others, + rules guard) · **F046** (Hard-Fix 3: full user audit trail — trace all mutation paths by user) · **F047** (Soft-Fix 1: Priced-Item hover shows the user who approved the BC price; depends on F046). Plus an INVESTIGATION (trace who priced Line 3 Rows 4 & 54 + correlate with the BC-write-failure window) — Coach lane running (code feasibility), live-data trace pending Jon. NOT promoted to build; awaiting investigation + Jon priority.
     2026-07-23 — 🚨 PRJ402119 incident cont'd (source: Jon): BOM-lock requirement. Freddy stamped **F048** (lock a sent BOM completely — qty/price/lead-time/vendor — + suppress auto-pricing incl. auto-check on project open; the only sanctioned post-send change is via F049) · **F049** (PO-receipt modal: quoted-BOM cost vs current-BOM cost, salesman confirms BEFORE accepting the customer PO; prerequisite = capture a quoted-BOM snapshot at send, which may not exist today). Coach lane #2 investigating whether sent BOMs lock today + whether a quoted snapshot exists to check if PRJ402119's BOM changed post-send. Possible actual root cause of the incident. NOT promoted to build; awaiting investigation + Jon priority.
     2026-07-23 — G018 (Debug Logs viewer capped at 500 rows / can't reach older entries — reaches only ~July 14 despite older logs likely still in Firestore; add date-window filter or pagination) stamped + captured by Freddy (source: Jon, surfaced during PRJ402119 investigation). Also NEW fix candidate (unnumbered, pending Jon go): pricing PLAUSIBILITY / quoted-vs-current divergence check — the real gap that catches a wrong-magnitude price regardless of source (block-on-red does NOT). To be numbered when Jon greenlights the reframed build order. F045 (budgetary manager-gate) BUILT client-side, on Test V.044.
     2026-07-23 — Jon greenlit the reframed ASAP order (all 5 decisions). Freddy stamped **B052** (5-min BC poll divergence guard — the PRJ402119 root-cause fix: don't auto-apply a large downward BC price swing; keep current price, flag `bcPollDivergence`, log durably) — BUILT, on Test V.045, Coach reviewing. **F050** (send-time price plausibility / divergence check — belt-and-suspenders, may consume the B052 flag). Build order: B052 → F050 → F044 → F048 → F049 → F045 server-hoist.
     2026-07-23 — F051 (send-time freshness-through-validity gate: force new pricing+lead-time if any row would go stale within the quote's validity window; `priceDate+staleDays < sendDate+validity`) stamped by Freddy (source: Jon). Pairs with the sent-lock-vs-staleness fix (F048-adjacent) into one coherent quote-freshness/lock policy. Send-gate (hard block). Scope folded with the staleness trace.
     2026-07-23 — F051 EXPANDED (Jon): also check ON PROJECT OPEN (assume send-today) → notice + push Get New Pricing for items that'd expire within validity; plus the send-time hard block. F052 (Jon) stamped: expired-quote handling at PO receipt — PoReceivedModal warns if quote expired + two paths (accept-as-is w/ manual order adjustment to match PO amount; or re-quote → resend → request updated PO). Pairs with F049. All part of the "Quote Lifecycle & Lock" epic (F048+F049+F051+F052+staleness-suppress); scoping as one combined sequenced plan.
     2026-07-23 — B053 (Jon) stamped: auto-price-check-on-open (a) confirmed as the mechanism that bumped sent quotes' rev → IN-PROCESS demotion; (b) repeat-nag bug — opening the same project 3× re-shows the same "Accept new prices" modal for the same items (phantom diff / no persistence / no dismissal). Jon: feature gets in the way more than helps. Coach diagnosing root cause + sane-behavior recommendation. Related to F048 (suppress on locked) but distinct (unlocked-quote repeat-nag + possible rethink of on-open auto-check).
     2026-07-23 — F060 (Jon, live) stamped + BUILDING (Marc lane): To-Do / Dashboard restructure (DISPLAY-ONLY). (A) Side-panel To-Do rail: NEEDS ATTENTION capped to top 5 + clean QUOTES SENT EXCLUDED (Jon decision: option 1 of 3 — sent-clean belongs in the new expiring list, not Needs Attention) + NEW "QUOTES EXPIRING" list below (top 5 sent quotes within 7 days of quoteExpiresAt, incl. already-expired, soonest-first). (B) Dashboard MY DASHBOARD page: REMOVE the Email column (noise, not useful yet — underlying outlook state left intact); REPLACE "MY PROJECTS" list with QUOTES EXPIRING; THREE EQUAL columns (NEEDS ATTENTION | QUOTES EXPIRING | ARC NOTIFICATIONS, flex:1 1 0); add total price (_projectSellTotal = sell/quote total via computeBasePanelSellPrice) to the NEEDS ATTENTION + QUOTES EXPIRING rows (dashboard/pageMode only, not the narrow rail). New shared helpers: _projectSellTotal + _quotesExpiring. Origin: the NEEDS-ATTENTION-shows-clean-QUOTES-SENT finding (F042 dropped the old yellow+red-only filter → showed ALL bucketed projects). ✅ BUILT (Marc lane) + FREDDY-REVIEWED (diff verified data-safe display-only; _hlBoardTile scope confirmed @:45385 F043 helper; helpers pure) + Freddy added a dashboard empty-state ("✓ No quotes expiring soon.") so the column stays balanced when empty (rail still hides it). validate_jsx → JSX OK. ALSO folded in: service-card LINE-ITEM status pill removed (:35035, F058 sibling; Quote Summary sc.status Badge @:37279 kept). Committed to master, NOT deployed — awaiting Jon deploy go. Minor note: totals use _projectSellTotal (SELL/quote total, not the old MY-PROJECTS Σqty*unitPrice COST) — intentional (quote value is more meaningful for these lists).
     2026-07-23 — F061 (Jon, live) stamped + BUILT (reviewing→deploy): remove the ACTIVE ECO status-column filter from the main board so ECO projects fall into their REAL status columns (RFQs/In Process/etc.) and follow the normal process (they have RFQs) — KEEP the red ECO tile border for quick recognition. Reverses F024. Edits: computeProjectEffectiveStatus dropped the `if(computeActiveEco)return"active_eco"` short-circuit (~:16955); _todoBucketOf (:16779) + _todoClockStart (:16812) now use the real status (activeEco kept only for the Won/PO carve-out); ACTIVE ECO column removed from the main board order+labels (:45760/61) + the rail Sales-Pipeline buckets (:45301). RED BORDER UNTOUCHED — verified it's driven independently by `_hasActiveEcoTile=!!computeActiveEco(p)` at the tile render (:47211), NOT by status==="active_eco". validate_jsx → JSX OK. + F060 REFINEMENTS same batch (Jon live): (a) Dashboard NEEDS ATTENTION shows the FULL list (top-5 cap now rail-only); (b) rail QUOTES EXPIRING shows the "✓ No quotes expiring soon." placeholder when empty (was hidden); (c) rail NEEDS ATTENTION count moved to the RIGHT of the header as "5 of 15" and made a LINK to MY DASHBOARD (new onOpenDashboard prop → setView('dashboard')+setNavTab('user_dashboard')); bottom footer removed. RFQ LABEL — RESOLVED (Jon): rail "Pending RFQs" → "RFQs to Send" (one edit at _todoBuckets :45297 → flows to both the Sales-Pipeline pill AND the attention-row label via _bucketLabels :45330). Board column label left as "RFQs Send/Receive" (Jon scoped the rename to the rail). Jon's model: base = "RFQs to Send"; when quotes come back = "RFQs to Accept" → see F062.
     2026-07-23 — F062 (Jon, live) stamped — FOLLOW-UP (needs data plumbing, NOT yet built): dynamic rail RFQ label — flip "RFQs to Send" → "RFQs to Accept" per project when supplier quotes have RETURNED and await acceptance. BLOCKER: the To-Do rail only has project/BOM data; "received awaiting acceptance" = an rfqUploads submission with status==="submitted" (vs "imported"=applied), which lives in a SEPARATE collection loaded per-project (ProjectView portalSubmissions listener :38496) or globally in RFQ History (subByToken, received-count logic :45208-45215) — NOT in the rail's `projects` prop. From the rail's data alone a sent-awaiting row and a returned-awaiting-acceptance row are identical (both rfqSentDate + no price). FIX PATH: plumb a per-project "hasSubmitted-unapplied quote" flag into the rail/dashboard data (app-level rfqUploads rollup keyed by projectId, or extend the projects load) → then the attention/pill label reads "RFQs to Accept" when received>0, else "RFQs to Send". Scope + build separately.
     2026-07-23 — F059 (Jon, live) stamped — PARKED mid-build (superseded by F060 thread): "Mark Committed" re-anchor — a sent+diverged quote (quoteRev>quoteSentRev, e.g. 402119 Qv.03 vs sent Qv.02) gets a button that re-anchors quoteSentRev=quoteRev (+ quoteRevAtPrint) WITHOUT emailing, moving it to the locked Quotes-Sent column. Jon chose the SIMPLE quoteSentRev-bump approach (accepts the "sent Qv.02" history line then reads Qv.03) + a lightweight committedWithoutResend audit stamp recording the customer's true rev. Build pattern confirmed: mirror the send-anchor write (:34662/:34683 uses safeSave(uid,{...upd,_sendAnchorWrite:true}) to avoid a rev-bump), MINUS the email/quoteSentAt/expiry changes. Button placement: Quote Summary pane near the "Quote sent Qv.NN" line (:37670), shown when quoteSentAt && quoteRev>quoteSentRev. RESUME after F060. ★ SCOPE CONFIRMED MULTI-PROJECT (Jon 2026-07-23): affects at least PRJ402131 + PRJ402119 (+ the known IN-PROCESS 402124/402109/402092) — sent quotes rev-diverging back to IN PROCESS; 402131 newly appeared IN PROCESS after a refresh (Jon says he didn't intend to edit it → possible residual spurious-bump vector even with auto-pricing off — INVESTIGATE; permanent prevention = F048 lock-sent-BOM). F059 = the cleanup mechanism; F048 = the prevention. ✅ BUILT 2026-07-23 (v-pending-deploy): "✓ Mark Committed (no re-send)" button in the Quote Summary pane (inserted after the "✓ Quote sent Qv.NN" line ~:37693), shown only when quoteSentAt && quoteRev>quoteSentRev. Handler mirrors the send-anchor write EXACTLY (update(upd) optimistic + safeSave(uid,{...upd,_sendAnchorWrite:true}) → no rev-bump per B034 @:9406), MINUS the email — sets quoteRev=quoteSentRev=quoteRevAtPrint=rev + quoteLocked + additive committedWithoutResend audit {at,by,byName,customerRev(the OLD quoteSentRev),committedAtRev}. quoteSentAt/quoteSentTo/quoteExpiresAt UNTOUCHED (customer copy unchanged). Owner-priority gated + a single arcConfirm (deliberate state change). Note (Jon-accepted): the "✓ Quote sent Qv.NN" line will then read the NEW rev (quoteSentRev bumped) — the committedWithoutResend audit preserves the true customer rev in data. validate_jsx → JSX OK; update() confirmed @:39356. Clears the 402131/402119/etc. IN-PROCESS backlog. Awaiting Jon deploy + live verify (click on 402119 → moves to Quotes Sent). ⚠ BUG in v1.24.23 (Jon: "did not work") — FIXED: the button's onClick called `update()` INSIDE PanelListView, where update() is a ProjectView-LOCAL fn (out of scope) → ReferenceError on click. validate_jsx (syntax-only) missed it; tools/check-scope.js catches it (was a NEW violation in the v1.24.23 deploy output, unread). FIX: button now delegates to a new onMarkCommitted PROP; the actual re-anchor (update+safeSave _sendAnchorWrite) is implemented in ProjectView at the PanelListView render (mirrors the adjacent onMarkLost handler :40574). check-scope → No NEW violations. LESSON: read the check-scope.js line on EVERY deploy (out-of-scope refs pass validate_jsx but crash at runtime).
     2026-07-23 — F058 (Jon, live) stamped + BUILT + shipped: REMOVE the per-line-card status pill (:29392, was <Badge status={computeProjectEffectiveStatus({panels:[panel]})}/>). Root cause of Jon's status-disconnect report: the line card computed status from a SINGLE-PANEL projection {panels:[panel]} that DROPS project-level quoteSentAt/quoteRev/quoteSentRev → the "already-sent" short-circuit never fired → it showed pre-send readiness (READY TO SEND/RFQS) on an already-SENT+diverged quote, contradicting the QUOTE SUMMARY pane (:37309 = computeProjectEffectiveStatus(project) = TRUTH = IN PROCESS). Jon chose REMOVE (vs. fix-to-match) — rely on QUOTE SUMMARY for status. NOTE latent design point: line-card status was per-PANEL, Quote Summary is per-PROJECT — they were always different by design; the glaring wrong was pre-send states shown post-send. If per-line readiness is wanted back later, feed the quote-send context into the projection (fix-to-match) instead of the removed pill.
     2026-07-23 — F057 EXTENDED (Jon, live): "CS" display now also covers VENDOR=CUSTOMER $0 rows, not just the customerSupplied flag. Discovered via PRJ402119 Row 53 (vendor OVIVO = customer OVIVO) — ARC's OTHER model for "customer provides this part." Condition in both cells (unit-price :30784 + line-total :30801): `row.customerSupplied || (row.unitPrice!=null && +row.unitPrice===0 && _vendorMatchesCustomer(row.bcVendorNo,row.bcVendorName,project.bcCustomerNumber,project.bcCustomerName))`. Reuses the SAME predicate the red-rule uses (SSOT — display + red-exemption can't drift). Behavior: a $0 (explicit) vendor=customer row shows non-editable "CS"; a vendor=customer row with price>0 shows the real (editable) price; an UNPRICED (null) vendor=customer row still shows "—"/editable so a real pricing gap isn't hidden. NOTE: the null-guard (`unitPrice!=null`) was added in a fast-follow after the advisory review caught that `+null===0` would false-positive "CS" on unpriced rows — shipped v1.24.19 without it, v1.24.20 with it. validate_jsx.js → JSX OK.
     2026-07-23 — F051j (Jon) — Expire Junk BC Prices tool BUILT (Marc lane) + COACH-REVIEWED (verdict BLOCK — functional/accountability, NOT data-safety: Coach confirmed it CANNOT corrupt a legit price — PATCHes each record's own scanned @odata.editLink verbatim, bad URL 404s, never key-guesses; does NOT repeat the bcPushPurchasePrice single-key bug). Scope: scan PurchasePrices Direct_Unit_Cost≤25, group by exact cost, suspect=≥8 distinct Item_No OR cost==0.71 (pre-checked); expire=PATCH Ending_Date via editLink+If-Match; audit→companies/{cid}/bcPurchasePriceExpiries; preview-first (scan→check→arcConfirm→expire); yesterday default; admin card in APISetupModal. **B2 FIXED this pass:** added firestore.rules bcPurchasePriceExpiries block (isAdminMember create, field `by`, immutable) — was default-denied+swallowed → zero audit; needs `firebase deploy --only firestore:rules`. **B1 OPEN (the real gate):** editLink mechanism UNPROVEN on this tenant — FIX #182 (bcPushLeadTime :4779) found @odata.id/editLink NULL here → builds keys manually. If null under metadata=full → tool inert (all "not patchable"); if editLink carries a Company() segment → doubled → 404s. RESOLUTION: deploy scan-first (read-only, safe); Jon runs SCAN → notPatchableCount tells us. If patchable → expire OK. If not → rebuild PATCH with manual composite key (#182 pattern; needs full key: Item_No,Vendor_No,Currency_Code,Starting_Date,Variant_Code,Unit_of_Measure_Code,Minimum_Quantity — scan $select currently only pulls a subset). **M-fixes DEFERRED to expire-readiness pass (scan is read-only, don't need them):** M1 per-part visibility (expandable Item_No list before expiring), M2 clamp Ending_Date≥Starting_Date (recent junk → BC 400), M3 test-env fake-200 → phantom success + prod-Firestore pollution (detect _testEnvBlocked). Jon greenlit: B2+deploy+he scans. validate_jsx + check-scope both clean. ★ SCAN RESULT (v1.24.26, Jon live): scan FAILED — "unparseable JSON at position 235". Raw response (diagnostic surfaced): `{"@odata.context":".../ODataV4/$metadata#Company('Matrix Systems LLC')/PurchasePrices","value":[{"@odata.type":"#NAV.PurchasePrices"…` → this is LEGACY NAV-style OData (`/ODataV4/`, `#NAV.*` types) which CHOKES on `odata.metadata=full` (dies right after the @odata.type annotation). ROOT: metadata=full is unsupported here. This ALSO confirms B1 — no editLink under normal metadata (matches FIX #182 for ItemVendorCatalog). REQUIRED REBUILD (both fixes converge on #182's proven pattern): (1) scan uses default `Accept: application/json` (drop metadata=full); (2) $select the FULL NAV PurchasePrice compound key (Item_No,Vendor_No,Currency_Code,Starting_Date,Variant_Code,Unit_of_Measure_Code,Minimum_Quantity) + Direct_Unit_Cost,Ending_Date + capture @odata.etag; (3) build the PATCH key URL MANUALLY like bcPushLeadTime #182 (:4782) `PurchasePrices(Item_No='..',Vendor_No='..',Currency_Code='..',Starting_Date=YYYY-MM-DD,Variant_Code='..',Unit_of_Measure_Code='..',Minimum_Quantity=N)`; (4) NEW safety beyond #182 — read-only GET the constructed key URL FIRST to verify the key format resolves before any PATCH (gates expire; no blind writes). Then PATCH + If-Match etag, body {Ending_Date}. Fold in M1/M2/M3 with this pass. Awaiting Jon go on the rebuild. ✅ REBUILT (Marc lane, Jon chose A): scan now uses default metadata + full compound-key $select + builds record.keyUrl via _bcPpKeyUrl (#182 shape); verifyJunkKeyFormat does a READ-ONLY GET of the first record's keyUrl → keyVerified; expire PATCHes rec.keyUrl (+If-Match etag), M2 clamps Ending≥Starting, M3 catches _testEnvBlocked. Modal: canExpire=anyChecked&&keyOk&&!IS_TEST_ENV (write impossible unless the live key-verify passed + not Test), M1 per-group expandable Item/Vendor/date list, green "key verified" pill / red disabled banner on fail. Freddy reviewed diff; validate_jsx + check-scope clean. Coach's static re-review INTENTIONALLY skipped — the live read-only key-verify is a stronger check of the composite-key correctness than code-reading (only the tenant can confirm the 7-part key). Deploying scan; Jon re-scans → if key verifies, he can expire. ✅✅ DONE (v1.24.28, Jon live "Worked"): scan found 737 candidates / 2 suspect groups — $0.71 (95 records / 81 parts) + $1.24 (15 / 12); key-verify passed; expire initially 409'd on all 110 (NAV list carries no usable etag + If-Match:"*" rejected) → FIXED by fetching a FRESH etag per record via a single GET before each PATCH → expire succeeded, junk EXPIRED (Ending_Date set) + audited to bcPurchasePriceExpiries. Also reworded banner "PRODUCTION"→"connected BC database" (tenant URL is MATR_SndBx_01152026 — a SANDBOX). ⚠ OPEN: this cleaned the CONNECTED BC (the sandbox). If ARC's real/production BC is a DIFFERENT environment, it still has the $0.71/$1.24 junk → re-run the tool connected to that env. Jon (2026-07-23): the sandbox (MATR_SndBx) is the current working DB; a BATCH MOVE of the new Item DB → the other (prod) DB is planned. So: cleaning the sandbox junk NOW means the move carries clean PurchasePrices IF the move includes them; if the target DB's PurchasePrices are separate, re-run F051j connected to that env after/before the move. Tool is reusable + preview-first for that pass. PREVENTION still queued: B057 (write path should expire the superseded price so junk can't re-accumulate) + F041 (primary-vendor pick) before auto-pricing is re-enabled.
     2026-07-23 — B057 (Jon, live — SYSTEMIC, prevention) stamped: the BC PurchasePrice WRITE PATH doesn't retire the superseded price. Jon: "this is supposed to be happening anytime a new price is entered — it should be setting the old price's Ending Date so it's expired." Two linked defects in bcPushPurchasePrice (:5519): (a) the update PATCHes with a SINGLE-segment key `PurchasePrices('ITEMNO')` (:5546) on a COMPOSITE-keyed entity (Item+Vendor+Starting_Date) → the PATCH can misfire → it falls through to POST a NEW record, so multiple dated PurchasePrice rows accumulate per item, NONE expired; (b) it never sets Ending_Date on the prior/superseded record. Result: old + garbage prices pile up as "active," and bcFetchPurchasePrices' most-recent-Starting_Date selection can grab the junk (root mechanism of the $0.71 accumulation). FIX (when pricing re-enabled): correct the PATCH to target the exact record via @odata.editLink (like the F051j expire tool) AND, on a genuine new price, stamp Ending_Date on the superseded record so only the current price is active. Pairs with F041 (primary-vendor pick) + F051j (one-time cleanup). Currently MOOT (scraper write-back + auto-pricing disabled) — this is the re-enable-prerequisite prevention. → BUILDING now (Jon: "do B057 now", 2026-07-23): Marc lane rewriting bcPushPurchasePrice to (1) fix the composite-key PATCH via _bcPpKeyUrl + fresh-etag (F051j-proven), (2) supersede semantics — same-day Starting_Date → update cost in place; OLDER active → set Ending_Date=(pushdate−1) to expire + POST new; UoM-scoped so it never clobbers a different price line. High blast radius (~14 active callers: portal-apply/DigiKey/Mouser/manual/alternates) → Coach review + Jon controlled-test-push before trusted live. ⛔ B057 BUILT (Marc lane) + COACH-REVIEWED → BLOCK (3 ship-blockers, 2 = silent data loss). NOT committed/deployed. F1 (data loss): expire scope keys on UoM ONLY → would expire a legit different-currency/variant/volume-break line at the same UoM; FIX = scope to the full lane ARC writes (UoM + Currency='' + Variant='' + MinQty=0). F2 (price-less item): expire-then-POST → POST failure leaves item with NO active price; worse, bcFetchPurchasePrices (F041) ignores Ending_Date → ARC shows expired cost while BC sees none; FIX = POST-new-FIRST, expire only on cr.ok (both main + same-day paths). F3 (no-ops for scrapers): blank-UoM callers (:29079/:29190/:33574) — BC fills base UoM on the created record but scope looks for blank → never matches → keeps POSTing dups (defeats the fix for the exact scraper accumulators); FIX = scope-UoM must == written-UoM (resolve base UoM or require concrete UoM) + NEEDS a live BC fact: what UoM does BC persist when POST omits Unit_of_Measure_Code? F4 (med): same-day branch returns without running the expire loop → other older-active records never expired; FIX = run expire loop after same-day update too (idempotent). F5 (verify): un-expire via Ending_Date:"0001-01-01" is a NEW op (F051j only ever SET ending) — verify BC accepts it live. Coach cleared Q4-Q7 (concurrency semaphore-bounded, caller contract preserved, date math sound, data-safety OK). ★ Freddy REC: PARK — non-urgent (scraper write-back already killed, nothing accumulating), delicate high-blast-radius money-path deserves a fresh pass (F1/F2/F4 clean; F3 needs a live BC-UoM fact + design choice; then Coach re-review + Jon test push). Jon chose PUSH (2026-07-23). FIXES APPLIED: F1 → scope now full-lane (UoM===bcUom && Currency==='' && Variant==='' && MinQty===0) — can't expire a foreign-currency/variant/volume-break line. F2 → reordered POST-new-FIRST then expire-only-on-cr.ok (POST failure leaves old price active, never price-less). F4 → same-day path now runs _expireOlderActive() after the in-place update. F3 → blank-UoM safely no-ops (full-lane ''-scope won't match BC's base-UoM records → no over-expire) + console.warn; active callers pass concrete UoM so they get the full fix; documented limitation (concrete UoM needed before re-enabling blank-UoM scrapers). F5 → un-expire sentinel "0001-01-01" kept, verify-live (low-freq same-day-re-push-after-expire). Factored a shared _expireOlderActive() closure used by both paths. validate_jsx + check-scope clean. AUTHORITATIVE verification = Jon controlled test push (not a 3rd review — fixes are direct Coach-prescribed): push a price on a test item that has an OLDER existing PurchasePrice → confirm BC ends with the old record Ending_Date=yesterday + the new record active, and other UoM/currency lines untouched. ✅✅ DONE + VERIFIED (v1.24.29, Jon test push "Success"): supersede works — old expired, new active, no dup, no over-expire. B057 CLOSED.
     2026-07-23 — F041 STATUS (Jon: "And F041"): CORE ALREADY LIVE (bcFetchPurchasePrices preferredVendors :5570/5609 + runPricingBackground :16006 + runPricingOnPanel :28826). Remaining = dormant edges only (commitBcItem pass vendorNo, dead-vendor re-verify, 5-min poll, on-open check — all behind gated AUTO_BC_REPRICE) → Freddy to finish after B057 (avoid concurrent app.jsx edits).
     2026-07-23 — F057 (Jon, live) stamped + BUILT (shipped v1.24.18): show "CS" instead of $0.00 for customer-supplied BOM rows, in BOTH the unit-price cell (:30784, new branch beside the labor "— auto" branch; non-editable muted "CS") AND the line-total cell (:30801). Jon scoped it to internal BOM ONLY — NOT the printed customer quote, NOT RFQ/supplier docs. Pure DISPLAY change: stored unitPrice stays 0, red-rule/totals/pricing paths untouched (data-safe, additive). Origin: PRJ402119 Line 3 Row 53 read as $0/no-LT/not-red and confused Jon ("bit me again") — a customer-supplied row is correctly exempt from red (_isExcludedFromPriceCheck :16471) but the $0 display looked like a missing price. validate_jsx.js → JSX OK. Committed to master; NOT deployed (Jon gate + pricing freeze). NOTE: also surfaced a latent red-rule hole — _vendorMatchesCustomer (:16483) fuzzy 3+char-prefix match could false-positive-exempt a real $0 row from red; candidate B-item if it recurs.
     2026-07-23 — B056 (Jon, live — HIGH VALUE, recurring) stamped: trailing-dot hostname (`matrix-arc.web.app.`) breaks the ENTIRE app, not just BC. A dotted FQDN is a DIFFERENT browser origin → separate localStorage/IndexedDB/service-worker/Firebase-auth scope → app loads EMPTY (blank To-Do list, missing status tiles, Firestore write-queue exhausted, "nothing saves") AND BC OAuth redirect_uri mismatch (AADSTS50011) → BC offline / won't reconnect. Cost Jon ~an hour of whack-a-mole this session; recurred (see memory reference_bc_auth_aadsts50011_trailing_dot). FIX: boot-time guard in public/index.html head — `if(location.hostname.endsWith('.')){location.replace(location.href.replace(location.hostname,location.hostname.slice(0,-1)));}` BEFORE the app/bundle loads, so a dotted host self-corrects to the clean origin and the cascade can never start. ~3 lines, origin-safe (only fires on a malformed host), unrelated to the pricing freeze. Build-ready; offer to ship with next deploy. Also: user-side, purge the dotted URL from Chrome autocomplete (Shift+Delete the suggestion) + fix bookmarks/shortcuts.
     2026-07-23 — B055 (Jon, live) stamped: BC Item Browser results table — when MFR + Vendor columns async-populate ("finally update"), the table overflows and pushes the rightmost column (Customer-Supplied toggle) outside the modal; the table/modal does not resize or reflow to fit. Layout/overflow bug in the BC Item Browser results grid. LOW-MED (cosmetic-ish but the toggle becomes unreachable without horizontal scroll).
     2026-07-23 — ✅ SAVE-FAILURE INVESTIGATION — RESOLVED (root cause = STALE EDITING-LEASE GHOST, the known gap #5b bug; NOT a new bug, NOT the kill-switch, NOT data corruption). Crossing a part "not accepting it" + "nothing saved when I closed the tab" were BOTH the single-editor lease (editingBy/editingTabId, LEASE_STALE_MS=90000 @:565): Jon's uncleanly-closed browser left the lease held → reopened session saw a ghost holding it → silently read-only (edit affordances suppressed, writes rejected by the lease guard). Fix in the moment: reload after >90s → tab reclaims the expired lease → editable → cross wrote through (Jon confirmed). PERMANENT FIX ALREADY BUILT + reviewed: branch `gap5b-f015` (BroadcastChannel liveness → instant ghost-adopt, no 90s trap), held only for multi-device verification (G1–G16, needs Jon+Andrew+2nd device). ★ RECOMMEND prioritizing gap5b-f015 verify+ship — this cost Jon real work today.
     2026-07-23 — B054 (Jon, queue-for-later): Auto-assign Suppliers modal suggests vendor "ROYAL - SALT LAKE CITY" (e.g. for PANDUIT) which does NOT exist in BC — should be "Royal Wholesale". Bad/duplicate learned vendor name in manufacturerVendorMap (auto-assign learning). Same vendor-data-quality family as the PRJ402119 incident (sweep also showed "ROYAL - SALT LAKE CITY" as a distinct vendor on 11 rows). NOT urgent — parked; check when there's time (likely a vendor-name normalization/dedup + validate-against-BC-vendor-list fix).
     2026-07-23 — QUICK WINS built + shipping together (Jon: "do the quick wins"): ✅ B056 (trailing-dot boot guard in public/index.html <head> — self-corrects a dotted host before anything loads; Freddy inline). ✅ B055 (BC Item Browser overflow — Marc lane: minWidth:0 on the flex results container so overflow:auto engages horizontally + vendor cell maxWidth:200/ellipsis/title so long async vendor names don't push the far-right "Use" toggle off-screen). ✅ G018 (Debug Logs 500-cap — Marc lane: added a from/to date-window filter to DebugLogsModal — where createdAt >= fromMs / <= toMs, single-field range so no composite index; quick-range buttons + date window are mutually exclusive; reaches entries past the 500-most-recent cap). All display/query-only, validate_jsx + check-scope clean. Freddy reviewed diffs. -->
     2026-07-23 — G019 (Jon, live) stamped: HIDE all Engineering Questions UI (the "## answered" / "N ?" pulsing pill in QUOTE SUMMARY, the questions modal trigger `setEqModalPanelId`, the "Questions for Customer" block on printed quotes, + any dashboard/tile refs) — "develop this out another time." Approach: a single `QUESTIONS_ENABLED=false` flag gating every render surface (reversible — flip to true to restore intact). Display-only, no data change (panel.engineeringQuestions preserved). MUST grep exhaustively (engineeringQuestions / eq / "Questions" / on_quote / EqModal / setEqModalPanelId) so no reference strays through (enumeration is a floor). Queued behind the F044/F046/F047 lane (same file — avoid concurrent-edit clash). Freddy to build inline after that cluster deploys.
     2026-07-23 — F044+F046+F047 cluster ✅ BUILT + Freddy-reviewed + DEPLOYED. F046: per-row priceSetBy/priceSetAt stamped at ~35 price-write sites (BC apply/cross, alternates, templates, contingency, AI, portal, scrapers, manual, poll-accept, ECO w/ base-attribution-inherit, null-on-price-clear) — additive, data-safe, graceful. F047: extended the existing price tooltip → "Priced by {who} · {date} · {source}" with "not recorded" fallback for pre-F046 rows. F044: Send hard-blocks on ANY red row for non-managers (arcAlert+return, buttons disabled); MANAGERS/admins get a destructive force-send arcConfirm (Jon ruling — keep a manager override) with if(!_ok)return; both send paths (QuoteSendModal.handleSend + _doInlineQuoteSend); removed the old auto-flip-to-Budgetary + "Override(Send as Firm)" bypass. Print gate stays soft + "Send Quoted BOM to Customer" stays un-gated (Jon rulings). Limitation: F046/F047 forward-only (can't retro-trace pre-deploy prices). validate_jsx + check-scope clean.
     STILL OPEN after 2026-07-23 quick-wins: Quote Lifecycle & Lock epic (F048/F049/F051/F052), F044 (block-send-on-red), F046+F047 (audit trail + hover-approver), F050 (send-time plausibility gate; sweep tool exists), F062 ("RFQs to Accept" dynamic label — needs submission plumbing), gap5b-f015 (editing-lease ghost fix — BUILT on branch, needs multi-device verify), F041 broader supplier-matrix + dormant edges. PARKED: B053, B054. DEFERRED (pricing re-enable): F041b skip-expired, write-side plausibility gate, scraper extraction fix.
<!-- (log continues) -->
